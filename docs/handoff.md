# Конспект для следующей сессии

# РАЗДЕЛ 0 — КОРОТКО. Прочитать целиком, потом действовать

**Файл длинный (`wc -l < docs/handoff.md` → 3340) и почти весь — история.
Здесь всё, что нужно знать до первой правки. Подробности каждого пункта —
ниже, по ссылкам.**

> Число это устаревает от любой правки файла, поэтому рядом стоит команда:
> читателю дешевле пересчитать, чем поверить (I6). Стояло «3288» — верно до
> правок 25.08 (вечер), до того «3047» и «2880».
>
> **Привязка теперь к КОММИТУ, а не к дате, и это поправка того же дня.**
> Числу «3288 на 25.08» дата не помогла: за 25.08 файл правился несколько раз,
> и число разошлось с собой же внутри суток. Тот же случай разобран у прогона
> ниже («66 наборов»), там он стоил пяти часов.
>
> **Своего коммита это число назвать не может — оно живёт в файле, который
> этим коммитом и меняется.** Поэтому здесь стоит не хеш, а способ его
> получить: последний коммит, тронувший файл, — `git log -1 --format=%h --
> docs/handoff.md`, и тогда `git show <коммит>:docs/handoff.md | wc -l`
> проверяет число задним числом. Дата так не проверяется вовсе. Первая
> редакция этой врезки называла хеш прямо и была выдумкой: хеша ещё не
> существовало, коммит не был сделан.

## С ЧЕГО НАЧАТЬ — ЧЕТЫРЕ СТРОКИ, ОСТАЛЬНОЕ НИЖЕ

**1. ЗАНЯТОСТЬ ПОДКЛЮЧЕНА В ОБОИХ ЛИСТАХ, НО ПРОВЕРЕНА ГЛАЗАМИ ТОЛЬКО У
РАБОТОДАТЕЛЯ.** Лист набора дней сверен со снимком 25.08 — занятые дни
залиты, пояснение под сеткой на месте. **Лист ответа музыканта глазами не
проверен ни разу**, и это единственная половина проводки, которую никто не
видел. Смотреть у Теймура: предложение → «Cavab ver» → занятый день
предложения залит предупредительным, под сеткой блок «Bu gün məşğulsan».

**2. ПРОШЕДШАЯ ЗАНЯТОСТЬ УБРАНА — НА ТРУБКАХ НЕ ПРОВЕРЕНА.** Заливка теперь
только на будущих днях: на прошедшем выбора нет, а лишняя краска учит глаз
не замечать цвет, который завтра должен остановить руку. Смотреть: месяц
назад чист, впереди закрашено.

**3. СТРОКА ЛЕНТЫ ПЕРЕПИСАНА.** Даты **отрезками** («14–31 avqust» вместо
восемнадцати чисел), сверху **подпись инициатора** — «Rafael təklif edir» /
«Sən təklif etdin» (N164). **Перенос на вторую строку при переполнении принят
как плата** за видимые даты: типовая строка 45 знаков при измеренных 43,
обрезки нет намеренно.

**4. ИМЯ В ЦИТАТАХ (N141) СДЕЛАНО 25.08 — N141 ЗАКРЫТ НАПОЛОВИНУ, 2 ИЗ 5.**
`_replySenderName` переведён на `_otherUserCached`, сторож «е» расширен на
оба места, четыре порчи дали **+24 −1** и уронили ровно «е». **Третья ветвь:
в ГРУППЕ имени нет вовсе** (`_isDirectChatCached`) — там `_otherUserCached`
первый попавшийся участник, и подписать им чужую цитату значило бы записать
в данные уверенную неправду вместо неуверенной нелепости.

**Починены те двое, что ПИШУТ В ДАННЫЕ; ждут трое, и все трое только
рисуют** — это и есть довод, почему у них нет срока: `chat_screen.dart`
(подпись цитируемого в поле ввода; запасной путь заголовка прямого чата) и
`message_info_screen.dart` («$otherName oxudu»). Доля стоит в самой пометке
N141, потому что «починено» без доли читается как «починено всё» (I64).

**Лечения задним числом не будет** — записано решением: перепись прода 25.08
дала 10 цитат при одной расходящейся, а её **нечем отличить от переименования
человека** (N142).

---

**Где мы.** Работа над предложениями работы, четыре шага. **Шаги 0–3
сделаны, выложены и проверены на трубках** (шаг 3 — 22.08).

**ЗАНЯТОСТЬ ПОДКЛЮЧЕНА 25.08 В ОБОИХ ЛИСТАХ. ПОЛОВИНА ПРОВЕРЕНА НА ТРУБКЕ,
ВТОРАЯ ЖДЁТ.** Прогон зелёный: **68 наборов из 68 файлов на диске, 854
теста, 0 неуспешных** — состав `flutter test --reporter json`, сверен с
`find test -name '*_test.dart' | wc -l`; число тестов из отчёта
`flutter test --reporter compact` (`+854`). Снято 25.08 на рабочем дереве
поверх `c23aa03`.

> **ЗДЕСЬ СТОЯЛО «66 наборов, 811 тестов», И ЭТО БЫЛО ВЕРНО — ПЯТЬ ЧАСОВ.**
> Число снято той же командой (`flutter test --reporter json`) и с той же
> датой (25.08) на коммите `e37d57e`, 09:09;
> `git ls-tree -r --name-only e37d57e test | grep -c '_test\.dart$'` даёт
> **ровно 66**, то есть замер был честный. В 11:35 коммит `1085e83` добавил
> `offer_dates_line_test.dart` и `offer_feed_row_test.dart` — и число стало
> неверным, не изменившись ни на знак.
>
> **Разбиение сложено вслух и сходится в обе стороны (I13):** 66 + 2 = 68;
> 811 + 26 (два новых набора, `flutter test` по ним двоим) + 17 (дописано в
> шесть существующих наборов, правившихся после замера) = 854.
>
> **УРОК, РАДИ КОТОРОГО ВРЕЗКА И СТОИТ: у числа были И ДАТА, И КОМАНДА — и
> оно всё равно устарело.** I6 требует одно из двух; здесь было оба, и обоих
> не хватило. Дата в этом репозитории имеет цену суток, а суток мало: 25.08
> в `main` ушло несколько коммитов, два из них добавили наборы. Команда
> спасает того, кто её выполнит, — но она не говорит, что пора.
>
> **Отсюда поправка для чисел прогона: привязывать их к КОММИТУ, а не к
> дате.** «68 из 68 на `c23aa03`» проверяемо задним числом и врёт заметно —
> `git ls-tree` ответит про любой коммит. «68 из 68 на 25.08» не врёт вовсе,
> оно просто перестаёт что-либо значить к обеду.
>
> **Это НЕ противоречие, и разбирать его как противоречие было бы ошибкой.**
> Первое подозрение (25.08) звучало так: два числа при одной дате, оба с
> командами — значит одно из них неверно, и до третьего замера неизвестно,
> какое. Третий замер был сделан и показал третье: **неверных нет, есть
> устаревшее.** Это I7 дословно — устарела не находка, а её доказательство.

- **проверено глазами, снимок 25.08, трубка Рафаэля:** лист набора дней
  работодателя — занятые дни залиты, под сеткой «məşğul günləri də seçə
  bilərsən», строки про незнание нет. Это доказало дорогу
  `personalEventsProvider` + `eventsAsParticipantProvider` →
  `busyDaysProvider` → лист **на настоящих данных**, а не только в тестах —
  ту самую половину, которую тестом достать нечем (сторож там текстовый);
- **НЕ проверено: лист ответа музыканта** (сторона Теймура) и оба состояния
  незнания. Это первое дело следующей сессии.

**НА ТРУБКАХ СТОИТ `1085e83`, А НЕ ВЕРШИНА — И ЭТО ЗНАЧИМО.** Последняя
сборка 25.08, 11:36–11:44: Рафаэль (`00008101-001261402669003A`, кабель) —
Xcode build 125,1 с, установка и запуск **11,1 с**; Teymur
(`00008101-001A146911A3003A`, **по Wi-Fi**) — 132,1 с и **248,7 с**. У обоих
в логе `Flutter run key commands.`, подпись командой `9X7JFR6HXR`.

> **Чего на трубках НЕТ: `d9f1b8b` — починки источника имени.** То есть в
> ленте у них по-прежнему стоит «Naməlum təklif edir» на чужом предложении.
> Это **известный и уже исправленный** дефект, а не новый: увидите его —
> не заводите находку, пересоберите.

**Замер Wi-Fi за 25.08, четыре значения: 146,1 / 192,5 / 188,7 / 248,7 с** —
против 11,1 с по кабелю. Одно число сюда записывать нельзя, разброс почти
вдвое; вывод «переткнуть дешевле, чем ждать» держится с запасом.

> **Прежняя запись читается с датой:** 20.08 установка по сети занимала
> **465 с**. Сегодняшние четыре замера втрое-вчетверо меньше — то есть число
> 465 не свойство Wi-Fi, а один случай.

| Шаг | Состояние |
|---|---|
| 0 — сверить правила с продом | сделан 19.08, пересверен 20.08 |
| 1 — предложение в ленте | сделан, переделан в короткую строку (`66aaa37`) |
| 2 — музыкант отвечает | **сделан и выложен** (`3bfc93c`) |
| 3 — приём и создание вечеров пачкой | **СДЕЛАН И ПРОВЕРЕН НА ТРУБКАХ 22.08** |
| 4 — уведомления о ходах 2–4 (N130) | не начат; **после занятости**, решение владельца 22.08 |

**ПЕРЕСБОРКА ТРУБОК И ПРОВЕРКА N154 — СДЕЛАНЫ 22.08, БОЛЬШЕ НЕ ПЕРВОЕ
ДЕЛО.** Обе трубки собраны из `16a82b1`, то есть правка `3abb0e4` (N154) в
них есть. **Проверено глазами на трубке Рафаэля, снимок 22.08:** на
предложении с ответом стоят `TEYMUR ORUCOV CAVAB VERDİ`, `0 gün`,
`28, 30 — yox`, `Ətraflı`, `Təklifi geri götür`, и **строки ожидания
нет** — под шапкой пусто, как и решено 20.08. Дно листа на снимке видно,
значит «нет» сказано про весь лист, а не про видимую часть (I43).

Замер сборки 22.08, **обе трубки были на кабеле**, собирались по одной:
Рафаэль (`00008101-001261402669003A`) — Xcode build 95,2 с, установка и
запуск 11,0 с; Teymur (`00008101-001A146911A3003A`) — 79,9 с и 10,6 с.
Для сравнения, 20.08 по Wi-Fi одна установка занимала **465 с**: если
трубка не на кабеле, переткнуть кабель дешевле, чем ждать.

**ШАГ 3 ЗАКРЫТ 22.08 — ПРОВЕРЕН ОТ НАЧАЛА ДО КОНЦА НА ДВУХ ТРУБКАХ.**
Правила выложены (набор `a7968515`), предложение создано уже при них,
музыкант ответил, работодатель принял, вечер появился у обоих. **Проверено
глазами, снимки 22.08:** у обоих в составе `gəlir`, у музыканта вечер
открывается обычной карточкой, а не экраном приглашения.

**Два дефекта найдены на трубке в тот же заход и починены** — оба в составе
необратимой записи, и оба тестами не ловились: **N157** (музыканта
спрашивали второй раз о том, на что он уже ответил) и `createdAt`
ISO-строкой вместо отметки сервера. Разбор — N157 в `AUDIT_TODO.md`, там же
признак: **исчерпывающий на вид список полей — не то же, что полный.**

**ОТКРЫТО ПОСЛЕ ШАГА 3, из увиденного на трубке:** **N158** — «00:00» у
вечера без времени; **N159** — «принято» сказано дважды на одном экране. Оба
записаны и не чинились.

## ПЕРВОЕ ДЕЛО — ПРОВЕРИТЬ ЗАНЯТОСТЬ НА ТРУБКАХ

**Сделано 25.08, ждёт глаз.** Обе трубки видны (`flutter devices`, 25.08):
Рафаэль `00008101-001261402669003A` **на кабеле**, Teymur
`00008101-001A146911A3003A` по Wi-Fi. Собирать по одной, первым того, кто на
кабеле; Teymura дешевле переткнуть кабель, чем ждать (замер 20.08: 465 с по
Wi-Fi против 11,9 с по кабелю).

**Что смотреть — три состояния, и молчание только одно из них:**

1. **у музыканта** открыть предложение → «Cavab ver». В сетке ответа день,
   на который у него уже есть вечер, должен быть **залит** (тусклый голубой,
   `kOwnerOther` с альфой), и под сеткой — «məşğul günləri də seçə bilərsən».
   Строки «məşğul günlərin hələ göstərilmir» быть **не должно**. **Это
   единственный пункт, который ещё не смотрели;**
2. ~~у работодателя открыть «İş təklif et»~~ — **сделано 25.08, сошлось**
   (снимок в разделе 0);
3. **на медленной связи** (или сразу после запуска) в первый миг под сеткой
   обязана стоять строка «məşğul günlərin hələ göstərilmir», и **пропасть
   сама**, без закрытия листа, когда календарь ответит. Это N143 в третий
   раз, и тестом оно покрыто — но проверить глазами стоит, потому что тест
   не знает про настоящую задержку сети.

**Чего проверка НЕ докажет (I50):** что занятость верна на записях с `Z` —
их 13 из 72 (перепись 07.08) и все в прошлом, а сетки прошлое не предлагают.
Расхождение разборов записано находкой N161 и не чинилось.

## ЧТО СДЕЛАНО 25.08 — ЗАНЯТОСТЬ, ОБА ЛИСТА

**Соединены две готовые половины, своего кода — одна сборка.**

- **`lib/core/agreements/occupied_days.dart`** — новое правило
  `occupiedDaysOf`: какие СУТКИ человека заняты. Внутри ничего своего:
  `liveEvents` (дедуп, без отменённых, без нечитаемых дат) + `dayRoleOf`
  (приглашение не занимает, отказ не занимает) + `eventDay` (строка с `Z`
  приводится к местному времени);
- **`lib/features/job_offer/busy_days.dart`** — `busyDaysProvider`, один
  поставщик на оба листа, и в нём **вся честность**: `BusyDays.known`
  отличает «занятых нет» от «мы не смотрели». Признак незнания механический
  — `asData == null` у **обоих** потоков, по образцу
  `agreements_screen.dart:3702`; плюс пустой uid читается как «не знаем»;
- **проводка в двух местах:** `job_offer_sheet.dart` (лист ответа
  музыканта) и `job_offer_entry.dart` (лист набора дней работодателя), оба —
  через `Consumer` **внутри модалки**, иначе занятость, пришедшая после
  открытия листа, до экрана не доедет;
- **литерал `busyUnknown: true` снят** — вся неправда была в нём: он
  утверждал «не знаем» независимо от того, знаем мы или нет.

**ПРОШЕДШАЯ ЗАНЯТОСТЬ НЕ ЗАЛИВАЕТСЯ — решение владельца 25.08, принято по
виду на трубке в тот же день.** На снимке было залито полмесяца назад: 3, 4,
6–9, 12–17, 22, 24 августа. Довод записан целиком, потому что он объясняет,
почему это не косметика:

> **Заливка здесь не сообщение о календаре, а предупреждение о выборе.** На
> прошедшем дне выбора нет, предупреждать не о чем, а лишняя краска **учит
> глаз игнорировать тот же цвет, который завтра должен остановить руку.**
> Календарь показывает всё по-прежнему.

Снято в **обоих** листах и в **обеих** половинах показа — и в клетке, и в
поясняющей строке под сеткой: разойдись они, строка объявляла бы, что занятые
дни выбирать можно, там, где не покрашено ничего.

**«Сегодня» берётся одним способом, второго источника нет:** `isPastDay`
(`core/job_offer/offer_draft.dart:84`), и заливка берёт **готовую** локальную
`past`, посчитанную строкой выше для запрета нажатия. Прибитое тестом
«сегодня» (`now`) работает сразу на обе половины.

**ЗАНЯТЫЙ ДЕНЬ ГОВОРИТ, ЧЕМ ОН ЗАНЯТ — правка 25.08 по виду на трубке,
три части одним заходом.**

1. **Цвет предупредительный.** Стоял `kOwnerOther` с альфой — «взято из
   макета `mugam-14-secim`, а не подобрано на глаз». На устройстве он
   оказался **неотличим от невыбираемого дня**: обе клетки тускло-серые, и
   предупреждение читалось как «сюда нельзя». Взят набор `kWarn*` — тот же,
   которым говорит о занятом времени красная плашка в карточке вечера.
   **Макет проиграл трубке, и это записано в самом коде**, чтобы следующий
   не «вернул как в макете».
2. **Выбранный занятый день сохраняет рамку.** Золотая заливка выбора
   закрывала предупреждение целиком — то есть ровно в тот миг, когда человек
   на занятый день согласился. Так Teymur 22.08 и отметил день, где у него
   уже стояла работа.
3. **Нажал занятый день — сказано, ЧЕМ он занят, и по надписи открывается
   сам вечер.** Виджет один на оба листа (`BusyDayNotice`), дверь одна на
   проект (`eventDetailRoute`). **Вечера перечисляются ВСЕ**, каждый своей
   строкой и в свою карточку: два вечера в один день — обычная жизнь, и
   показать один значило бы спрятать второй молча (N51/I11).

**Поставщик из-за этого отдаёт не даты, а вечера** — `occupiedEventsByDayOf`,
а `occupiedDaysOf` теперь её ключи. Два прохода разошлись бы: набор сказал бы
«занято», список вечеров оказался бы пуст, и каждое выглядело бы правильным
порознь.

**И листы приняли ОДИН объект вместо трёх полей.** Было `busyDays` +
`busyUnknown`, стало `BusyDays` (дни, вечера, признак «знаем ли»). Побочная
польза оказалась крупнее правки: **умолчание стало «не знаем»** вместо
прежнего «мы посмотрели, всё свободно», а состояние «занятые дни есть, но мы
не знаем» стало **невыразимым по устройству**, а не запрещённым. Один тест
из-за этого переписан — он проверял противоречие, которого больше не бывает.

**Тестов стало +51 (774 → 825), наборов +2 (64 → 66).** Новые:
`test/occupied_days_test.dart` (14 — само правило),
`test/busy_days_provider_test.dart` (7 — три состояния, пустой uid, чей
календарь), четыре в `job_offer_sheet_live_test.dart` (настоящая проводка с
канарейкой), пять в `job_offer_days_sheet_test.dart` (показ трёх состояний),
один в `source_invariants_test.dart` (точка вызова спрашивает занятость).

**Семнадцать порч сделано и снято** (I10 — правкой на месте, не `git
checkout`): пять на подключении занятости, шесть на снятии прошедшей, шесть
на предупредительном цвете и надписи.
**Дважды предсказание разошлось с прогоном, и оба раза записано:** `read`
вместо `watch` уронил **три** вместо названного одного, снятие `toLocal()` —
**два** вместо названных трёх (разбор — N161). Одна порча оказалась
**неверной**: `asData!` на `null` уронил провайдер и потащил восемь тестов, к
правилу отношения не имеющих, — тот самый признак из I46, переделана на
правку значения. **Шесть порч на прошедшей занятости совпали с названным
поимённо, все шесть** — четыре по одному тесту (по одному на каждое место,
где решается «прошедшее ли»), и две канареечные: снятие заливки в сетке
ответа уронило **три**, в листе набора — **один**.

**Заведено находками:** **N160** (сторож проводки зелен по причине, не
связанной с тем, что сторожит — третий случай подряд, третьим способом),
**N161** (два разбора даты), **N162** (долг: отказ потоков календаря не
виден никому).

## РЕШЕНИЕ ВЛАДЕЛЬЦА 22.08, С ДОВОДОМ — ПОЧЕМУ ЗАНЯТОСТЬ БЫЛА ПЕРВОЙ

**Кандидатов было два — занятость и уведомления (шаг 4). Выбрана
занятость.** Довод владельца записан здесь целиком, а не пересказан: без
него следующий увидит два незакрытых долга и возьмётся за тот, что выглядит
крупнее.

> **Занятость идёт ПЕРЕД уведомлениями. Сегодня живьём выяснилось, что
> Теймур согласился на 25 августа, не видя, что день уже занят —
> предупреждение приложение показало после того, как отказаться стало
> нельзя. Это неверный ответ, данный вслепую, и он необратим: вечера
> созданы.**
>
> **Уведомления же чинят молчание — человек узнаёт позже, но узнаёт, и
> ничего необратимого не происходит.**
>
> **Плюс занятость дешевле: всё посчитано, `occupiesCalendarOf` и оба потока
> готовы, соединяются две половины. Строка `answer-busy-unknown` снимется тем
> же заходом, и её снятие проверяемо тестом.**

**Что это значит для порядка работ, одной строкой:** разница не в размере
долга, а в том, **что становится непоправимым**. Молчание отнимает время;
слепой выбор отнимает возможность выбрать иначе.

**«Где чинить — ровно одно место» ОКАЗАЛОСЬ ДВУМЯ, и второе нашлось до
кода** (I64: проверять не наличие правила, а каждого, кто под него
подпадает). Мест, где человек ВЫБИРАЕТ день, два: лист ответа музыканта и
лист набора дней работодателя. У обоих параметр `busyDays` был, поставщика
не было ни у одного — работодатель предлагал дни так же вслепую. **Решение
владельца 25.08: чинить оба, переменная одна — соединение поставщика с
потребителем, применённое дважды.**

**Оговорка про два незнания (I47) РЕШЕНА, и вот чем.** Признак незнания
механический — `asData == null` у **обоих** потоков, по образцу
`agreements_screen.dart:3702`; отдельно пустой uid. Листание по месяцам к
этому отношения не имеет вовсе: оба потока идут **без `limit` и без
диапазона дат**, то есть календарь приходит на все месяцы сразу, и стрелки
месяца не стоят ни одного чтения и не заводят нового состояния загрузки.

## ВТОРЫМ — ШАГ 4, УВЕДОМЛЕНИЯ О ХОДАХ 2–4 (N130)

Разбор целиком в `docs/plan.md`, «ПОРЯДОК ПОДКЛЮЧЕНИЯ». Сегодня три хода из
четырёх проходят молча, и после шага 3 это заметнее всего: **музыканта
взяли, а он не узнал** — молчание поставлено нарочно, именем
`lastActionType: 'agreed'`, и снимается оно **здесь и только здесь**. Внутри
шага 4 решается и то, чего нет ни у одной стороны: **одно уведомление на
предложение, а не по одному на каждый день.**

Доставка push на трубки проверена 18.08 в обе стороны, значит проверка
руками возможна.

**ПРАВИЛА ВЫЛОЖЕНЫ 22.08.** В проде набор
`a7968515-56c1-4738-a2cc-eb47e84f8bd0` от 22.08 02:00:48Z. Сверка после
выкладки — **канарейка 0, без `//` — 0**, при 1359 строках с обеих сторон;
канарейка к сверке (тот же `diff` против прежнего набора) — **37/2**, обе
строки те самые. Парный тест на оба предела заведён ДО выкладки:
`functions/test/job-offer-rules.test.ts`, 7 проверок, из них 2 возвратные.
Точка отката — прежний набор `2a8410bb-…`, команда в `docs/deployed.md`
(в проекте не запускалась ни разу).

**ДОЛГ РЯДОМ, ПОРЯДКА МЕЖДУ НИМ И ЗАНЯТОСТЬЮ НЕ ЗАДАНО:** уборка мёртвого
хода в карточке (`onSendAnswer`, `canPickDays`, `canSend`, `_checkbox`,
`_picked` и их тесты — **одним заходом**, по одному не разбирать). Он мелкий
и ни на что не влияет; занятость решена первой (выше), уведомления вторыми.

**Числа по занятости УСТАРЕЛИ 25.08 и оставлены как история:** здесь стояло
«`busyDays` имеет трёх потребителей и **ноль** поставщиков». Поставщик
теперь есть — `busyDaysProvider`, — и подключён к двум потребителям из
трёх. **Третий не подключён и не забыт:** `JobOfferCard.busyDates`
(`job_offer_card.dart:57`) — карточка предложения в ленте только ПОКАЗЫВАЕТ
дни, выбора там нет, и правило «человек не выбирает вслепую» на неё не
распространяется. Захочется пометить занятые дни и там — поставщик готов,
работа на одну строку.

> **ЦЕНА ЭТОГО ДОЛГА ИЗМЕРЕНА 22.08, А НЕ ПРЕДПОЛОЖЕНА, и потому работа
> перестала быть необязательной.** В первом же настоящем прогоне шага 3
> музыкант отметил 25 августа, **не видя, что у него на этот день уже есть
> работа** — «Toy · 15:00». Узнал он об этом из красной плашки «Bu gün artıq
> tədbirin var» **в карточке вечера, после принятия**, то есть когда
> отказаться уже нельзя: отзыв принятого закрыт правилом.
>
> Занятость в приложении **есть и работает** — плашка её и показала. Не
> подключена она ровно в одном месте: в сетке, где музыкант отмечает дни
> (`busyUnknown: true` в `job_offer_sheet.dart`). Человек выбирает вслепую
> там, где данные уже посчитаны.

**ЧТО ЗНАТЬ ПРО ПРОВЕРКИ, ИНАЧЕ ЗЕЛЁНОЕ ПРОЧТЁТСЯ ШИРЕ, ЧЕМ ОНО ЕСТЬ:**
запись ответа в Firestore **тестами не достаётся** — подделки Firestore в
проекте нет, эмулятор только у `functions`. Зелёный прогон означает «ответ
дошёл до писателя целым», а не «ответ в базе». Единственное подтверждение
записи — снимок с трубки 20.08.

**СТРОКА ПРЕДЛОЖЕНИЯ В ЛЕНТЕ ПЕРЕПИСАНА 25.08 — ДАТЫ ВМЕСТО ЧИСЕЛ.**
Было «1/2 gün · Toy · qəbul edildi», стало «25 avqust · 1/2 gün · Toy ·
qəbul edildi», плюс верхняя строка «Rafael təklif edir» / «Sən təklif
etdin» (N164).

> **Довод измерен, а не предположен.** Владелец увидел в ленте две
> одинаковые строки и спросил, два это предложения или одно показано
> дважды. Спросили прод (`runQuery` по `offers`, 25.08): **два разных** —
> `2OUMbfWR` (25, 26 → отмечен 25) и `iGaE4pu5` (22, 23 → отмечен 22). Число
> говорит сколько, человеку нужно какие.

**Числа этой работы, чтобы не пересчитывать:** в проде **19 предложений**,
все в одном чате; дат в предложении — медиана 2, максимум **18** (14–31
августа); **через границу месяца 0 из 19**; сплошным отрезком — 13 из 19.
Строка ленты держит **43 знака** (295 pt под текст, 6,84 pt на знак — снято
по снимку с трубки). Перечислением не влезает 1 набор из 19, **отрезками — 0
из 19**, поэтому хвоста «və daha N» нет: сжатие честнее, оно называет все дни.

**Типовая строка — 45 знаков, то есть переносится на вторую.** Это плата за
видимые даты, решение владельца 25.08; сторожа на длину нет намеренно —
проверка «не длиннее 43» краснела бы и от улучшения.

**ИСТОЧНИК ИМЕНИ ПОЧИНЕН В ПОДПИСИ ПРЕДЛОЖЕНИЯ (25.08), В ЦИТАТАХ — НЕТ.**
Подпись брала `chatData['name']` — поле, которое **mugam-v2 пишет со стороны
создателя чата и не обновляет**, отчего вторая сторона читает там своё имя
либо пустоту; на трубке это дало «Naməlum təklif edir». Переведено на
`_otherUserCached`, сторож стоит (`source_invariants_test.dart`, «е»).

**Перепись цитат снята заново 25.08: 10 цитат, расхождение у одной** («Q1»
против «Rafael Dagli», тот же документ, что 19.08), **пустых имён ноль** —
дорога открыта, но по ней ещё не проходили. Первая редакция переписи дала
**ноль цитат**: искала плоское `replyToSenderName` (имя в модели Dart), а в
базе лежит карта `replyTo` с ключом `senderName`. Поймали канарейки — 408
сообщений и 12 учёток при нуле цитат не сходятся.

**ОТКРЫТО И ЖДЁТ, ВОСЕМЬ на 25.08** (N141 среди них: подпись предложения
починена, цитаты нет). Было четыре; 25.08 заведены **N161**
(два разбора даты — плашка считает по Гринвичу, календарь по местным суткам),
**N162** (долг: отказ потоков календаря не виден никому) и **N163** (сторож
на согласие двух функций слеп к порче, сдвигающей обе одинаково — ловит
расхождение, не ловит общий сдвиг; для сдвига нужен тест на само значение).
**N164** (направление предложения в ленте не выражено ничем) заведён и
починен тем же заходом; **N165** — комментарий, объясняющий поле, живёт у
одного читателя, а не у поля (я наступил на N141 вторым, через шесть дней
после того, как сам её записал) — заведён, сторож поставлен на двоих
известных читателей, сырая карта чата наружу отдаётся по-прежнему. **N160** заведён и в своём месте закрыт тем же заходом — сторож проводки
переписан, — но обход остальных сторожей проводки по его признаку **не
делался**. Обход по признаку N163 — тоже не делался, числа нет.

- **N152** — правило ответа (`firestore.rules:801-806`) не сторожится ни
  одним тестом; нужен файл в `functions/test/` с эмулятором.
- **N153** — у предложения с прошедшими датами клетки не нажимаются и не
  сказано почему.
- **N155** — обход тестов по осям «состояние данных × подключённость» не
  делался.
- **N156** — **тест, поднимающий виджет своими руками, не может сторожить
  проводку: он подаёт то, отсутствие чего проверяет.** Второй случай подряд
  после N151. Перепись 22.08: **14 пар** «тест строит виджет и подаёт
  обработчик, который прод может не передать», проводка покрыта **у одной**
  (`JobOfferCard.onOpenAnswer`). Список — N156 в `AUDIT_TODO.md`. Не
  чинился по указанию владельца: принесён список.

**Правила работы — `CLAUDE.md`, читать до всего.** Свежайшее там — **I64**:
проверять не наличие правила, а каждого, кто под него подпадает.

---

**Состояние — на 20.08, конец дня. Подробности первого дела — ниже.**

# ЗАКРЫТО 22.08: ПЕРЕСБОРКА ОБЕИХ ТРУБОК И ПРОВЕРКА N154

**СДЕЛАНО 22.08 — раздел оставлен как история, делать по нему нечего.**
Обе трубки собраны из `16a82b1`, проверка у Рафаэля прошла: пять
ожидавшихся строк на месте, строки ожидания нет. Замеры и снимок — в
разделе 0. Ниже — то, ради чего пересборка затевалась.

**НА ОБЕИХ ТРУБКАХ БЫЛА СБОРКА БЕЗ ПРАВКИ N154.** Последняя выкладка на
устройства — 20.08 из коммита `3bfc93c`; правка `3abb0e4` в них **не
попала**. Пересборка была начата и снята по решению владельца — доделана
22.08.

**Что в них не так, конкретно:** у **Рафаэля** на предложении, где Teymur
уже ответил, под шапкой стоит **ложная строка ожидания** «Teymur Orucovdan
cavab gözlənilir» — то есть экран спорит сам с собой. Разбор — N154.

**ЧТО ДОЛЖНО БЫТЬ ПОСЛЕ ПЕРЕСБОРКИ** (открыть у Рафаэля то самое
предложение):

- шапка **`TEYMUR ORUCOV CAVAB VERDİ`**
- крупно **`0 gün`**
- **`28, 30 — yox`**
- **`Ətraflı`**
- **`Təklifi geri götür`**
- **и НИ СЛОВА про ожидание.** Вместо строки — ничего: молчание, решение
  владельца 20.08. Правда уже написана трижды выше, а «принять пока
  нельзя» объясняло бы человеку наше расписание работ, а не его дело.

**Порядок сборки:** по одной за раз, первой та, что на кабеле. 20.08 на
кабеле был Рафаэль (`00008101-001261402669003A`), Teymur
(`00008101-001A146911A3003A`) — по Wi-Fi, и установка у него заняла
**465 с** против 11,9 с по кабелю. Переткнуть кабель на Teymura дешевле,
чем ждать восемь минут на каждую проверку.

# ЧТО СДЕЛАНО 20.08

**ШАГ 2 ЗАКРЫТ И ВЫЛОЖЕН — `3bfc93c`.** Музыкант отвечает: «Cavab ver» в
листе предложения → `JobOfferAnswerSheet` поверх → `setMyAnswer` пишет
`answers.<uid>`. Подробности («Ətraflı») намеренно **не перенесены** в лист
ответа — перенос завёл бы второе место, где рисуются те же данные, и они
разошлись бы, как разошлись `eventTime` с `details` (N139).

**ПОСЛЕДНИЙ СТЫК ПОДТВЕРЖДЁН СНИМКОМ С ТРУБКИ, А НЕ ТЕСТОМ, и это стоит
знать до того, как полагаться на зелёный прогон.** У второй трубки
появились «0 gün» и перечисленные отказанные дни — значит `setMyAnswer`
доехал до Firestore и вернулся на другое устройство. Widget-тесты этого
доказать не могут: подделки Firestore в проекте нет
(`fake_cloud_firestore`, `mocktail`, `mockito` — ни одной в
`pubspec.yaml`), эмулятор есть только у `functions`. **Зелёный прогон
означает «ответ дошёл до писателя целым», а не «ответ в базе».**

**Найдено и починено сверх плана:** N146 (микрофон без адресата, правило
соблюдено у трёх из четырёх → **I64** в `CLAUDE.md`), N147 (обход: 53
привязки, 19 могут быть `null`, три живые дырки; две в `topbar.dart`
починены), N154/N155 (ложная строка ожидания и тесты, бравшие прошлое и
будущее, но не настоящее).

**Записано и НЕ чинилось:** N152 (правило ответа
`firestore.rules:801-806` не сторожится ни одним тестом), N153 (у
предложения с прошедшими датами клетки не нажимаются и не сказано почему),
N148 закрыт.

# ПОРЯДОК ДАЛЬШЕ

**1. ШАГ 3 — `JobOfferAcceptSheet`, создание вечеров пачкой, и ВЫКЛАДКА
ОБОИХ ПРЕДЕЛОВ ОДНИМ ДЕПЛОЕМ.** Единственный необратимый шаг работы, несёт
одну переменную (I48). Разбор целиком — `docs/plan.md`, «ПОРЯДОК
ПОДКЛЮЧЕНИЯ», шаг 3.

**2. УБОРКА МЁРТВОГО ХОДА В КАРТОЧКЕ — отдельным коммитом ПОСЛЕ шага 2, и
это решение, а не забывчивость.** `onSendAnswer`, `canPickDays`, `canSend`,
`_checkbox`, `_picked` и их тесты снимаются **одним заходом**. Довод
владельца: пока живой ход не работал, мёртвая отметка была единственным,
что показывало, как ход выглядел.

**3. ЗАНЯТОСТЬ — соединение двух готовых половин, не новая работа.**
Потребителей у `busyDays` три, передач **ноль** (замер 20.08: `grep -rn
"busyDays\|busyDates" lib` → 13 совпадений). Поставщик есть:
`occupiesCalendarOf` (`event_conflict_banner.dart:102`) поверх
`dayRoleOf(e, uid) == DayRole.occupied`, плюс потоки
`personalEventsProvider` и `eventsAsParticipantProvider`. **Свой вопрос у
этой работы один:** лист листается по месяцам, и «занятости нет» обязано
отличаться от «ещё не загрузилось» (I47). Пока не соединено — лист говорит
об этом словами: `məşğul günlərin hələ göstərilmir`, ключ
`answer-busy-unknown`. **Снять строку — часть этой работы**, ключ для того
и поставлен, чтобы снятие проверялось тестом, а не памятью.

# ПРАВИЛА НЕ ВЫЛОЖЕНЫ

В проде набор `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe`, `updateTime` **14.08
01:32:44Z** (спрошен у `/releases`, а не взят из записи). **Расхождение
20.08: канарейка 37, без `//` — 2.** Обе строки названы дословно в
`docs/deployed.md`, обе добавлены в файл, из прода не пропало ничего:
`dates.size() <= 31` и `eventType.size() <= 24`. **Выкладываются вместе, в
шаге 3** — решение владельца 20.08: выложить раньше значит оторвать правило
от работы, которую оно защищает.

---

# СТАРАЯ ТОЧКА СТАРТА — ОСТАВЛЕНА ДЛЯ ИСТОРИИ, РАБОТУ ОТСЮДА НЕ НАЧИНАТЬ

Ниже — раздел, каким он был утром 20.08, до шага 2. Числа в нём верны на
своё время.

## ЧТО СДЕЛАНО К 20.08 — чтобы не начать заново уже сделанное

**Шаг 0 (сверить `firestore.rules` с продом) — СДЕЛАН 19.08.** Разбор и
команда сверки — `docs/deployed.md`. **Но с тех пор файл ушёл вперёд, и это
главное, что надо знать про правила** — см. строку 2 ниже.

**Шаг 1 (`JobOfferCard` в ленту) — СДЕЛАН И ПЕРЕДЕЛАН.** Карточка в ленте
прожила один день: 19.08 лента переведена на **короткую строку**
(«2 gün · Toy · cavab gözlənilir»), а карточка открывается **листом**
поверх. Коммит `66aaa37`. Сворачивание списка дней снято вместе с этим —
у листа своя прокрутка.

**Собрано на обе трубки 20.08, `flutter run --release`, обе встали:**
Рафаэль (кабель, `00008101-001261402669003A`) — сборка 215,7 с, установка
13,5 с; Teymur (Wi-Fi, `00008101-001A146911A3003A`) — 134,0 с и 236,7 с.
Подпись обеим командой `9X7JFR6HXR`.

**Лист предложения проверен глазами на трубке 20.08, и проверка нашла
дефект.** У получателя лист рисовал заголовок, «1 gün · Toy», день — и
**кружок с микрофоном, который был единственным нажимаемым местом и не
делал ничего**. Причина: правило «кнопка без адресата не рисуется» было
записано в `job_offer_card.dart` дважды словами и соблюдено у трёх действий
из четырёх. Починено, сторож поставлен — **N146**, правило вынесено в
`CLAUDE.md` как **I64**: *проверять не наличие правила, а каждого, кто под
него подпадает*.

**Обход того же класса по `lib` — N147:** 53 привязки, 19 могут быть
`null`, **три живые дырки**. Две (`topbar.dart`, колокольчик и «AZ» на
главном экране) починены 20.08 со сторожем `test/topbar_dead_buttons_test.dart`.
**Третья — N149, закрыта 22.08 решением владельца: квадрат снят, отмена не
подключена.** Довод владельца: нажимаемое, которое ничего не делает, хуже
отсутствия; подключение отмены — своя работа со своим вопросом (что делать
с уже выгруженной частью). Сторож — `test/upload_cancel_dead_button_test.dart`,
**5 тестов**. **Обход N147 закрыт целиком: три дырки из трёх.**

**Пустота под карточкой на листе — не поломка, а состояние шага 2:**
`JobOfferAnswerSheet` до сих пор ни разу не вызван (`grep -rn
"JobOfferAnswerSheet" lib` → только свой файл и два комментария). Кнопка
«Cavab ver» не рисуется именно поэтому.

## ДВЕ ИСПОЛНЯЕМЫЕ СТРОКИ — БЫЛИ ТОЧКОЙ СТАРТА УТРОМ 20.08, ШАГ 2 С ТЕХ ПОР СДЕЛАН

> **Отсюда НЕ начинать.** Заголовок переписан вечером 20.08: строка 1
> (подключить `JobOfferAnswerSheet`) закрыта коммитом `3bfc93c`, строка 2
> (выкладка правил) не делалась и уехала в шаг 3. Живая точка старта —
> в самом верху файла.

**Первая — согласованный 19.08 шаг 2. Вторая — НЕ из того согласования, и
это сказано прямо:** по `docs/plan.md` выкладка правил входит в **шаг 3**, а
не идёт рядом с шагом 2. Она стоит здесь потому, что просрочена с 19.08 и о
ней легко забыть, — но **порядок работы она не меняет: шаг 2 делается без
неё.**

**1. ШАГ 2 — ПОДКЛЮЧИТЬ `JobOfferAnswerSheet`.** Передать `onOpenAnswer` в
`JobOfferCard` из `job_offer_sheet.dart` (сейчас `_card` передаёт только
`onWithdraw`) и открыть им экран ответа. Как только обработчик появится,
кнопка нарисуется сама — условие уже стоит. Заодно снять мёртвый ход
внутри карточки: `onSendAnswer`, `canPickDays`, `canSend`, `_checkbox`,
`_picked` и их тесты — **одним заходом, а не по одному** (решение записано
у поля `onSendAnswer`).

**2. ВЫКЛАДКА ПРАВИЛ — ЭТО ШАГ 3, А НЕ ШАГ 2, И СЕЙЧАС ОНА НЕ ДЕЛАЕТСЯ.**
Строка стоит здесь **справкой**, чтобы про долг не забыли, — и она **не
входит в ближайшую работу**.

**ШАГ 2 ПРАВИЛ НЕ ТРЕБУЕТ ВОВСЕ:** `answers.<uid>` уже разрешён выложенным
набором `2a8410bb-…` от 14.08. В `docs/plan.md` это сказано прямым словом:
«Правила на этот ход уже выложены; новых не нужно». Два непрошедших предела
принадлежат **шагу 3** — «выкладывается **до** первого прогона создания
вечеров, не после».

**РЕШЕНИЕ ВЛАДЕЛЬЦА 20.08: СЕЙЧАС НЕ ВЫКЛАДЫВАТЬ.** Довод записан, потому
что иначе он потеряется и деплой сделают «заодно»: **оба предела нужны шагу
3, и выкладка принадлежит ему.** Выложить сейчас — оторвать правило от
работы, которую оно защищает; к шагу 3 оно станет «выложено когда-то, зачем
— в сообщении коммита», то есть ровно тем, от чего лечит I35.

**Сверка переснята 20.08, только чтение. Три числа: код возврата 0,
канарейка 37, без `//` — 2.** Набор в проде спрошен у него самого
(`/releases` → `cloud.firestore` → `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe`,
`updateTime` 14.08 01:32:44Z). Обе расходящиеся строки, дословно, обе
**добавлены в файл**, из прода не пропало ничего:

```
>           && request.resource.data.dates.size() <= 31
>           && request.resource.data.eventType.size() <= 24
```

Файл **1359** строк против **1328** в проде. Команда сверки и разбор — в
`docs/deployed.md`, там же числа заменены на эти.

**ОБЕ СТРОКИ ЖДУТ ОДНОГО ДЕПЛОЯ, вместе** — так решено в `26be038`.
**Пределов в шаге 3 два, а не один**, и с 20.08 оба занесены в
`docs/plan.md` вместе с доводом к каждому. До 20.08 `eventType.size() <= 24`
не был назван в плане ни разу и жил только в сообщении `26be038`.

> **ЗДЕСЬ СУТКИ СТОЯЛА МОЯ ОШИБКА, И ОНА ОСТАВЛЕНА ВИДНОЙ.** 20.08 я
> написал в этом месте, что расхождение «**не измерено**» и что я «не
> выдумываю число». Неправда: перемер был сделан 19.08, точный, и лежал в
> сообщении `26be038` — «37 СТРОК, ИЗ НИХ ИСПОЛНЯЕМЫХ 2». Я прочитал
> устаревшую запись в `docs/deployed.md` и не спросил `git log` по файлу,
> который её испортил. **Признак на будущее: прежде чем объявить число
> неизмеренным — `git log -1 --format=%B` по последней правке того файла**
> (I36: «замер недоступен» проверяется попыткой, а не оценкой). Разбор —
> **N150**, помечен красным как рецидив I35.

**Порядок целиком — `docs/plan.md`, «ПОРЯДОК ПОДКЛЮЧЕНИЯ»:** 0 правила
(сверка сделана, выкладка нет), 1 карточка (сделана, переделана под
короткую строку), 2 экран ответа, 3 экран приёма вместе с созданием
вечеров пачкой, 4 отправитель уведомления (**N130**). Решение о создании
вечеров принято 18.08 разбором до кода, **код не написан**.

---

**PUSH НА iOS — ниже, разделом 0-А. Связка закрыта 18.08**: дорога от
записи в базу до баннера на чужой трубке пройдена в обе стороны. Читать
ради того, что этим доказано и что осталось непроверенным, — **но начинать
работу не оттуда.**

---

# РАЗДЕЛ 0-А. PUSH НА iOS ДОШЁЛ ДО ТРУБКИ — 18.08, ПЕРВАЯ ДОСТАВКА ЗА ВСЮ ИСТОРИЮ ПРОЕКТА

**ЗАМЕР 18.08: отправка из консоли Firebase Cloud Messaging («Отправить
тестовое сообщение») на токен Теймура — БАННЕР ПРИШЁЛ** на «iPhone Guitar
Prosessor». Токен, **впервые записанный 14.08 и с тех пор не менявшийся**,
оказался жив спустя четыре дня: переполучать не понадобилось.

**Осторожно с `updatedAt` — он тут не свидетель (перемер 18.08).** В базе
поле показывает **`03:47:11Z` сегодняшнего числа**, а не 14.08 19:49:10Z:
оно переписывается при каждом перезапуске приложения. **Токен при этом тот
же** — сверена вся строка, 142 знака совпадают со снятой 14.08
посимвольно. Чем получено: обход `collectionGroup('pushTokens')` 18.08,
только чтение, плюс сверка строки целиком. Почему поле так себя ведёт —
N132 в `AUDIT_TODO.md`.

**Доказано этим пять звеньев:** ключ APNs `VU77A6RG22` работает, привязка
к `com.mugam.mugamFlutter` верна, entitlement `development` (APNs sandbox)
в бандле есть, разрешение на устройстве выдано, токен живой и адресует
эту трубку. **Не доказано ничего из `functions/src`:** сообщение шло из
консоли, `onNewMessage`, `sendPushToUid` и `sendCallPushToUid` в проверке
не участвовали. Разбор целиком — `AUDIT_TODO.md`, раздел N25-push,
«ДОСТАВКА PUSH НА iOS СОСТОЯЛАСЬ».

**PUSH ХОДИТ С ТРУБКИ НА ТРУБКУ — 18.08, ТРИ ПРОГОНА, ОБА НАПРАВЛЕНИЯ.**
Теймур → Рафаэль (04:19:51Z) — баннер пришёл, **тап открыл нужный чат**;
Рафаэль → Теймур (04:20:44Z) — не пришло; Рафаэль → Теймур повтором
(04:25:36Z) — пришло. Триггер `onNewMessage` поднялся **трижды из трёх**
(записи 04:19:53, 04:20:45, 04:25:37 — задержка 0,88–1,8 с).

**Этим закрыт НАШ код, а не только связка с Apple:** триггер
(`index.ts:425`), чтение `pushTokens` получателя (`:523-526`),
`sendFcmPush` по каждому документу (`:529-532`), и попутно — что
`data {chatId, type, senderId}` (`:517-519`) доходит и разбирается
клиентом.

**Прогон 2 — не поломка:** тем же кодом в ту же сторону прогон 3 дошёл.
Данные указывают на **подавление присутствием** (у Теймура тот же чат в
`activeChatId`, приложение было живо — его токен переписан через 31 с),
но **прямого замера нет: удачный путь `onNewMessage` не пишет ни строки**,
а `lastSeen` перезаписывается и истории не хранит. Разбор с оговорками —
`AUDIT_TODO.md`, раздел N25-push.

**Версия «дело в сборке на Теймуре» снята:** push на него дошёл дважды —
из консоли и в прогоне 3, тем же приложением и тем же токеном.

**Токены целы: 4 документа, как и до прогонов; `pruneIfTokenDead` не
срабатывала. Предупреждений и ошибок в журналах ноль за 30 минут** по
всем службам при работающей канарейке.

**ВТОРАЯ ТРУБКА СОБРАНА 18.08.** `flutter run --release -d
00008101-001261402669003A` (Рафаэль, кабель, iOS 26.0) встал: Xcode build
161,4 с, установка 14,5 с, **на подписи и профиле не споткнулся** —
подписано командой `9X7JFR6HXR`, устройств в профиле разработки **2**.
Токен Рафаэля появился сразу: документ
`8e75f79fe5240f12ac25e0d116e20144`, `updatedAt` `2026-08-18T04:13:21Z`,
владелец сверен по `users` — **Rafael Dagli**. Документов `pushTokens`
стало **4 при 12 пользователях** (было 3), разбор — `AUDIT_TODO.md`,
раздел N25-push.

> **ЗАПИСЬ ОТ 18.08, И ТОЧКОЙ СТАРТА ОНА БЫТЬ ПЕРЕСТАЛА.** Шаг 0 сделан
> 19.08, шаг 1 сделан и переделан под короткую строку, шаг 2 закрыт 20.08
> (`3bfc93c`). Сверка правил переснята 20.08 — 37/2, разбор в
> `docs/deployed.md`. Живая точка старта — **в самом верху файла**.
> Помечено вечером 20.08: до этого файл в трёх местах звал себя точкой
> старта, и все три указывали на разное.

**~~ТОЧКА СТАРТА СЛЕДУЮЩЕГО ЗАХОДА~~ — ШАГ 0: СВЕРИТЬ `firestore.rules` С
ПРОДОМ.** Выкладка 14.08 (набор `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe`)
записана **только в сообщении коммита `71d2ae9`, в `docs/deployed.md` её
нет** (I35), — значит текст прода снимается заново, командой из
`docs/deployed.md`. Стоит первым потому, что при старых правилах
отказывает уже создание предложения, а человеку это видно как «кнопка не
работает», то есть весь заход объяснится не тем.

**Дальше шаг 1 — `JobOfferCard` В ЛЕНТУ.** Он открывает всё остальное:
пока карточки нет, музыкант видит якорь обычным текстом, и ходы 2–4
недостижимы руками (замер 18.08: `JobOfferCard`, `JobOfferAnswerSheet`,
`JobOfferAcceptSheet` — по **0** вхождений в `lib` вне своих файлов,
канарейкой `JobOfferDaysSheet` → 1).

**Порядок целиком — `docs/plan.md`, «ПОРЯДОК ПОДКЛЮЧЕНИЯ»:** 0 правила,
1 карточка, 2 экран ответа, 3 экран приёма вместе с созданием вечеров
пачкой, **4 — отправитель уведомления (N130)**.

**N130 — ЧЕТВЁРТЫЙ ШАГ, А НЕ ПЕРВЫЙ, и здесь это сказано прямо, потому
что раньше стояло обратное.** Связка push закрыта: дорога от записи в
базу до баннера на чужой трубке пройдена целиком и в обе стороны, и
осталось то, что от неё не зависит и **никогда не работало не по вине
Apple**: ответ музыканта, принятие и отзыв предложения не порождают ни
уведомления, ни следа — **на подколлекцию
`chats/{chatId}/offers/{offerId}` не подписан ни один триггер** (обход
14.08: 14 триггеров, попаданий по `offers/` — ноль). Три хода из
четырёх происходят молча. **Но делать отправителя раньше шага 3 нельзя
по порядку владельца 14.08:** уведомление, ведущее туда, чего ещё нет,
обещает экран и приводит в пустоту.

**Чего в этом заходе так и не проверили** (полный список — в
`AUDIT_TODO.md`, N25-push): push при **полностью закрытом** приложении,
общий веер `sendPushToUid`, звонки `sendCallPushToUid`, Android-сторона,
и двенадцать положительных проверок с четырьмя «не должно прийти».

## ЧТО ИЗМЕНИЛОСЬ 14–15.08 — числами

Apple Developer Program **оплачен и активирован 14.08** (письмо пришло).
Team ID — **`9X7JFR6HXR`**, тот же, что стоял в проекте с 30.06: платное
членство легло на существующую команду, новой не завело. Менять
`DEVELOPMENT_TEAM` не понадобилось.

**ПЕРЕПРОВЕРЕНО 18.08, ПЕРЕД КОММИТОМ, И ЭТО ВАЖНЕЕ, ЧЕМ КАЖЕТСЯ.**
Team ID **проверен и не менялся**: `9X7JFR6HXR` стоит в **3 из 3**
конфигураций (Debug, Release, Profile) — и в рабочем дереве, и в версии
из коммита (`git show HEAD:ios/Runner.xcodeproj/project.pbxproj | grep -c`,
канарейка «любой `DEVELOPMENT_TEAM`» → 3). **Постороннего Team ID в
проекте нет и не было:** `JXNMVFYS7X` — **0** вхождений по всему дереву
(`grep -rn --exclude-dir=.git --exclude-dir=build --exclude-dir=Pods`,
канарейка `9X7JFR6HXR` тем же проходом → 7 строк) и **0** коммитов в
истории (`git log --all -S "JXNMVFYS7X"`).

Записано потому, что в разговоре 18.08 прозвучало обратное — будто в
проекте стоял чужой Team ID, а Xcode заменил его на верный. **Такой
правки не было**: пять строк 15.08 подключают файл прав, `DEVELOPMENT_TEAM`
среди них нет ни одной. Проверка стоила минуты и заняла место догадки
(I57: спрашивать не «когда считали», а **чем это считано**).

| Признак | Было | Стало | Чем снято |
|---|---|---|---|
| `CODE_SIGN_ENTITLEMENTS` в проекте Xcode | **0** | **3** | `grep -c` по `project.pbxproj`, канарейка `DEVELOPMENT_TEAM` → 3 |
| `aps-environment` в профиле | **0** | **1** | `grep -a -c` по `.mobileprovision`, канарейка `get-task-allow` → 1 |
| срок профиля | 2026-08-20 | **2027-08-14** | `grep -a -A1 ExpirationDate` |
| устройств в профиле | 1 | **2** | `grep -a -A5 ProvisionedDevices` |
| `LocalProvision` (признак бесплатной подписи) | 1 | **0** | `grep -a -c` |
| документов `pushTokens` в проде | **2** | **3** | обход `users`→`pushTokens`, 14.08 и 15.08 |
| живых iOS-токенов | **0** | **1** | там же |

**Главная находка захода: «entitlement на месте» в `docs/plan.md:2617`
было неправдой в той части, которая решает.** Файл
`ios/Runner/Runner.entitlements` лежал с `aps-environment`, но в проект
Xcode подключён **не был** — `CODE_SIGN_ENTITLEMENTS` в `project.pbxproj`
отсутствовал вовсе, и собранный бандл прав на push не имел (`grep -a -c
"aps-environment"` по `embedded.mobileprovision` собранного 14.08 → **0**
при канарейке `get-task-allow` → 1). Утверждение было про существование
файла, а не про то, что он применён (I57).

**И ЭТО, А НЕ БЕСПЛАТНЫЙ АККАУНТ, — НАСТОЯЩАЯ ПРИЧИНА, ПО КОТОРОЙ PUSH НА
iOS НЕ РАБОТАЛ НИКОГДА.** Разница не в словах: бесплатный аккаунт объясняет
отсутствие доставки **снаружи**, и с ним ничего нельзя было сделать до
оплаты; неподключённый файл прав лежал **внутри репозитория** всё это
время, и починка стоила пяти строк.

Точнее, чем «файл лежал»: `ios/Runner/Runner.entitlements` **был в
репозитории и отслеживался git** (`git ls-files` его находит) — то есть
выглядел как сделанная работа. При этом `CODE_SIGN_ENTITLEMENTS`
отсутствовал **во всех трёх конфигурациях** (Debug, Release, Profile;
было 0, стало 3), значит собранный бандл прав на push не имел **ни в
одной сборке**, и `getToken()` падал бы даже на платном аккаунте.

Подключено **15.08, пять строк** в `project.pbxproj`; закоммичено
**18.08**, после того как доставка подтвердила подпись делом.

## ЧТО СДЕЛАНО РУКАМИ ВЛАДЕЛЬЦА (не мной, и потому неповторимо командой)

1. **Ключ APNs выпущен** в консоли Apple. **Key ID `VU77A6RG22`**,
   Environment `Sandbox & Production`, Key Restriction `Team Scoped (All
   Topics)`. Скачивается один раз, второго раза Apple не даёт;
2. **ключ загружен в Firebase** — проект `mugam-club`, Project settings →
   Cloud Messaging → Apple app configuration → `com.mugam.mugamFlutter`,
   Key ID `VU77A6RG22`, Team ID `9X7JFR6HXR`;
3. **в Xcode**: выбрана платная команда, добавлена способность **Push
   Notifications** (она и дописала три `CODE_SIGN_ENTITLEMENTS`),
   зарегистрировано второе устройство.

## ТРУБКИ — КТО ЕСТЬ КТО. Раньше в конспекте этого не было, и путались

| Кто | Имя у `flutter devices` | UDID | Связь |
|---|---|---|---|
| **Teymur** | `iPhone Guitar Prosessor` | `00008101-001A146911A3003A` | Wi-Fi, iOS 26.1 |
| **Rafael** | `iPhone` | `00008101-001261402669003A` | кабель, iOS 26.0 |

**Обе в профиле подписи** — прежняя запись «Teymur в профиле НЕТ»
(раздел 0-Б ниже) устарела 14.08, отдельная регистрация больше не нужна.

## ~~СБОРКА: ТОЛЬКО НА ТЕЙМУРА. РАФАЭЛЬ НЕ СОБРАН~~ — ВЕРНО НА 15.08, ОТМЕНЕНО

> **Рафаэль собран 18.08 и пересобирался 20.08 дважды.** Заголовок оставлен
> с зачёркиванием, потому что числа под ним верны на свою дату; врал только
> он сам. Помечено 20.08 — тот же обход, что снял три спорящих «точки
> старта» в этом файле.

`flutter run --release -d 00008101-001A146911A3003A` — **встала успешно**,
по подписи и профилю не оборвалась. Из лога: `pod install` 56,8 с,
`Xcode build done` 149,1 с, `Installing and launching` 242,5 с, дальше
список интерактивных команд.

**Сборка шла по Wi-Fi**, хотя намеревались по кабелю: кабель между делом
отошёл. На компиляцию это не влияет, только на передачу.

**НА РАФАЭЛЕ СТОИТ СТАРЫЙ КЛИЕНТ БЕЗ ПРАВ НА PUSH.** Собрать на него —
второй шаг захода, после проверки доставки на Теймуре. По одной трубке за
раз, Рафаэль по кабелю.

## ПЕРВЫЙ iOS-ТОКЕН В ИСТОРИИ ПРОЕКТА

```
users/1IBr5oqcHCUZRpkpE4lGyPAKBNn2/pushTokens/7240a36c205ff24e55d2b94a16d192e4
поле token, длина 142, updatedAt 2026-08-14T19:49:10.841710Z
владелец — Teymur Orucov

caDT803MO00zr8ikH7TAxi:APA91bFhEV9tsQOQcxV2Z-eZq5dWBY8vt-uEiw0ol85q1RRXKPrQwKateKNow-iorDU2lhcfs8kWyGi1i4sZDlLxxVbKmhUCUfnKSV447jlQBn_uAId2Ohc
```

**Чем опознан как iOS и чем НЕ опознан.** Поля платформы в документе нет —
поля ровно `[token, updatedAt]` у всех трёх документов. Опознание
косвенное: форма id (32 знака `0-9a-f` — так пишет нынешний `_deviceId`,
`push_notification_service.dart:51-62`) плюс время: лог сборки дописан в
19:49:05Z, документ записан в 19:49:10.841710Z — **разрыв пять секунд**.

**Два других документа — андроидные, прежней схемы id, оба ЖИВЫЕ**
(`SKQ1.220303.001` от 24.07, `BP2A.250605.031.A3` от 20.07; проверка
вхолостую 14.08: к удалению 0 из 2). Они и есть контроль: ноль по ним
означал бы неверный запрос, а не отсутствие токенов.

## ЧТО ПРОВЕРЕНО ПО КОДУ И НЕ ТРЕБУЕТ ПЕРЕПРОВЕРКИ

- **пишет токен одно место** — `push_notification_service.dart:129-137`;
  дорога до него `main.dart:183` (`authStateChanges`) → `:186`
  (`registerToken`) → `:103` (запрос разрешения) → `:111`/`:114`
  (`getToken`, вторая попытка через 2 с) → `:118`;
- **`fcmToken` в документе пользователя — обманка.** Наш код пишет туда
  пустую строку при создании учётки (`auth_service.dart:54`) и больше
  никогда не трогает и нигде не читает;
- **читателей `pushTokens` в `functions/src/index.ts` пять**: отправляют
  `:157` (звонки), `:228` (общий веер), `:523-526` (внутри `onNewMessage`,
  свой обход); не отправляют `:201` и `:2054`;
- **цепочка для сообщения в чате**: `onNewMessage`
  (`chats/{chatId}/messages/{messageId}`, `:425`) → подавление
  присутствием `isWatchingChat` `:504` → чтение токенов `:523-526` →
  отправка **на все** документы `:529-532`;
- **мёртвые токены удаляются**: `DEAD_TOKEN_CODES` `:79-82` (ровно два
  кода), `pruneIfTokenDead` `:84-98`, зовётся из `:116` и `:152`; путь
  `onNewMessage` передаёт `tokenDoc.ref` (`:532`), значит прикрыт тоже;
- **`aps-environment` = `development`** и в файле прав (`:5-6`), и в
  профиле → **APNs sandbox**. Ключ `.p8` покрывает обе среды;
- **provisional-разрешение исключено по устройству кода**:
  `requestPermission()` зовётся без аргументов (`:103`), а в
  `firebase_messaging 16.4.1` (`pubspec.lock:466`) `provisional` по
  умолчанию `false`. Значит баннер обычный, не тихий.

**Чего нельзя снять ИЗ КОДА — и чем оно снимается вместо этого (I50).**
Фактическое значение `settings.authorizationStatus` нигде не
записывается: `requestPermission()` зовётся без сохранения ответа
(`push_notification_service.dart:103`), сборка боевая, и вывести значение
из кода нельзя ни в какую сторону.

**Способ есть, и он не кодовый: «Настройки» → «Уведомления» →
`mugam_flutter` на самой трубке.** Там глазами видно то, чего нет в
данных: разрешены ли уведомления вообще, включены ли баннеры, не уведён
ли показ в «Сводку». Записано именно способом, чтобы в следующий раз это
не выводили из кода — из кода оно не выводится.

**И третий способ, задним числом, сильнее обоих: доставка.** 18.08 баннер
пришёл (ниже), значит разрешение на устройстве Теймура было выдано, а не
предположено. Это доказательство существует только после успешной
доставки и не годится, пока её нет: до неё вопрос «выдано ли разрешение»
как раз и решает, чем объяснять пустой экран.

Права, вшитые в бандл, я по-прежнему не читал: `codesign -d
--entitlements` в той сессии отклонялся по разрешениям.

**Это предупреждение закрыто задним числом, 18.08:** оно говорило «читал
файл прав и профиль, а не права в собранном бандле» — а до 15.08 прав в
бандле не было **вовсе** (`CODE_SIGN_ENTITLEMENTS` отсутствовал во всех
трёх конфигурациях), теперь они есть, и доставка 18.08 доказала это
сильнее любого чтения: баннер не пришёл бы на бандл без прав.

## ЧТО МОЖЕТ НЕ СРАБОТАТЬ ПРИ ПЕРВОЙ ОТПРАВКЕ — чтобы пустой результат читался, а не гадался

**Обрывает доставку:** токен скопирован не целиком (142 знака); токен
устарел после перезапуска; ключ APNs не привязан к приложению;
уведомления выключены в «Настройках» (iOS отбрасывает молча, отправитель
видит успех); телефон не в сети.

**Доставляет, но баннера не видно:** «Не беспокоить»/Фокус; «Сводка
уведомлений»; приложение открыто на экране (спасает
`setForegroundNotificationPresentationOptions`, `main.dart:210`).

**Не поломка, но выглядит как она:** `flutter run` держал сеанс на трубке
Теймура; нажатие `q` закрывает приложение.

## ДОЛГИ ЭТОГО ЗАХОДА — ЧЕТЫРЕ, ВСЕ НЕ ПРИМЕНЕНЫ

**1. ⛔ КЛЮЧ `.p8` ЛЕЖИТ ВНУТРИ РЕПОЗИТОРИЯ.** Путь:
`assets/yeni plan uzre/AuthKey_VU77A6RG22.p8`. Он **неотслеживаемый**
(`git status --porcelain` → `??`) и **не входит в сборку ассетов**
(`grep -c "yeni plan" pubspec.yaml` → 0 при канарейке `assets/icons` →
29). **Но `git clean -f` снесёт его молча, а второй раз Apple его не
отдаст.** Вынести в `~/Documents/mugam-keys` руками через Finder —
записью вне папки проекта я не располагаю. Правило `*.p8` в `.gitignore`
дописать не удалось: правка отклонена.

**2. ~~Правки Е1–Е4 согласованы не были и в файлы не легли.~~ ЗАКРЫТО
18.08: применены Е1–Е9.** Что куда легло:
- **Е1** — перемер 15.08 у строки «iOS-токенов нет вовсе»,
  `AUDIT_TODO.md` (стало 1 из 3, чем получено, поля платформы нет);
- **Е2** — заведена **N131** (признак платформы из базы не читается;
  чинится полем `platform` в `_saveToken`);
- **Е3** — к числу **673** приписан «замер 02.08 по журналам Cloud
  Functions» в `docs/plan.md` и в двух местах этого файла: `:1164` и
  `:1414` по нынешней нумерации; третье вхождение (`:214`) дату уже
  имело. **Адрес `:931` из прежней записи был верен — по коммиту**
  (`git diff -U0` даёт для него `@@ -931 +1200,2 @@`); разошёлся он
  потому, что раздел 0-А, дописанный 15.08, в коммит не попал и сдвинул
  нумерацию рабочего дерева на 269 строк. **Ссылка на строку
  незакоммиченного файла живёт до следующей вставки выше по тексту** —
  тот же класс, что I33 про отметки времени;
- **Е4** — критерии замера токенов перенесены в `AUDIT_TODO.md`, раздел
  N25-push (место выбрано и подтверждено 18.08);
- **Е5** — тип работы один на всё предложение, `docs/plan.md`;
- **Е6** — критерии первой проверки доставки, записанные до отправки,
  туда же в N25-push;
- **Е7** — способ снять `authorizationStatus` глазами, ниже в этом файле;
- **Е8** — отменён абзац «iOS-токенов нет и не будет» (`AUDIT_TODO.md`,
  оговорка 2 в «Живость токенов»): зачёркнут, рядом чем отменён;
- **Е9** — сама доставка 18.08 записана в `AUDIT_TODO.md` (N25-push) и в
  шапке этого раздела.

**3. ~~Критерии, по которым отличали «токена нет» от «смотрим не туда».~~
ПЕРЕНЕСЕНЫ 18.08 (Е4) в `AUDIT_TODO.md`, раздел N25-push** — дословно, со
всеми тремя ветвями («отсутствие доказано», «смотрим не туда», «не
доказательство ничего»). Здесь оставлен указатель, а не копия: жили они в
файле сессии, а замерять ими будут каждый следующий обход токенов, а не
только тот.

**4. ~~`project.pbxproj` изменён и не закоммичен~~ — ЗАКОММИЧЕНО 18.08.**
5 строк добавлено, 0 убрано (`git diff --numstat`). **Прежняя разбивка
называла четыре строки вместо пяти** («ссылка на файл прав и три
`CODE_SIGN_ENTITLEMENTS`»): «ссылка» — это на деле **две** строки.
Поимённо, все пять:

1. объявление `PBXFileReference` для `Runner.entitlements`;
2. включение его в группу `Runner` (`PBXGroup` → `children`);
3. `CODE_SIGN_ENTITLEMENTS` в конфигурации **Debug**;
4. то же в **Release**;
5. то же в **Profile**.

`DEVELOPMENT_TEAM` среди них **нет ни одной** — проверка выше.
Коммитить решили после того, как сборка подтвердит подпись; она
подтвердила, а 18.08 подтвердила и доставка.

## ПОРЯДОК СЛЕДУЮЩЕГО ЗАХОДА

1. вынести ключ `.p8` из репозитория (руками, Finder);
2. ~~**тестовое сообщение из консоли Firebase** на токен выше, приложение
   свёрнуто. Это проверяет ключ, entitlement и токен — и **ни строчки
   нашего кода**, одна переменная~~ — **СДЕЛАНО 18.08, баннер пришёл**
   (шапка раздела);
3. **следующее по счёту** — собрать на Рафаэля (кабель, `flutter run --release -d
   00008101-001261402669003A`), дождаться его токена, проверить, что
   документов стало **4**;
4. настоящая дорога: сообщение с трубки на трубку, у получателя
   приложение свёрнуто (иначе сработает подавление присутствием — это
   требование, а не дефект). В журнале функции искать отсутствие
   `FCM push failed`;
5. четыре проверки «не должно прийти» и двенадцать положительных —
   таблицы в `AUDIT_TODO.md`, раздел «N25-push: что проверить в день
   включения APNs»;
6. ~~применить Е1–Е4~~ — **сделано 18.08, применены Е1–Е9**; остаётся
   **закоммитить `project.pbxproj`** (5 строк, долг 4 выше).

## ЧЕГО НЕ ЖДАТЬ ОТ ЧИСЛА 673

«673 неудачные отправки за 30 дней» — данные прода **02.08**, скользящее
окно. Превратить их в «до/после» нельзя: окно другое, вклад сегодняшних
отправок в нём не виден. **Мерить успех по падению этого числа — мерить
не то.** Доказательство доставки — баннер на экране и отсутствие
`FCM push failed` в журнале.

## МЕШАЕТ РАБОТЕ: `dontAsk` ВМЕСТО `acceptEdits` (N113)

За этот заход **молча отклонены**: правка `.gitignore`, создание папки
`~/Documents/mugam-keys`, `codesign -d --entitlements`, `security
find-identity`, часть обходов `grep`/`find`. Это долг №1, и он уже стоил
конкретной работы, а не только удобства.

**НАЙДЕН ОБХОД НА ЖИВОЙ СЕССИИ — 18.08, проверено делом.** Правка файла
настроек по-прежнему не помогает (программа держит список с её старта), но
**переключение режима в самой сессии работает**: `Shift+Tab` до «accept
edits» либо `/permissions`. В тот же заход подряд отклонённые правки
`AUDIT_TODO.md` и `docs/handoff.md` после переключения легли обе. То есть
«менять только между сессиями» верно про **файл** и неверно про **режим**;
стоило это одного сообщения человеку вместо потерянного захода.

---

# РАЗДЕЛ 0-Б. ГДЕ ВСТАЛИ 14.08: КОД НАПИСАН, СБОРКИ НЕТ, ПРОГОН НЕ НАЧИНАЛСЯ

**Про работу с предложениями. Не тронуто с 14.08, вечера.**
**Оговорка после 0-А:** запись ниже «Teymur в профиле НЕТ» и «профиль
истекает 20.08» **устарели 14.08** — обе трубки в профиле, срок
2027-08-14.

**ПЕРВОЕ ДЕЛО ЭТОЙ РАБОТЫ — СБОРКА НА ОБЕ ТРУБКИ И ПРОГОН.** (Первое дело
**захода** — раздел 0-А, push: он начат и не закончен.)

## ЧТО СОБИРАТЬ И КАК

Rafael по кабелю, Teymur по сети — **но Teymur в профиле подписи НЕТ**
(замер `embedded.mobileprovision` 14.08: устройств 1). Ему нужен кабель
хотя бы раз. **Профиль истекает 20.08.**

Собирать **один раз** и ставить один бундл на обе: две сборки дают
разошедшуюся подпись (N89, наступил 14.08 — `✓ Built` и код 0 при отказе
`codesign`). После сборки **обязательно** `codesign --verify --deep
--strict`; сошлось — ставить, не сошлось — `flutter clean` и заново
(пересборка начисто 14.08 заняла **1382 с ≈ 23 минуты**).

## ПРОГОН — ВОСЕМЬ ПУНКТОВ ПО ПОРЯДКУ

1. **строка предложения в ленте** у обоих: «5 gün təklif · Toy» у
   отправителя, «2 gün cavab» после ответа, «gələ bilmir» при отказе;
2. **нажатие** на строку — открывается лист;
3. **экран ответа**: нажимаемы ТОЛЬКО предложенные дни, свои занятые видны
   и выбираются;
4. **отметка** ушла, строка в ленте сменилась;
5. **экран приёма**: крупно «N gün» и даты, тише «— yox»;
6. **«Qəbul edirəm»**;
7. **вечера в календарях обоих**;
8. **ответ нулём дней**: кнопка «Heç birinə gələ bilmirəm», строка
   «gələ bilmir», у инициатора «Qəbul edirəm» НЕ показывается, только отзыв.

**ЧЕТЫРЕ ИЗ ВОСЬМИ СЕГОДНЯ ПРОЙТИ НЕЛЬЗЯ — сказано здесь, чтобы это не
выяснилось на трубке.** Пункты 1 и 2 требуют `bubbleFor`, которого нет;
пункт 4 требует, чтобы ответ слал свой якорь, — не написано; пункт 7
требует создания вечеров по принятию — не написано. **Проходимы 3, 5, 6, 8
— и только если открыть экраны в обход ленты, чего с телефона сегодня
нельзя.**

**То есть честный первый шаг следующего захода — не прогон, а `bubbleFor`.**
Прогон становится возможным сразу после него.

## ЧТО ВЫЛОЖЕНО В ПРОД

**Правила подколлекции `chats/{chatId}/offers/{offerId}` — выложены.**
Набор `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe` (14.08), прод и файл по 1328
строк, `diff` ноль. Полностью — `docs/deployed.md`.

**ПОПРАВКА 15.08 — ЭТО ВЕРНО ТОЛЬКО ПРО РАФАЭЛЯ.** На трубке **Теймура**
14.08 ночью встала свежая сборка (`flutter run --release`, раздел 0-А), и
в ней экраны ответа и приёма уже есть. **Прогон, который ниже объявлен
непроходимым, на Теймуре частично проходим** — состав проверить по коду,
а не по этой записи. На **Рафаэле** клиент прежний.

**НА ТРУБКАХ КЛИЕНТ СТАРЫЙ.** Последняя установка — сборка от 14.08 утра
(коммит `30c2839`). Всё, что написано после неё, на телефонах отсутствует:
экраны ответа и приёма, строка состояния, порог сворачивания, уборка
записей. **Предложения на трубках сегодня — обычные текстовые сообщения,
нажатие ничего не открывает.**

## РАБОТА 1 НАПИСАНА НЕ ЦЕЛИКОМ — поимённо, чтобы не искать

**Готово:** лист выбора дней по макету `mugam-14-secim` · детали у каждого
дня · копирование с вопросом · общая сетка `OfferMonthGrid` · экран ответа ·
экран приёма · строка состояния из трёх видов · правило «принимать нечего
при нуле» · `offerId` в модели · уборка временных записей.

**НЕ НАПИСАНО, три вещи:**

1. **`bubbleFor`** — лента не читает `offerId`, строка не нажимается.
   `grep -c "offerId" lib/features/chat/screens/chat_screen.dart` → **0**;
2. **ответ не шлёт своего якоря** — музыкант пишет отметки в документ, в
   переписке не появляется ничего;
3. **принятие не создаёт вечеров** — правило выложено, экран написан, а
   писателя нет: сервер подколлекцию не видит (N130), клиент вечера не
   пишет.

## НЕЗАКРЫТОЕ — списком

- **N130** — подколлекция невидима серверу: ответ, принятие и отзыв не
  порождают ничего. **Уточнение к формулировке: push о САМОМ предложении
  уходит** (якорь — обычное сообщение, `onNewMessage` его не отсеивает), но
  выглядит как обычное сообщение. Молчат три хода из четырёх;
- **N129** — утечка временных файлов записи, **три места**: перезапись
  голоса в листе, отменённая запись в чате, отправленная запись остаётся
  лишней копией (`persistPendingFile` делает `copy`, исходник не удаляет
  никто). Работа заведена в `docs/plan.md` тремя частями;
- **N128** — `setJobOffer` без единого вызывающего; снимается вместе с
  четырнадцатью полями раунда;
- **N127** — «создаёт только инициатор» было круговым; закрыта.

### ДВА ПУНКТА, КОТОРЫЕ ПРОСИЛИ ЗАПИСАТЬ, А ЗАМЕР ИХ НЕ ПОДТВЕРДИЛ

**Записаны как непроверенные, а не как находки** — иначе по ним станут
чинить исправное.

**«Пять видов событий пишут `lastMessage` без `messages`».** Механизма нет:
`lastMessage` пишет одна функция — `previewFieldsFrom` (`index.ts:295`), и
она берёт `type`, `text`, `seq` **из документа сообщения**. Строка
предпросмотра без сообщения существовать не может. Шесть мест, которые
нашлись рядом (`grep -rn "isSystem': true" lib` → **6**), — это
группа: создал, вышел, добавил, удалил, назначил админом, снял, — и все
шесть **настоящие сообщения**. **Если поведение всё же наблюдалось —
назвать событие, тогда найдётся настоящая причина.**

**«Расхождение `canOfferOnDay` с экраном выбора».** Не найдено: и экран, и
точка входа выводят день из одного `isPastDay`, второго правила нет.

## APPLE DEVELOPER PROGRAM — ОПЛАЧЕН 14.08

**Со слов автора:** программа оплачена 14.08, **Enrollment ID
992K2KX5B4**, ждём письма об активации, до двух рабочих дней.

**Что это открывает, когда придёт:**

- **push на iOS впервые** — до сих пор его не было вовсе (бесплатная
  учётка), и потому N130 непроверяема руками: только по логу функции. После
  активации проверка становится настоящей;
- **уведомление, ведущее прямо в лист предложения** — то самое, ради чего
  порядок «сперва экраны, потом уведомление» и был выбран: вести теперь
  будет куда;
- **регистрация Teymur** и профиль без недельного срока.

**До письма ничего из этого писать не надо** — проверить всё равно нечем.

## ЧТО ПРОВЕРЕНО НА ТРУБКЕ И РАБОТАЕТ

- **вход** — «İş təklif et» в листе вложений («+»), в шапке «⋮» его больше нет;
- **лист** — сетка месяца, занятые дни видны, прошлые не нажимаются;
- **снятие дня** — строка дня уходит либо переходит на оставшийся;
- **тап по выбранному, но не открытому дню** — открывает, а не снимает;
- **детали по дням** — час 14-го не перетекает на 20-е и возвращается при
  переходе обратно, «Ətraflı» у дня с вписанным раскрывается сам;
- **карандаш** в клетке дня с деталями (13 пикселей, 9 из макета не читались);
- **тип** — три кнопки плюс «Digər» со своим полем; отправлено с типом
  `vvvbbbb`, то есть своё поле доезжает до базы;
- **ОТПРАВКА ЦЕЛИКОМ**: лист → документ в `chats/{chatId}/offers/{offerId}` →
  сообщение-якорь в ленте. Три якоря на снимке владельца.

## ЧТО ДАЛЬШЕ — ПЕРВЫМ ДЕЛОМ

**КАРТОЧКА НЕ ПОДКЛЮЧЕНА К ЛЕНТЕ.** Виджет написан, у него 22 теста, но
`chat_screen` его не рисует: `grep -rn "JobOfferCard" lib` даёт только сам
файл, `offerId` лента не читает. Сегодня предложение видно **обычным
текстовым сообщением** — «5 gün: 26, 27, 28, 29, 30 avqust · Toy». Это
задумано как живой шов со старой сборкой, но новая обязана рисовать карточку.

**Это и есть следующая работа.**

## ЧЕГО НЕТ И ЧТО МЕШАЕТ

- **Teymur не получает сборку**: в профиле подписи **одно устройство**, и это
  Rafael (замер `embedded.mobileprovision` 14.08). Нужен один раз кабель;
- **профиль истекает 20.08** — после этого сборки перестанут ставиться;
- **ответ музыканта и принятие на трубке не проверялись** — вторая сторона
  недоступна, пока Teymur без сборки.

## ПЕРВЫЙ ВОПРОС — РЕШЁН 13.08 АВТОРОМ. Подколлекция

> **Предложение — отдельный документ `chats/{chatId}/offers/{offerId}`**, плюс
> **сообщение-якорь** в `messages` со ссылкой `offerId`, дающее карточке место
> в ленте.

Решающий довод: правила для подколлекции чата уже написаны (образец —
`messages` с `parentMembers()`), `hasOnly` на горячем документе чата не
задевается. Полностью — `docs/plan.md`, «ФОРМА ХРАНЕНИЯ — РЕШЕНО 13.08».

**Там же три вещи, записанные до кода:** четырнадцать полей раунда на документе
чата не трогаются (в разговоре их было названо шесть — расхождение разобрано);
**старые предложения новым экраном не показываются, и поддержку им не
дописывать** (решение автора 13.08 — у старой формы отметок по дням нет вовсе,
брать «кто отметил и когда» неоткуда); голосовое остаётся сообщением типа
`audio`, предложение хранит только их id.

## ЧТО СДЕЛАНО 13.08 ПОСЛЕ РЕШЕНИЯ — ОБЩАЯ ЗАПИСЬ ГОЛОСА (работа 2)

**Вынесена ПРЕЖДЕ карточки, решением автора** — мест ровно три (чат, карточка,
«Gələ bilmirəm»), и писать карточку первой значило бы завести четвёртую копию.

- `lib/core/audio/voice_recording_session.dart` — сеанс записи;
- `lib/core/audio/audio_session_gate.dart` — сессия звука;
- `test/voice_recording_test.dart` — **7 тестов**, до этого запись не была
  покрыта ничем.

**Прогон 13.08: 44 набора из 44 на диске, 559 тестов, 0 неуспешных**
(`flutter test --reporter json`). `flutter analyze` — **45 замечаний, все в
файлах, которых работа не касалась**, в новых и правленых ноль.

**НА ТЕЛЕФОНЕ НЕ ПРОВЕРЕНО.** Тесты покрывают разбор волны и единственность
объявления; сама запись — разрешение, отложенный старт, отбрасывание случайного
тычка, мгновенность отклика — проверяется только руками на трубке. **Это первый
пункт прогона следующей сборки.**

**СЛЕДУЮЩИЙ ШАГ — правила и парный тест, до кода экрана.** Словесная редакция
правил (что разрешено, кому, и чего не разрешено) уже лежит в `docs/plan.md`,
раздел «ПРАВИЛА ПОДКОЛЛЕКЦИИ — СЛОВАМИ».

**ПЕРЕД ПРАВИЛАМИ СПРОСИТЬ АВТОРА: какие два хода приняты.** 13.08 в ответе
сказано «оба хода — да» и назван отзыв предложения инициатором, но в моей
редакции правил отзыва не было — состав ходов не сходится, и угадывать его в
необратимой вещи нельзя.

**НЕ ЗАМЕР, ждёт ответа автора:** сколько существующих предложений держат
четырнадцать полей. Со слов автора 27, чем считано — не спрошено.

## ТРИ ВОПРОСА, НА КОТОРЫЕ РАЗБОР ОТВЕТИЛ

**1. Вечер без времени приложение примет — но соврёт на экране.** `date` —
строка, `DateTime.parse('2026-08-09')` даёт полночь, правила формат не
проверяют. Однако в коде уже стоит обход этой ямы
(`job_offer_date_sheet.dart:114`): «полночь означала бы мероприятие в 00:00»,
и потому подставляется `kJobOfferDefaultHour = 19`. **То есть сегодня
приложение не умеет вечера без времени — умеет подставлять час, которого никто
не выбирал.** Плюс совпадение считается до минуты (`exactConflictsAt`): два
вечера без времени в один день прочтутся как встреча в одну минуту.
**«Время необязательно» — правка трёх мест: хранение, показ, совпадение.**

**2. Ответ музыканта сегодня — «да/нет» на весь чат** (`recipientAgreed`,
bool). **Места для «три из пяти» нет.** И сервер по этому bool **сразу
создаёт вечер** — а новая цепочка требует, чтобы вечеров ещё не было.

**3. Раунд на чат один, и с тремя шагами ломается дважды:** переотправка
музыканта — это правка того же предложения, а не новый раунд, и модель их не
различает; второе предложение в том же чате **стирает первое вместе с
отметками и голосовыми** (`setJobOffer` чистит все ключи раунда, довод записан
там же).

## ЦЕПОЧКА ИЗ ТРЁХ ШАГОВ — в `docs/plan.md`

**Ключевое: вечера создаются ТОЛЬКО после «Qəbul edirəm».** Ответ музыканта —
**встречное предложение, а не согласие**: звали на пять, дали три, и
работодатель часто не соглашается.

## МАКЕТЫ

**Девять пронумерованных макетов** в `docs/design/`: 1, 2, 3, 4, 5, 6, 8, 9,
10 (седьмого нет). **Файлов 15** — восемь `.html` и семь `.png`; у
`mugam-2-addim` только картинка (`ls docs/design/`, 13.08).

Положены 13.08: **`mugam-9-teklif`** (сторона музыканта) и
**`mugam-10-secim`** (сторона работодателя) — читать парой, это один разговор
с двух телефонов. Подписи и три вещи, видные только на макетах, — в
`docs/design/README.md`.

**Соответствие «все стороны нарисованы» — со слов автора, мной не сверено.**
Что предложение у обоих нарисовано — проверено чтением; про уход из вечера,
Tezliklə и первый запуск сказано автором, и какой файл что показывает, я не
открывал.

## ТРИ РАСХОЖДЕНИЯ МАКЕТОВ С КОДОМ — со слов автора

1. **Строка про смену стартовой вкладки** — мелкая работа.
2. **`Ətraflı` с временем и местом** — часть работы 1. **Проверено мной**:
   на `mugam-9-teklif` карточка «1 gün» раскрывает время, место и заметку.
3. **Пять описаний в `Tezliklə`** — правим код по макету.

**Первое и третье мной не проверялись** — записаны как сказанные, чтобы не
потерялись, но замером не являются.

---

# С ЧЕГО НАЧИНАТЬ — ПОРЯДОК ЧЕТЫРЁХ РАБОТ (13.08)

**Уговор о работе целиком — `docs/how-we-work.md`.** Главный принцип оттуда,
который решает споры: **приложение НЕ ведёт переговоры.**

1. **Карточка предложения в чате** — выбор нескольких дней, галочки, ответ,
   обновление у обоих.
2. **Общая запись голоса** — вынуть из чата.
3. **«Gələ bilmirəm»** — кнопка в карточке вечера, окошко причины, отправка в
   чат.
4. **«Убрать из состава»** — кнопка у владельца рядом с пометившимся.

## РАБОТА 1 САМАЯ БОЛЬШАЯ, И НЕ ПОТОМУ ЧТО ЭКРАН

**«Несколько дней» меняет ДАННЫЕ, а не показ.** Предложение сегодня — **поля
на документе чата `chats/{chatId}` с ОДНОЙ датой**; своей сущности у него нет
(`docs/plan.md`, Часть 5, раздел «Ограничение»). Код односуточный:
`jobOfferDefaultDate`, `jobOfferDateOnDay`, `canOfferOnDay` — все про один
день.

Карточка с пятью днями и галочками — это **новая форма хранения**: набор дат,
у каждой свой ответ, обновляемый обеими сторонами. **Значит работа 1
начинается с разбора данных до кода**, по правилу темпа: разбор до кода — для
необратимого, а форма хранения необратима.

---

# ПРОМЕЖУТОК, О КОТОРОМ НАДО ЗНАТЬ ПЕРВЫМ ДЕЛОМ (13.08)

**Участник, который согласился, СЕЙЧАС НЕ МОЖЕТ ВЫЙТИ ИЗ СОСТАВА и не может
назвать причину. Это не дефект, это промежуток между двумя шагами.**

13.08 снято решением автора: зелёная строка договорённости, раскрытие с
историей, четыре хода отмены по согласию, плашка состояния с окошком на три
пункта, кнопка «?» у вышедшего, старый экран карточки договора. Взамен придёт
**«Gələ bilmirəm»** — односторонний ход с ответом инициатора. **Он ещё не
написан.**

**ЧТО УЧАСТНИК ВСЁ-ТАКИ МОЖЕТ — сказано точно, потому что «не может ничем»
было бы неправдой.** Блок ответа в карточке цел
(`agreements_screen.dart:3139`): **«Bacarmıram» работает**, это ход
`answersForSelf`, выложенный 11.08. Значит сказать «не смогу» есть чем.

**Чего нет:** выйти из `musicians` (это была «Siyahıdan sil», снята вместе с
окошком «?»), назвать причину словами или голосом, и получить от инициатора
один из трёх ответов.

**СКОЛЬКО ПРОДЛИТСЯ: до того, как написано «Gələ bilmirəm» и выложены правила
под него.** Не «пока не дойдут руки» — условие проверяемое.

**СЛЕДУЮЩАЯ РАБОТА — ОНА, И ВПЕРЁД НЕЁ НИЧЕГО НЕ БЕРЁМ.** Указание автора
13.08. Разбор и порядок — `docs/plan.md`, работа «ДОГОВОРЁННОСТЬ КАК ОТДЕЛЬНАЯ
ВЕЩЬ УБИРАЕТСЯ ЦЕЛИКОМ».

**Промежуток кончается работой 3** («Gələ bilmirəm»), не раньше и не позже.

---

# ЧТО ИЗ НАПИСАННОГО СНИМАЕМ, А ЧТО ОСТАЁТСЯ (13.08, решено явно)

## ОСТАЁТСЯ: `left` в `answers` — и наконец получает писателя

**Не снимать.** Он заводился под прежнюю схему, но новой подходит точно:
«В составе вечера имя помечается» — это и есть `left`, а подпись «İşdən
çıxdı» под именем уже написана (`event_answers.dart:348`).

**Отличать `left` от `cant` нужно, и это не переговоры, а память.** `cant` —
«меня звали, я не смог»; `left` — «я согласился и потом отказался». Владельцу
эти два случая не одинаковы, а приложение обязано помнить итог.

**СЛЕДСТВИЕ, КОТОРОЕ СОКРАЩАЕТ РАБОТУ 3: нового правила не нужно.**
`answersForSelf` в проде уже разрешает участнику писать себе любое из
`['going', 'waiting', 'cant', 'left']` (`firestore.rules:843`). Значит
«Gələ bilmirəm» — **правка клиента без выкладки правил**. Проверить перед
кодом, что причина уходит в чат обычным сообщением (это существующий путь), и
всё.

**Отсюда же отпадает N124** — правило переноса теряло `left` у выбывшего из
состава. По новой схеме из состава убирает **владелец, руками**, и делает это
осознанно.

## СНИМАЕМ: три хода состояния — но только один из трёх

- **`ownerSetsStatus` — мёртв.** «Под вопросом» отменено, плашка снята.
  **Это и есть шестая строка в списке мёртвых ходов.**
- **`ownerCancelsOwnEvent` — ЖИВ**, единственная отмена вечера.
- **`ownerRestoresOwnEvent` — ОЖИВЁТ** долгом «возврат из отмены». Из мёртвых
  выбывает.

**ШЕСТЬ МЁРТВЫХ ХОДОВ, поимённо:** `requestsCancel` (732), `confirmsCancel`
(753), `withdrawsCancelRequest` (940), `restoresEvent` (992),
`declinesCancelRequest` (1002), `ownerSetsStatus` (861).

## ПРОВЕРИТЬ ПЕРЕД РАБОТОЙ 4: седьмой кандидат

**`leavesEvent` (789)** — выход участника из `musicians` своими руками. По
новой схеме из состава убирает **владелец**, значит участнику этот ход не
нужен. Клиентский метод `leavePersonalEvent` пока **имеет вызывающего**
(`agreements_screen.dart:4554`), поэтому в мёртвые он сегодня не записан.
**Решить это в работе 4**, а не раньше: если вызывающего снимем — ходов к
снятию станет семь.

## СНИМАЕТСЯ ПОКАЗОМ, НО НЕ ИЗ ДАННЫХ

- **`unsettled`** — один документ из 91, показывается обычным. Решение, не
  пропуск (`docs/plan.md`).
- **`DayRole.declined` и серая точка** — писались под «отказ от приглашения».
  Приглашение станет галочкой в карточке предложения; **что помечать на сетке
  — вопрос работы 1**, до неё не трогать.

---

# ДОЛГИ — ЧЕТЫРЕ, С ОБЯЗАТЕЛЬНЫМ ПОРЯДКОМ

1. **Отменённый вечер остаётся в календаре красным со значком стоп.** Сейчас
   **исчезает совсем**: `liveEvents` (`day_buckets.dart:91`) выбрасывает
   `cancelled` до раскладки по дням, а в `month_marks.dart` красного нет ни
   разу.
2. **Возврат из отмены — ТОЛЬКО ПОСЛЕ ПЕРВОГО.** Ход
   `ownerRestoresOwnEvent` в правилах есть, кнопки нет. **Кнопке негде жить,
   пока отменённый вечер не виден** — открыть его сегодня неоткуда. Наоборот
   не выйдет: получится кнопка на экране, до которого нет дороги (N125).
3. **Перевод четырёх мест на общий вход в чат.** Сам вход
   (`open_direct_chat.dart`) сейчас **без единого вызывающего** — его звала
   только снятая «Dəqiqləşdir». Понадобится работе 3.
4. **Шесть мёртвых ходов в правилах — одной выкладкой**, не раньше работы 3.

---
Дата в шапке ставится ЗАНОВО при каждой правке раздела 0: 08.08–11.08 здесь
трое суток стояло «08.08, утро», пока раздел 0 жил своей датой. Шапку читают
первой, и она называет срок годности всему, что ниже.
Дальше по надобности: `docs/plan.md` (полный план и порядок работ),
`AUDIT_TODO.md` (реестр находок и правил), `docs/job-offer-plan.md`
(фича «İş təklif et»), `docs/deployed.md` (что выложено в прод).

Разделы с номерами 1 и выше — **архив прошлых заходов**, они датированы и
оставлены для истории. Единственный раздел «с чего начать» — нулевой;
если их когда-нибудь станет два, верен тот, что датирован позже, и второй
надо снести в тот же заход: две точки входа по отдельности выглядят
правильными, и выбирают из них наугад.

---

## 0. С ЧЕГО НАЧАТЬ — состояние на день 12.08

# ГДЕ ВСТАЛИ: ПРАВИЛА В ПРОДЕ, СБОРКА НА ТРУБКАХ, КОД В `main`

**Расхождения между продом и трубками НЕТ.** Это сказано первой строкой,
потому что владелец в конце сессии предполагал обратное — что правила выложены,
а на трубках старая сборка. **Проверено, и это не так:**

- сборка сделана **после** выкладки, одна на обе трубки: бундл 11:13:31,
  последняя правка в `lib/` 11:01:21;
- установка прошла на обе, код возврата 0 у каждой;
- **и главное — на ней работали руками:** четыре хода состояния прогнаны через
  плашку-кнопку и окошко выбора, которых в старой сборке **не существует**.
  Прогон был бы невозможен на старом коде.

**Следовательно, сборка НЕ первое дело.** Пересобирать нечего: прод, `main` и
обе трубки сошлись в одной точке. Если следующая сессия всё же решит собрать —
это не ошибка, а трата двадцати минут.

**Клиентская часть шага 6 написана ЦЕЛИКОМ и лежит в `main`** — коммит
`23c5298`, запушен. **Не начинать её заново.** Внутри: плашка-кнопка с окошком
выбора, кнопка «?» у вышедшего с двумя действиями, общий вход в чат
(`lib/core/chat/open_direct_chat.dart`), правило `offersLeftMemberMenu`, общая
запись правки `_writeEventEdit`, два новых набора тестов.

## ЧТО ВЫЛОЖЕНО СЕГОДНЯ

**12.08, 07:07 UTC — правила Firestore, набор `f0ca5a51-7865-4542-8c53-83324676f84d`**
(прежний — `50a559f2…` от 11.08). Выложены:

- **`ownerSetsStatus`** — владелец водит вечер между «в силе» и «под вопросом»;
- **`ownerCancelsOwnEvent`** — владелец отменяет СВОЙ ЛИЧНЫЙ вечер один
  (у договорённости отмена по-прежнему только через согласие второй стороны);
- **`ownerRestoresOwnEvent`** — и возвращает его из отмены;
- **`left`** пятым значением в `answersForSelf`.

Сверка: прод и файл — **1160 строк**, `diff` **ноль**, канарейка против прежней
редакции **65** строк. Проверено на трубке: **4 хода из 4**, отказов по правам
ноль. Полностью — `docs/deployed.md`.

**`left` выложен, а писать его НЕКОМУ** — ни одно место в `lib` этого значения
не записывает (**N121**). Отсюда следствие для прогона: кнопка «?» и общий вход
в чат **непроверяемы на трубках**, и выглядит это как «кнопки просто нет».

# ЧТО ДЕЛАТЬ, В ПОРЯДКЕ

**1. ДОГНАТЬ ПРОГОН — четыре проверки НА НЕПОРЧУ, две трубки.** Пять проверок
«впервые» уже прошли (три хода состояния плюс обе ветки подписи под отменой);
остались те, что работали раньше и могли пострадать:

- отмена по согласию у договорённости, три исхода: отзыв, отказ, подтверждение;
- ответ участника «Gəlirəm» / «Bacarmıram» и занятость календаря по `going`;
- **правка вечера не стирает ответы** — самый ценный пункт: обе записи правки
  сведены сегодня в `_writeEventEdit`, и это единственное место, где перенос
  мог сломаться;
- добавление нового участника: ему «cavab gözlənilir», прежним — их ответ.

**2. ОБХОД ПО НОВОМУ ПРАВИЛУ (N124): где ещё правило собирает данные ЗАНОВО по
нынешнему составу** — такое правило по устройству теряет всё, чему положено
состав переживать. Форма для поиска: цикл по текущему списку, из которого
строится новая карта, вместо правки существующей. Вопрос к каждому значению:
живёт ли оно дольше членства.

**3. Снять старую карточку договора** (`_AgreementDetailScreen`) — только
после того, как пункт 1 пройден: новая доказана, старая снимается.

**4. Убрать слово «договор» с экранов** — последним, как записано в шаге 6.

**Механизм выхода (пункты 3–4 шага 6) — отдельная работа, и она разблокирует
сразу три вещи:** `left`, кнопку «?» и общий вход в чат. Пока её нет, эти три
проверить нельзя ничем. Разбор — N121, N124 и `docs/plan.md`, шаг 6.

## ЧТО НАЙДЕНО 12.08 — семь находок и три правила

**N118** счёт копий в конспекте был вдвое меньше настоящего · **N119** два
входа в чат отказывают молча · **N120** действие наводилось на самого владельца
· **N121** кнопка «?» не показывается никому · **N122** «N NƏFƏR» считает
разное у разных документов · **N123** правка стирает повод «под вопроса» ·
**N124** сборка карты заново теряет переживающее состав.

**I57** число без прохода выглядит замером · **I58** одинаковый вызов не значит
одинаковое действие · **I59** спросить, откуда взялись данные, прежде чем
искать, кто их скрыл.

---

## АРХИВ: состояние на утро 12.08

# ТРИ ВЕЩИ, СТРОГО В ЭТОМ ПОРЯДКЕ

**1. Вынести общий вход «открыть чат с человеком», затем дописать кнопку «?» у
вышедшего участника.** Дорога в чат написана ДВАЖДЫ своими словами:
`chats_screen.dart` (`_openChatWith`) и `user_profile_screen.dart`. Третью
копию писать нельзя. Разбор и порядок — `docs/plan.md`, «Общий вход в чат»;
кнопка «?» — только у владельца и только у вышедшего, два выбора
(`Dəqiqləşdir` в чат, `Siyahıdan sil` из состава).

**2. Выложить правила** и сверить прод с файлом: строки, `diff`, канарейка
против прежней редакции. Одной строкой сказать, что изменилось в правах.

**3. ОДНА сборка на обе трубки и прогон целиком.** Одна, а не две: сборка раз
из двух даёт разошедшуюся подпись (N89), и лишний заход стоит двадцати минут
плюс риска.

## ПРАВИЛА УЖЕ НАПИСАНЫ И ЛЕЖАТ НЕВЫЛОЖЕННЫМИ — не писать заново

В `firestore.rules` готовы и **не выложены** три хода: `ownerSetsStatus`
(владелец переводит вечер между `Dəqiq` и «под вопросом»),
`ownerCancelsOwnEvent` и `ownerRestoresOwnEvent` (отмена и возврат ЛИЧНОГО
вечера, оба с `isAgree == false`), плюс `left` пятым значением в
`answersForSelf`.

**Парный тест — 17 из 17** (`functions/test/event-status-rules.test.ts`), весь
набор functions **298 из 298**. Проверка возвратом: снятие `isAgree == false`
роняет ровно два теста, и именно названных.

## ПОЧЕМУ ОСТАНОВИЛИСЬ ИМЕННО ЗДЕСЬ

**Вынос общего входа требует прочитать оба существующих места** — как они
зовут `getOrCreateDirectChat`, как открывают экран чата, что делают с ошибкой.
Писать, не прочитав, значит завести **третью копию с выдуманным поведением**,
то есть ровно то, ради чего вынос и затевается. Контекст сессии кончился
раньше, чем это чтение, и остановка сделана на записи, а не на коде.

## ЧТО СДЕЛАНО 12.08 — коротко

Карточка вечера собрана **по макету** (`mugam-6-kart`, левый экран): дата
крупно, тип и место, плашка состояния, состав компактными строками с ответами,
заметка, голосовая заглушка, строка договорённости с раскрытием, кнопки внизу.
**Плашка стала кнопкой:** окошко выбора из трёх состояний, и **у вечера с
договорённостью оно другое** — под `İş ləğv olundu` стоит «{имя}
təsdiqləməlidir», а сам выбор не отменяет, а **запускает отмену по согласию**.

**Двери сведены к одной** (N117): шесть мест открытия вечера, три ходили мимо
общей двери. **N116** закрыта — карточка не показывала состояние вовсе.
Отмена по согласию проверена на трубках во всех трёх исходах: отзыв, отказ,
подтверждение.

**Заведены работы:** третий повод `ownerDoubt`; возврат договорённости из
отмены (пара ходов-зеркал); общий вход в чат; владелец может ответить за
участника. **Правила I54, I55, I56** — в `AUDIT_TODO.md` и `CLAUDE.md`.

---

# АРХИВ: состояние на ночь 12.08

# ГДЕ МЫ: ШАГ 4 ЗАКРЫТ, КАЛЕНДАРЬ ПЕРЕДЕЛЫВАЕТСЯ ПО МАКЕТУ

**Работа «договоры и мероприятия — одна сущность» (`docs/plan.md`): шаги 1–5
позади.** Правила шага 4 в проде. Сейчас идёт вид календаря по макету
`docs/design/mugam-8-teqvim.html` — правки одна за другой, каждая с прогоном и
сборкой на обе трубки.

## ЧТО СДЕЛАНО 11–12.08, по порядку

**Шаг 4 — ответ участника.** Четыре пункта: занятость по `going` (закрывает
N75), показ состава четырьмя словами, ответ участника как поступок (окно,
запись, вход отвечающего), правило `answersForSelf()` в `firestore.rules`.
**Прогон на трубках 6 из 6.** Разбор — `docs/check-answers-step4.md`.

**Шаг 5 — «Müqavilələr» как фильтр календаря: СДЕЛАН, СВЕРЕН И СНЯТ.** Сверка
составом прошла (разность в обе стороны пуста), а потом выяснилось, что
**переключателя «Hamısı | Müqavilələr» в макете нет вовсе** — я вывел его из
формулировки плана и не открыл макет. Решение владельца: делать календарь по
макету. Переключатель и `calendar_filter.dart` удалены целиком.

**Календарь по макету** (`mugam-8-teqvim.html`), правки 11–12.08:

| Что | Состояние |
|---|---|
| цвет владельца и три буквы имени на дне | сделано |
| статус формой: залито — в силе, рамка — под вопросом | сделано |
| счёт под сеткой: люди → `Şübhəli` → `Boş` | сделано |
| заголовок `AVQUST 2026` (19pt, разрядка 1.8, `#E09A2B`, слева) | сделано |
| стрелки «‹ ›» и значок «⌄» | убраны |
| шапка: три закладки → переключатель `Gün | Ay` (18pt) | сделано |
| пометка занимает ВСЮ клетку, не кружок внутри | сделано |
| сетка закреплена, прокручивается только низ | сделано |
| **фильтр месяца по человеку** — нажатие на имя в счёте | сделано |
| дневной экран: «Bu gün» и «SABAH» закреплены, неделя прокручивается | сделано |

**Макеты уложены в `docs/design/`** — четыре штуки, 9,9 МБ в историю. У
`mugam-5-teqvim` первый экран помечен как СТАРАЯ редакция (там `MÜQ` 4 раза и
`Müqavilə` 1; в `mugam-8` их ноль) — легенду брать только из `mugam-8`.

## ЧТО ОТКРЫТО — читать до первой правки

**1. У списков «Müqavilələr» и «Tədbirlər» НЕТ ВХОДА.** Закладки убраны
решением владельца, фильтр по типу снят. Договоры видны только клетками месяца
и фильтром по человеку. Нужен ли им вход — решение владельца, не сделано
намеренно.

**2. Функции НЕ ВЫЛОЖЕНЫ.** В `functions/src/index.ts` добавлена одна строка:
`onChatUpdated` пишет `answersWrittenByOwner: true`. Без выкладки ничего не
ломается — сервер пишет карту сразу по обеим сторонам, то есть полную, и
случая «ключа нет» там не возникает. Выложить вместе со следующей серверной
работой.

**3. Работа «владелец может ответить за участника»** (`docs/plan.md`) — N114
другой рукой: правилом шага 4 не закрывается, потому что перенос делает
владелец законной правкой состава.

**4. `N113` — в `.claude/settings.local.json` стоит
`permissions.defaultMode: "dontAsk"`.** Это «отказать без вопроса», а не
«разрешить»: на новой сессии правки файлов отклоняются молча. Верное значение
— `acceptEdits`. **Менять между сессиями**, на живой возвращается обратно.

**5. Из макетов написано 4 пункта из 30** (счёт 11.08, `docs/plan.md`).
Ненаписанное — отдельные работы: голос (Часть 11), выбор нескольких дней,
`Dəqiq / Şübhəli` (третий повод `ownerDoubt`), название группы состава,
свёртка голосовых, зелёная строка договорённости.

## ЧИСЛА, СНЯТЫЕ 11–12.08

| Что | Число | Чем пересчитать |
|---|---|---|
| `personalEvents` в проде | **76** (договоров 27, обычных 49; сумма сходится) | разовый скрипт по коллекции, только чтение |
| наборов тестов Flutter | **39 из 39 на диске**, неуспешных 0 | `flutter test --reporter json > лог 2>&1`; `ls test/*_test.dart \| wc -l` |
| тестов functions | **281 из 281**, 15 наборов | `cd functions && npm test` |
| `flutter analyze` | **0 ошибок**, в `lib/` 0 предупреждений | `flutter analyze > лог 2>&1` |
| правила Firestore в проде | набор `50a559f2…`, 1102 строки, `diff` пуст | `docs/deployed.md` |

**N89 уточнена: дело не в перерыве в правках, а в ИНКРЕМЕНТАЛЬНОСТИ.** За два
дня семь чистых сборок — все целые; из двух инкрементальных одна дала
разошедшуюся подпись. Порядок: `codesign --verify --deep --strict` отдельной
командой после каждой сборки, при отказе — `rm -rf build/ios/iphoneos
build/ios/Release-iphoneos` и заново.

---

# АРХИВ 11.08: ШАГ 4 ЗАКРЫТ ЦЕЛИКОМ — прогон пройден на обеих трубках

**Все четыре пункта сделаны, правила в проде, прогон 6 из 6.** Разбор шагов и
что осталось непроверенным — `docs/check-answers-step4.md`.

| Пункт | Состояние |
|---|---|
| 4 — занятость по `going` | сделано, проверено возвратом и на трубках; закрывает N75 |
| 2 — показ состава | сделано, слова взяты из макета `mugam-6-kart` |
| 3 — ответ участника как поступок | сделано: окно, запись, вход отвечающего |
| 1 — правило в `firestore.rules` | **В ПРОДЕ 11.08, 15:25 UTC**, набор `50a559f2…` |

**Что изменилось в правах одной строкой:** участник мероприятия теперь может
записать СВОЙ ответ в `answers` — и больше ничего; до выкладки в чужой документ
не мог писать никто, кроме владельца и шести названных ходов.

**Прогон нашёл N115 и она починена в тот же заход.** Первый же ответ участника
в старом вечере стирал согласие всех остальных: клиент пишет точечно, и в
документе без карты это создавало карту из одного ключа, а переключатель читал
«карта есть → отсутствующий ключ значит „не спрашивали“». Теперь переключателем
служит `answersWrittenByOwner` — отметка о том, кто карту заполнял.

**Заведено за день:** N112 (владелец ждал ответа на своём вечере — починена),
N113 (`dontAsk` запретил правки вовсе), N114 (ушедший возвращался
согласившимся — починена), N115 (починена), правила I54 (поле-рассказ, которому
верит сервер) и I55 (порча мимо настоящего пути данных).

**Осталось из этой работы:** заведена отдельная работа «владелец может ответить
за участника» (`docs/plan.md`) — N114 другой рукой; правилом шага 4 не
закрывается по устройству.

---

# АРХИВ ТОГО ЖЕ ДНЯ: ШАГ 4 НАЧАТ. СТРОКУ НЕОБРАТИМОСТИ УЖЕ ПЕРЕШЛИ

**Работа: «договоры и мероприятия — одна сущность», `docs/plan.md`.** Шаги 1,
2 и 3 сделаны и закрыты; шаг 4 начат, и **первая его часть — та самая, после
которой назад дороги нет, — написана и запушена** (`606b9b9`).

**Что именно перешли.** В `answerOf` теперь: карты нет → `going`; **карта
есть, ключа нет → `notAsked`**; ключ есть → его значение. До этой правки
«карта есть, ключа нет» читалось как «идёт», после — как «его не спрашивали».
Откат вернёт согласие тем, кого уже показали неспрошенными.

**Заведено `notAsked`** — четвёртое состояние, читаемое, но никогда не
записываемое (оно и есть отсутствие ключа). Причина: первая редакция сливала
«не спрошен» и «ждём» в один `waiting`, то есть нарушала I47 назавтра после
того, как I47 было записано.

**Ответы переносятся при правке состава**, узкий вход `answersForRewrite()`
для писателя, сторож по исходникам держит ровно одно место вызова.

## ОСТАЛОСЬ ДВА — было четыре, два сделаны 11.08

| # | Что | Состояние |
|---|---|---|
| 4 | **занятость по `going`**, а не по `musicians` | **СДЕЛАНО 11.08**, проверено возвратом; закрывает N75 |
| 2 | **показ состава**: идёт / ждём / **не спрошен** / не может | **СДЕЛАНО 11.08**, проверено возвратом |
| 3 | **конфликт при согласии** — своим диалогом, тремя ответами | код; разбор принят владельцем 11.08, см. ниже |
| 1 | **правило в `firestore.rules`** — участник отвечает за себя | **выкладка правил = изменение прода**, последним |

### Что именно сделано 11.08 по пунктам 4 и 2

**Занятость — одной воронкой.** Заведена чистая функция `occupiesCalendarOf`
(`lib/shared/widgets/event_conflict_banner.dart`): владелец занят всегда,
остальные — только при `going`. Фильтр стоит **внутри `conflictEventsOnDay`**,
через которую ходят оба экрана, а не у вызывающих: отдай его вызывающим — и
«занято» разошлось бы между листом и календарём молча.

`currentUid` у трёх правил сделан **обязательным намеренно** — необязательный
оставил бы забытому вызывающему старое поведение, и оно ничем бы себя не
выдало (I31). Компилятор перечислил все места сам: **8 в `lib/`** (пять в
календаре, три в листе) и **25 в тестах**; я предсказывал 7 — счёт по `grep`
дал 8.

**Показ состава — словом, а не цветом.** `participantAnswerLabel`
(`lib/core/agreements/event_answers.dart`) даёт четыре разных слова:
`Gəlir` / `Gözlənilir` / `Soruşulmayıb` / `Gələ bilmir`. Показан в двух местах:
`_PartyRow` подробного экрана (**отдельной строкой, не вместо роли** — иначе
завели бы N109 второй раз) и фишки в карточке списка (после инструмента, тем
же разделителем).

**Цвета не тронуты намеренно (I51):** красный на этих экранах уже означает три
разные вещи (N110), палитра ломает своё правило (N111), и обе находки уехали в
работу о форме статуса. Покрасить «не может» красным сейчас значило бы завести
своими руками то, что мы там же и осудим.

**Форма правки состава НЕ тронута, и это названо, а не забыто:** там состав
редактируется, у только что добавленного человека ответа в документе ещё нет,
и ярлык был бы пустым у половины фишек. Показ там — отдельное решение, не
входящее в пункт 2.

**N112 заведена и починена в тот же заход.** `answersForParticipants` ставила
`waiting` **владельцу**, если он в составе (у договоров он там: `onChatUpdated`
кладёт обе стороны) — то есть спрашивала человека, идёт ли он туда, куда сам
всех позвал. Затронуто **18 документов из 75** (16 договоров = 27 − 11 и 2
мероприятия = 48 − 46, перепись 10.08). Умолчание владельца теперь `going`, но
**не безусловное**: его собственный знакомый ответ переносится — иначе будущая
дверь выхода владельца (N108) затиралась бы каждой правкой места.

### Числа этого захода

| Что | Число | Чем пересчитать |
|---|---|---|
| наборов тестов | **37 из 37 на диске**, неуспешных 0 | `flutter test --reporter json > лог 2>&1`, разбор `type == "suite"`; `ls test/*_test.dart \| wc -l` |
| ошибок `flutter analyze` | **0** | `flutter analyze > лог 2>&1` |
| замечаний `flutter analyze` | **44** (база 10.08 — 40) | там же; **ни одно не в моих строках**: 34 из них — давние `use_null_aware_elements` в `firestore_service.dart` |

**Расхождение +4 к базе замечаний не разобрано** — оно пришло не из этого
захода, а из правок 11.08 (шаг 4, первая часть). Хвост, не поломка.

**Три проверки возвратом, предсказания названы до порчи (I46):**
снятие фильтра занятости — сказал 3, упало **4**; снятие умолчания владельца —
сказал 1, упало **1**; слив `notAsked` с `waiting` в ярлыках — сказал 2, упало
**2**. Единственный промах — четвёртым покраснел сторож «в `lib/` нет
предупреждений анализатора»: порча была сделана как `if (false && …)`, а это
мёртвый код. **Промах впервые оказался в сторону ЗАНИЖЕННОГО покрытия** —
разбор в I46, счёт там же переписан.

### N113 — настройка запретила правки вовсе

`permissions.defaultMode: "dontAsk"` в `.claude/settings.local.json` — это
**«отказать без вопроса»**, а не «разрешить без вопроса». Правил на запись в
списке `allow` **ноль при 807 правилах**, поэтому на новой сессии две правки
подряд были отклонены молча. Верное значение — **`acceptEdits`**. **Файл не
тронут намеренно:** менять разрешения посреди работы значит рисковать
заблокировать руки на середине, а правка на живой сессии, по записи ниже,
возвращается программой обратно. Менять между сессиями и проверять делом.

**НАЧИНАТЬ ЗАВТРА НЕ С ПРАВИЛ.** Сперва код пунктов 2–4, **правила
последними, одним заходом с прогоном**. Довод: выкладка правил меняет права в
проде, и делать её посреди недописанного экрана значит смешать две переменные —
при поломке не разобрать, что сработало (I48).

Приём для правила уже выбран и лежит в плане: `diff().affectedKeys()
.hasOnly([request.auth.uid])`, как у `clearedBy` (`writesOnlyOwnClearedBy`).
Довод тот же: там чужой ключ стёр бы историю, здесь — ответил бы за человека.

## РЕШЕНИЯ 11.08, которые не должны потеряться

**1. Примиритель на сервере ОТМЕНЁН.** Довод был «mugam-v2 не обновится
никогда» — довод исчез: **следов записи mugam-v2 в проде нет 13 дней**
(коллекции `agreements` и `invites` молчат с 29.07, Expo-токенов 0, все 10
сообщений без `clientPlatform` оказались нашими же системными записями о
группах). Заменяет его перепись `censusAnswers.ts`: **база 0 расхождений при
75 документах**, и ненулевая недостача будет поводом завести примиритель **по
факту, а не по опасению**.

**Отдельно и честно (I50): в `personalEvents` отличить его запись от нашей
нельзя вовсе** — те же поля, тот же способ. Там вывод стоит не на данных, а на
слове автора, и это сказано прямо, а не закрыто правдоподобным.

**2. N62 ЗАКРЫТА.** Условие снятия было записано как **решение автора, а не
замер**, — оно получено. Само снятие `cancelledBy` — **не сейчас**: шаг 4
необратим, чужую правку мешать нельзя.

**3. Заведена работа «Разбор наследия mugam-v2»** (`docs/plan.md`) — девять
мест, **две части**: А — кодом (`cancelledBy`, дедуп push), Б — выкладкой
правил с прогоном (блоки `agreements` и `invites`, послабление по
`clientPlatform`, лазейка `system`, гонка create, чтение `pushTokens`).
**Части вместе не делать.** Цена ошибки названа по каждому месту: что
сломается у человека, если mugam-v2 всё-таки жив.

## Прогон шага 3 — пройден 11.08

Четыре пункта из четырёх на обеих трубках. **Вердикт тремя строками:** старое
не испорчено; новое не проверялось — его нет; **серверная половина в проде
отсутствует по решению** (выкладывать инертный фильтр незачем, поедет с шагом
4). Разбор — `docs/check-answers-step3.md`.

**Сборка на трубках свежая**, собрана начисто 11.08: **N89 воспроизвелась
второй раз** — `✓ Built` и код 0 при разошедшейся подписи. Записано числом: два
раза из двух после перерыва в правках `lib/`.

**СЧЁТ ПО N89 УТОЧНЁН ВЕЧЕРОМ 11.08 — прежнее «два из двух» было слишком
широким.** За день собирали четырежды:

| Сборка | Чистка перед ней | `codesign` |
|---|---|---|
| 1-я, шаг 4 | `rm -rf` был | **0** |
| 2-я, починка N115 | не было | **0** |
| 3-я, шаг 5 | не было | **ОТКАЗ** — `a sealed resource is missing or invalid` |
| 4-я, пересборка | `rm -rf` был | **0** |

**Значит признак не «после перерыва в правках», а проще: инкрементальная
сборка ИНОГДА оставляет подпись разошедшейся, а сборка начисто — ни разу.**
Два из двух чистых — целые; из двух инкрементальных одна отказала. «Иногда» —
это и есть худший вид: та, что прошла, ничем не отличалась на вид от той, что
не прошла.

**Порядок отсюда: `codesign` отдельной командой ПОСЛЕ КАЖДОЙ сборки, а при
отказе — `rm -rf build/ios/iphoneos build/ios/Release-iphoneos` и заново.**
Проверка стоит секунды, пересборка — двадцать минут, а невыявленное
расхождение стоит прогона по вчерашней сборке.

---

## 1. АРХИВ — состояние на конец дня 10.08

# ЧАСТЬ 6а ЗАКРЫТА 10.08: механика проверена на устройстве, показ переделывается работой о форме статуса

**Прогон пройден на трубках, вердикт владельца — «6а подтверждена
целиком».** Проверено: **вход в состояние** (Теймур вышел через разрешение
конфликта → сервер записал `[unsettled] h9VNI3WJJXYkmcsIt8kT → unsettled
(memberLeft)`), **повод** («İştirakçı ayrıldı — təşkilatçı qərar verir»),
**показ в списке и карточке**, **кнопка возврата в силу**.

**Формулировка закрытия — её и держать: механика проверена на устройстве,
ПОКАЗ переделывается работой о форме статуса.** То есть закрыт не «внешний
вид третьего состояния», а то, что состояние заводится, живёт, называет повод
и отпускается. Вид уходит в определённую работу (`docs/plan.md`, «РЕШЕНИЕ О
ФОРМЕ СТАТУСА»), и три находки прогона — **N109** (строка говорит роль вместо
события), **N110** (красный на одном экране означает три разные вещи, включая
созидательный возврат в силу), **N111** (палитра ломает своё правило) — входят
туда же.

**Что осталось непроверенным и почему — не забыть при следующем заходе:**
второй вход `workCancelled` (писателя нет до Части 7), выход в `cancelled`
владельцем (**N108** — двери нет), напоминание при молчании (нужна
приближающаяся дата), «повод обеим сторонам» (третьего в состав договора
добавить нечем — раздел состава есть только у обычного мероприятия).

---

## Перепись прода перед главной работой — снята 10.08, только чтение

**Число понадобится для сверки ПОСЛЕ работы «договоры и мероприятия — одна
сущность»: сверять можно лишь с тем, что снято ДО.**

| Что | Число |
|---|---|
| всего `personalEvents` | **75** |
| из них договоры (`isAgree === true`) | **27** |
| обычные мероприятия | **48** |
| по `status` | `agreed` 73, `unsettled` **1** (наш, 15.08), `cancelled` 1 |
| с непустым `musicians` | 41 |
| где в `musicians` больше одного | 25 |
| с `parentEventId` | **0** — поля ещё нет, как и ожидалось |
| без `lastActionType` вовсе | **58 из 75** |

**По владельцам (договоры / обычные / всего):** Teymur **12 / 32 / 44**,
Rafael **8 / 10 / 18**, Muhammed **4 / 0 / 4**, безымянные учётки — 5, 2, 1, 1.

**Число из довода `isParty()` переснято и ДЕРЖИТСЯ (I40 — говорю и о том, что
сошлось):** владельца нет в `musicians` у **11 договоров из 27** — в реестре
стояло «11 из 26», то есть само число то же, знаменатель вырос на один. А вот
у **обычных мероприятий владельца нет в `musicians` у 46 из 48** — и это не
дефект, а устройство: `addPersonalEvent` кладёт туда только выбранных.

Пересчитать: скрипт по `personalEvents` с разбивкой по `isAgree` и владельцам,
только чтение.

---

**Прежняя точка входа (выполнена):** сборка на трубки и прогон 6а по
`docs/check-unsettled-status.md`.

**Про сборку, чтобы не терять время зря:** бундл с 6а уже ставился на обе
трубки 10.08 в 03:35–03:37 и запускался. После этого в `lib/` не менялось
ничего — только тесты, документы и скрипты переписи. **Если завтра не
трогать клиентский код, пересобирать не нужно: на трубках уже стоит нужная
сборка.** Проверить, что это она: подпись `codesign --verify --deep
--strict` и поиск строк `Onsuz davam edirəm` / `şübhə altındadır` в
бинарнике — **по UTF-16, не по UTF-8** (N103).

**Утверждённое ниже изменение плана прогону 6а НЕ мешает и его не
отменяет:** третье состояние остаётся, меняется только то, как оно
называется на экранах. Прогнать надо на том, что выложено сегодня.

**СБОРКА ПЕРЕПРОВЕРЕНА 10.08 перед прогоном — ставить заново не нужно.**
`codesign --verify --deep --strict` дал 0 отдельной командой; `Onsuz davam
edirəm` и `şübhə altındadır` нашлись в `App.framework` по **UTF-16LE** (по
UTF-8 — ноль, как и должно быть, N103), при канарейке на заведомо
существующих строках. `ownerDoubt` — ноль, и это верно: третий повод не
написан.

**Признак «`lib/` не тронут после сборки» больше не работает, и это не
повод пересобирать (I33).** В `agreement_card.dart` дописан **комментарий**
про границу действия I28. Поведение не изменилось: `git diff` по `lib/` —
14 добавленных строк, **не-комментариев ноль**; `flutter analyze` в этом
файле — ноль замечаний. Проверять надо содержимое, а не отметку времени.

**ПРОГОН УМЕНЬШИЛСЯ ВДВОЕ ПО СОСТАВУ, и это его условие, а не результат.**
Три места названы до начала и помечены в `docs/check-unsettled-status.md`:
**§2 (второй вход `workCancelled`) — писателя нет вовсе**; **шаг 7 (выход в
`cancelled`) — N108, у владельца нет двери**; **§6 (молчание) — вне
сессии**. Остаётся один вход из двух и один выход из двух. Состав прогона
задан там же: договор Рафаэль ↔ Теймур **плюс третий в составе** — Ramil
Sumqayit `zEyNLGpzVpbsBChYnbxExC1cejI2`, иначе «повод обеим сторонам»
проверить нечем.

---

### PUSH НА ЭТИХ ТРУБКАХ НЕ ПРИХОДИТ. Читать до всего остального

**Аккаунт Apple бесплатный, Push Notifications им не поддерживается на
уровне учётной записи. Push на iOS здесь не работал НИКОГДА:** живых
iOS-токенов ноль, 673 неудачные отправки за 30 дней (**замер 02.08 по
журналам Cloud Functions**; `docs/plan.md`).

**Это ДРУГОЙ счёт, чем оплата Google.** 09–10.08 чинили оплату Firebase —
на Apple это не влияет никак. Ошибка уже была сделана: «оплату
восстановили» перетекло в «уведомления придут», и в шаги проверки трижды
попало «должно прийти уведомление». **Не ставить уведомление в шаги.**
Сервер проверяется журналом:

```
gcloud logging read 'resource.labels.service_name="markeventunsettled"' \
  --project=mugam-club --limit=10 --freshness=1h --format="value(timestamp,textPayload)"
```

---

### УТВЕРЖДЕНО АВТОРОМ 10.08 — договора как отдельной вещи больше нет

**Договоры и мероприятия становятся ОДНОЙ сущностью.** Это не переименование
и не косметика: сегодня `isAgree` разводит их по двум экранам, и вся
путаница последних находок растёт оттуда.

Что решено:

- **договорённость — это строка внизу карточки вечера**, а не отдельная
  вещь со своим списком;
- **любое добавление человека — это ПРИГЛАШЕНИЕ.** В состав человек
  попадает **только после согласия**. Сегодня наоборот: владелец вписывает
  людей сам, и никто ничего не подтверждает;
- **вкладка «Müqavilələr» станет фильтром в календаре**, а не отдельным
  местом;
- **слово «договор» уходит с экранов.**

### Что входит в то же изменение

- **три ответа в составе: идёт / ждём / не может** — вместо сегодняшнего
  «есть в списке или нет»;
- **приглашения своих — отдельными документами с `parentEventId`**, у
  зовущего схлопываются в одну строку (устройство, выбранное 09.08,
  остаётся);
- **создание вечера голосом**, несколько записей подряд;
- **выбор нескольких дней сразу**;
- **цвет и три буквы имени на дне календаря**;
- **статус формой: залито — точно, рамка — под вопросом**;
- **третий повод `unsettled` — `ownerDoubt`** (сам зовущий засомневался).
  Сегодня поводов два: `memberLeft` и `workCancelled`;
- **группы состава** — список людей с именем.

### Макеты — где они на самом деле лежат

В `docs/design/` пока только старые, 1–3 (05.08). **Новые лежат в
`assets/`, вперемешку с ресурсами приложения, и в `pubspec.yaml` не
подключены** — проверено, ноль ссылок и в коде тоже:

| Что | Файл |
|---|---|
| создание голосом, три состояния | `assets/tedbir/files/mugam-4-ses.{html,png}` |
| календарь с пометками | `assets/kalendar/files/mugam-5-teqvim.{html,png}` |
| карточка вечера | `assets/yenilenecekler/files/mugam-6-kart.{html,png}` |
| календарь, вторая редакция | `assets/yenilenecekler/kalendar 2/mugam-7-teqvim.{html,png}` |
| календарь, третья редакция | `assets/yenilenecekler/kalendar/mugam-8-teqvim.{html,png}` |

**Их надо собрать в `docs/design/` одним заходом** — сегодня они
неотслеживаемые, лежат в трёх разных папках, и `assets/` для них не место:
туда смотрит сборка. Не сделано намеренно: 16 МБ двоичных файлов кладутся
в историю один раз и навсегда, это решение владельца, а не попутная
уборка.

### Что это изменение ЗАКРЫВАЕТ из открытого

Записано, чтобы завтра не разбирать заново как живые вопросы:

- **N107** («приглашение приходит молча») — закрывается по существу:
  добавление становится приглашением с согласием, и у него появляется своё
  состояние;
- **N106** («выйти нечем») — закрывается ответом «не может» в составе:
  это и есть недостающая дверь;
- **вопрос 4 Части 7** («чем является шаблон состава») — отвечен: группы
  состава как список людей с именем;
- **вопрос 1** («где живёт кнопка») — снимается сам: экран остаётся один.

Открытыми из четырёх остаются **вопрос 2** (один список людей или два) и
**вопрос 3** (что видно, когда часть приглашений не ушла). Разбор с
числами и ценой — `docs/check-call-my-people.md`.

### ВАЖНО: `docs/plan.md` ещё НЕ переписан под это решение

Там по-прежнему Часть 13 «Договоры переезжают в календарь» как будущая
работа и `isAgree` как граница. **Два файла говорят разное, и это ровно
тот класс, которым уже платили (N80: читатель выбрал не тот).** Пока
верен ЭТОТ раздел — он датирован позже. Переписать план — отдельная
работа, и делать её надо целиком, а не по кусочку.

---

### Что сделано за этот заход

**Прод стоял шесть часов: 09.08, 21:16 → 10.08, 03:16 по Баку.** Оба
платёжных счёта были закрыты (N102), оплату восстановил владелец.
**Простой не стоил ничего, и это доказано:** пачки задним числом не
пришло, очередь была пуста всё время (18 подписок, пик непоставленных 0),
все три вечных расхождения проверены поимённо и дали ноль — событий за
эти часы не было вовсе. Разбор с канарейками к каждой пустоте —
`docs/deployed.md`.

| | Состояние |
|---|---|
| правила Firestore | **в проде** 09.08 21:40 UTC, набор `2432135e…`, `diff` с репозиторием пуст, канарейка против старой редакции 56 строк |
| функции | **в проде** 09.08 23:26 UTC, все 31 включая `markEventUnsettled`; сверено по составу имён, разность пуста в обе стороны |
| сборка с 6а | **стоит на обеих трубках** 10.08 03:35–03:37, обе запущены; что в бундле именно 6а — проверено поиском по UTF-16 (N103) |
| прогон 6а | **начат, остановлен на шаге 2** — он и дал N106 и N107 |

**Две переписи заведены и снимаются заново** (из `functions/`, только
чтение): `censusLostAgreements.js` и `censusMessageCount.js`. Их слепые
пятна печатаются в самом выводе. Главное: **их ноль стареет** — новое
предложение в чате стирает след пропажи навсегда.

**Правило I31** — проверка, утверждающая ОТСУТСТВИЕ, ломается тише, чем
утверждающая наличие; канарейка закрывает слепоту к материалу и не
закрывает слепоту к составу условия. Пять канареек поставлено, каждая
проверена возвратом порознь.

**Находки:** заведены N102–N107. Закрыты N103, N104, N105; открыты N102
(признак остаётся врать и после починки), **N106 и N107 — на решении
владельца**.

**Часть 12а (неисчезающий след согласия) разблокирована:** её отложили до
восстановления оплаты, оплата восстановлена. Цена и доводы — в
`docs/plan.md`, раздел «Часть 12а».

Номера коммитов здесь не пишутся — снимается `git log --oneline -1`.

---

### НЕЗАКРЫТОЕ — три вещи, названные вслух

1. **Часть 6а выложена, но на трубках не проверена ни на шаг.** Первое
   дело завтра (см. самый верх).
2. **Четырнадцать широких разрешений в `.claude/settings.local.json`
   остаются** — они открывают выкладку, запись в прод и удаление. Убрать
   их на живой сессии нельзя: программа держит список с её старта и
   записывает убранное обратно (проверено делом). Порядок: перечитать
   настройки, затем убирать, затем проверять делом.
3. **Четыре хвоста прогона пункта 6**, все по полминуты: кнопка на «Bu
   gün» после 19:00 пропадает; у получателя ничего не появляется от
   закрытого свайпом листа; отказ при открытом раунде возвращает к списку
   людей; заголовок «Müqavilə» над мешающим договором. **Последний
   решается сам утверждённым решением — слово уходит с экранов.**

---

### ХВОСТ: `musiciansProvider` — отдельным заходом ПОСЛЕ шага 1

**8 мест, 4 файла, только клиент — ни базы, ни правил, ни миграции. Это
N87.** Провайдер **буквально равен** `allUsersProvider`: обе строки зовут одну
функцию `watchAllUsers()`, фильтр по роли снят давно (он делал всех, кто
зарегистрирован как «qonaq», невидимыми навсегда).

Имя врёт дважды: он не про музыкантов (он про всех) и не отличается от
соседа. **Отдельным заходом, а не попутно** — иначе две работы в одном, и
непонятно, чья правка что сломала.

Родня — N69: слово `musicians` в проекте означает **пять разных вещей**
(состав вечера, этот провайдер, пустая коллекция в правилах, параметр
карточки ленты, роль пользователя). Разбор и цена — `docs/plan.md`, работа
«договоры и мероприятия — одна сущность».

### ХВОСТ: ТРИ СТОРОЖА ИЗ ДЕСЯТИ БЕЗ ГРАНИЦ — наше требование, не выполненное

**Требование записано в `CLAUDE.md`: «рядом с каждым сторожем пишется, чего
он не ловит».** Замер 10.08 показал, что оно не выполнено, и **пример в самом
разделе был один** — ровно тот случай, ради которого заведено I39.

| Сторож | Границы |
|---|---|
| `no-swallowed-exit.sh` | есть, в файле |
| `verify-bundle-before-install.sh` | есть, в файле |
| `no-destructive-git.sh` | есть, но **в `CLAUDE.md`**, не в файле |
| `day_buckets_test` | есть |
| `no_mock_data_test` | есть |
| `source_invariants_test` | есть |
| `guards_are_guards_test` | есть |
| **`agreement_cancel_test`** | **НЕТ** |
| **`soon_tabs_test`** | **НЕТ** |
| **`color_literals_test`** | **НЕТ** — есть раздел «что этот сторож делает», но это не границы |

**Итого 3 из 10, знаменатель назван намеренно (I40):** «трое без границ»
читается как поломка, «трое из десяти» — как долг, и второе верно.

**Тот же род, что N56: правило записано, а сверки не было.** Там реестр
отставал от кода не случайно, а системно, и лечилось это не памятью, а
красным тестом. Здесь так же: пока не появится проверка «у каждого текстового
сторожа есть строка о том, чего он не ловит», требование останется
пожеланием. **Проверка возможна** — это разбор по исходникам, как остальные
семь, и она утверждала бы НАЛИЧИЕ, то есть канарейки не требует (I31).

**Не чиню сейчас:** трогать три сторожа во время прогона Части 6а — это
правка того, чем прогон и проверяется. Делать отдельным заходом вместе с
долгом по восьми соседкам: там те же файлы.

### БАЗОВЫЕ ЧИСЛА ПЕРЕСНЯТЫ 10.08 — пять из шести разошлись (I7)

Повод: `flutter analyze` дал **40** замечаний против записанных **36**. Само
это ничего не значило (в правленом файле замечаний ноль), но подняло вопрос
шире: **какие ещё числа лежат здесь как база и с тех пор не пересняты.**
Пересняты все, которые снимаются даром — по коду и по прогонам:

| Что | Записано | Снято 10.08 | Чем пересчитать |
|---|---|---|---|
| замечаний `flutter analyze` | **36** (03.08, база N8) | **40**, ошибок ноль | `flutter analyze > лог 2>&1` |
| тестов `flutter test` | **95** (03.08) | **393**, все прошли, 36 наборов из 36 | `flutter test --reporter json > лог 2>&1` |
| мест `Colors.<имя>` в `lib/` (N81) | **180** (08.08) | **173** | `grep -roE "Colors\.[a-zA-Z]+" lib/ --include="*.dart" \| wc -l`; канарейка — свои имена, 1264 |
| файлов тестов (счёт I30) | **37** (09.08) | **36** | `ls test/*_test.dart \| wc -l` |
| функций в коде | **30** (архив, 08.08) и **31** (раздел 0, 09.08) | **31** обеими формами поиска | `grep -cE "^export const " functions/src/index.ts` |
| пользователей в Firestore | **12** (07–08.08) | **12 — держится** | разовый скрипт по `users`, только чтение |

**Команды в последней графе стоят там по расширенному I6, и расширен он
этим же случаем.** Правило читалось узко — «данные прода называют дату», — а
разошлись числа по **коду и прогонам**, которым дату никто не ставил.
Граница теперь не «прод против кода», а откуда число взято: **снято извне**
(прод, прогон, ОС) — нужна **дата**; **выводимо из репозитория** — нужна
**команда**, и она же и есть пометка срока годности, потому что читатель
пересчитает даром вместо того, чтобы верить. Число без даты и без команды
читается как вечное — так и соврало пять раз. **Сторожа на это нет и быть не
может:** номера находок, правил и строк — тоже числа, текстом их от замеров
не отличить.

**Судьбу решения меняет только первое.** «36, ни одного нового» стояло
воротами приёмки: сравнение с ним теперь даёт +4 на пустом месте, и следующий
станет искать поломку там, где её нет. **Хвост заведён, не чиню сейчас** —
разбор четырёх лишних отдельным заходом; шесть из сорока живут в `test/`,
которых в базе 03.08 не было вовсе, так что часть расхождения объясняется
самим ростом проекта, а не порчей.

**Отдельно про 30 против 31: в ОДНОМ файле два числа об одном.** Это не
противоречие — архивный раздел датирован 08.08 и верен на свою дату, — но
читается как противоречие, и выбирают наугад (N80 дословно). Правило чтения:
**числа в архивных разделах читаются вместе со своей датой**, а не как
текущее состояние. Текущее — только в разделе 0 и в `docs/deployed.md`.

**Что НЕ переснято, и чем снимать** (все требуют прода или эмулятора, то
есть времени, а не команды):

- ~~**16 в Firebase Auth** против 12 в Firestore (N78) — нужен листинг
  Auth~~ — **СДЕЛАНО 10.08, стоило минуту** (Auth 16, `users` 12, без
  профиля четыре). Записано зачёркнутым, а не удалено, потому что урок
  дороже строки: пункт лежал в «непереснятом» как требующий доступа,
  **которого у меня всё время хватало**. «Замер недоступен» проверяется
  попыткой, а не оценкой;
- **26 договоров, владелец вне `musicians` у 11** (06.08) — на этом числе
  стоит довод правила `isParty()`, то есть оно дороже остальных;
- **673 неудачные отправки push за 30 дней** — прод, **замер 02.08 по
  журналам Cloud Functions**;
- **240 совпадений по синонимам** — разовая команда, нигде не хранится,
  переснять можно только набрав заново (I16).

**А два числа отсюда пересняты не мной и не сегодня — они лежали в
СООБЩЕНИИ КОММИТА, пока конспект показывал старые:**

| Что | В конспекте было | Снято 10.08, коммит `8361455` |
|---|---|---|
| чатов и сообщений | 8 чатов, 370 сообщений (07–08.08) | **12 чатов, 377 сообщений**, расхождение 0 |
| тестов functions | 251, один упавший без имени | **258 из 258**, отдельным вызовом до выкладки |

**Оба исправлены выше по файлу. Хвост «один упавший без имени» остаётся** —
причина так и не названа, — но рамка у него другая: после того прогона был
полный зелёный, значит это не «висит красное», а «однажды мигнуло и не
повторилось».

**Класс, который тут виден, важнее самих двух чисел.** Свежий замер попал в
запись, которую **нельзя исправить и не принято перечитывать**, а файл,
который читают первым, остался со старым. Получилось наоборот нужному:
неизменяемая запись оказалась точнее читаемой. Отсюда порядок на будущее:
**замер, попавший в сообщение коммита, в тот же заход переносится в
конспект** — иначе он сохранён и потерян одновременно.

---

### ДВА СЛОВА ЖДУТ АВТОРА — прогону не мешают

Оба стоят временными, каждое **в одном месте**, помечены в коде:

- **надпись на кнопке возврата** — сейчас `Onsuz davam edirəm`
  («продолжаю без него»). Смысл, который она обязана нести: не «всё в
  силе», а именно «продолжаю без него» — состав не восстановился;
- **тексты уведомлений** о переходе и напоминания — сейчас
  «Müqavilə şübhə altındadır» / «Sabah: tədbir şübhə altında» плюс повод
  («iştirakçı ayrıldı» / «iş ləğv olundu»). **Увидеть их на трубке
  нельзя** — только в коде и журнале.

---

### Как продолжить прогон, когда решения будут

`docs/check-unsettled-status.md` — шаги написаны до кода и поправлены
10.08 по факту. Дорога выхода из состава там теперь описана настоящая:
Теймур создаёт своё мероприятие на ту же дату и время → в вопросе о
конфликте выбирает `Təqvimimdən sil` → подтверждает. Своё мероприятие
можно не сохранять: выход — отдельный ход.

Сажать мероприятие в `unsettled` руками — **отказано владельцем 10.08**,
довод оставлен на случай, если предложат снова: это правка боевых данных
ради проверки.

---

### Архив: что сделано за ночь 08–09.08 (заход до этого)

| Работа | Состояние |
|---|---|
| **пункт 6** — одна точка вызова предложения | закрыт, **проверен на трубках**: пять входов, сторож из четырёх счётов |
| **дверь в карточку по `eventId`** | сделана, проверена: тап на дневном экране и в календаре открывает карточку, договор открывается карточкой ДОГОВОРА |
| **прошлое в календаре** | приглушено и читаемо, «Boş idi»; сетка считает разложением, а не проходом на каждый день |
| **Часть 6а** — третье состояние `unsettled` | **написана целиком, не выложена, глазами не видена** |

**Правил выведено восемь: I23–I30.** Коротко, чтобы знать, что искать:

- **I23** — правило со вторым читателем выносится в тот же заход;
- **I24** — инлайн-условие несёт двойную службу: проверяет и сужает тип;
- **I25** — сторож, запрещающий законное, будет снят вместе с верной частью;
- **I26** — правило, законное для одного действия, ломается от новой дороги;
- **I27** — шаги проверки пишутся ДО КОДА: находят отсутствующие дороги
  и незаданные вопросы;
- **I28** — новое значение признака отличают там, где решают, что делать
  дальше, а не везде;
- **I29** — подтверждение нужно НЕОБРАТИМОМУ действию, а не «важному»;
- **I30** — улучшение кода может снять видимость с того, что сторожит его
  текст.

**Находки:** заведены N85–N101. Закрыты N87–N96 и N100, N101; открыты
N85, N86, N90 (частично), N97, N98, N99.

---

### Что ждёт РЕШЕНИЯ ВЛАДЕЛЬЦА, а не работы

**Пункт 7 («Öz adamlarımı çağır») — устройство выбрано, четыре вопроса
открыты.** Выбран вариант «отдельный документ на каждое приглашение плюс
`parentEventId`»: он не трогает `firestore.rules` и не трогает сторону
приглашённого. Шаги проверки написаны до кода —
`docs/check-call-my-people.md`. Осталось решить:

1. на каком экране живёт кнопка — на договоре, на мероприятии, на обоих;
2. выбор людей одноместный: расширять его или заводить второй;
3. что показывать, когда часть приглашений не ушла (раунд на чат один, и
   с кем-то он уже открыт);
4. что происходит с приглашениями своих при отмене родителя.

**Порядок работ:** 6а → 7 → 7а → 7б. Часть 6а стоит перед 7, потому что на
третьем состоянии стоят обе работы.

---

### Долги, записанные числом, чтобы не всплыли как открытия

**1. Четырнадцать широких разрешений** в `.claude/settings.local.json`
открывают выкладку, запись в прод и удаление. **Убрать их на живой сессии
нельзя** — программа держит список с её старта и записывает убранное
обратно (проверено делом). Порядок: перечитать настройки (`/hooks` или
перезапуск), затем убирать, затем проверять делом.

**2. Семь текстовых сторожей** ломаются от выноса значения в константу, из
них **пять — молча** (те, что проверяют отсутствие). Механическая проверка
названа: каждому сторожу отсутствия — соседка-канарейка. Разбор в
`docs/plan.md`, раздел Части 6а.

**3. Четыре хвоста прогона пункта 6**, все по полминуты: кнопка на «Bu
gün» после 19:00 пропадает; у получателя ничего не появляется от закрытого
свайпом листа; отказ при открытом раунде возвращает к списку людей;
заголовок «Müqavilə» над мешающим договором.

**4. Один упавший тест, причина неизвестна.** Первый полный прогон
functions 09.08 показал 1 из 251, повтор дал зелено, имя упавшего утонуло
в логах. **Из этого выведено правило** (CLAUDE.md): вывод длинного прогона
пишется в ФАЙЛ, и разбирается файл.

---

### Состояние железа, прода и выкладок

**Обе трубки на сборке 09.08, 05:26 — в ней ВЕСЬ пункт 6 и дверь в
карточку, но НИЧЕГО из Части 6а.** Рафаэль
`00008101-001261402669003A` (кабель), Теймур `00008101-001A146911A3003A`
(по сети). Собирать по разделу «Собирать РЕЛИЗ, ставить через devicectl»
ниже в архиве; `flutter run --release` установку не доводит.

**Функции выложены 08.08** — все 30, состав сверен по именам (30 в коде,
30 в проде, разность в обе стороны пуста). Правила Firestore выложены
06.08 и сходятся с репозиторием; этой выкладкой не трогались.

**Данные прода, сняты 07–08.08, только чтение** (I6 — пересниматься
скриптами из `functions/src/scripts/`):

- **ПЕРЕСНЯТО 10.08 листингом Auth, противоречие 15/16 разрешено.**
  В Firebase Auth **16**, в `users` **12**, **без документа в `users` —
  ЧЕТЫРЕ, а не три** (N78 говорила «три», это следовало из спорного 15).
  Домены Auth сошлись со старой разбивкой в точности: `m.com` 11,
  `example.com` 3, `gmail.com` 1, `n.con` 1 = 16. Документов `users` без
  учётки в Auth — **ноль**. Пересчитать:
  `getAuth().listUsers()` плюс чтение `users`, только чтение.

  **Состав четырёх назван, потому что он объясняет, откуда взялось «три»:**
  три учётки `qa-menu-a/b/c@example.com` от 02.07 — следы прогона, — и
  **`qarimxan@gmail.com`, созданная 07.08 и не входившая ни разу**: это
  учётка владельца, заведённая при работе 5а (сброс пароля). То есть «три»
  было верно до 07.08, а замер 08.08 его уже не пересчитал;

- **`подтверждённых ноль` — БОЛЬШЕ НЕ НОЛЬ: подтверждён один,**
  `qarimxan@gmail.com` (проверка адреса при работе 5а). Строка ниже
  исправлена. **Это лучший пример того, чем опасны нули (I37):** утверждение
  тихо перестало быть верным, и ни одна проверка не могла этого заметить —
  у нуля нет значения, по которому его отличают от другого нуля;
- почта: подтверждённых **один** (`qarimxan@gmail.com`, 10.08; было «ноль»
  на 08.08); домены `m.com` — 11, `example.com` — 3, `gmail.com` — 1,
  `n.con` — 1 (опечатка, N78) — разбивка **по Auth**, сумма 16;
- чатов **8**: групповых 2, личных 6; сообщений суммарно **370**.
  **УСТАРЕЛО: 10.08 снято 12 чатов и 377 сообщений** (коммит `8361455`,
  расхождение 0). Пересчитать — `node lib/scripts/censusMessageCount.js`
  из `functions/`;
- договоров со следами отмены — 2.

---

### Что сделано за заход — по работам

| Работа | Итог |
|---|---|
| прогон восьми пунктов дневного экрана | **8 из 8** на Теймуре |
| **N74** — сетка месяца считала свой счёт | найден прогоном, починен, подтверждён на двух трубках |
| **план из 17 частей** | перенесён в `docs/plan.md`; порядок работ сведён в ОДИН |
| **работа 5а** — сброс пароля | сделана и проверена целиком: экран → письмо → смена пароля → вход |
| **N76** — имя автора в строке группы | написан, выложен, закрыт на двух трубках, четыре случая из четырёх |
| **триггер отмены договора** | проверен на трубках; оказалось, он был в проде с 05.08, а не «впервые» (N80) |
| **экономия N43** | впервые измерена: 1 переиндексация на трубку за 10 минут вместо ~10 |
| **работа 5б** — цвета в одно место | 104 места из 133, пять имён, проверена глазами на шести экранах |
| **сторожа** | переписаны четыре, все проверены возвратом |

**Правил выведено восемь: I15–I22.** Коротко, чтобы знать, что искать:

- **I15** — способ измерения проверяется тем же вопросом, что проверка:
  при каком поведении кода он даст другой ответ;
- **I16** — разовая команда не хранится, значит не проверяется; защита
  одна — назвать ожидаемое число вслух и сверить;
- **I17** — в чистую функцию выносится то, что можно проверить возвратом;
  функция ради формы ничего не доказывает;
- **I18** — «ничего не шлёт» и «ничего не меняет» — разные утверждения;
- **I19** — проверка, ничего не изменившая в решении, называется
  подтверждением, а не находкой; и не предлагай имя, пока не прочитано
  хотя бы одно место из группы;
- **I20** — закрыв одну форму записи, спроси, какими ещё формами то же
  выражается. Цена измерена: поиск по форме дал **6% пропусков**;
- **I21** — отсутствие строки нельзя оспорить, потому что никто не
  заметит, что её нет. Список проверяемого пишется ДО обхода;
- **I22** — связанные значения получают ОДНО имя: раздельные разрешают им
  разойтись.

**Находки:** закрыты N66, N70, N74, N76, N84. Заведены и открыты N69,
N71–N73, N75, N77–N83. Всего открытых сейчас **29**.

---

### Что НЕ закрыто и требует решения ВЛАДЕЛЬЦА, а не сессии

1. **N79 — письмо сброса пароля ложится в спам и написано по-английски.**
   Механизм работает, цель работы достигнута наполовину: забывший пароль
   во «Входящих» не найдёт ничего. Чинится **настройкой проекта**, не
   кодом: консоль Firebase → Authentication → Templates → Password reset;
   там же язык шаблона и `Customize domain` для отправителя.
2. **N78 — три учётки могут войти без профиля**, и из этого состояния
   **нельзя выйти из аккаунта**: выход живёт в настройках, а в настройки
   попадают только из профиля, куда без профиля не попасть. Решить, что
   делать с записями; отдельно — валидатора адреса в регистрации нет
   вовсе.
3. **Что в «Klub» считается новым** — без ответа значок на вкладке не
   делается (Часть 4 плана).
4. **`docs/design/README.md`, «работа 5»** — содержимое строки с уборкой
   не сходится, похоже на описку. Намеренно не переведено.

---

### Долги, записанные списком, чтобы не всплыли как открытия

**1. Сторожа на `Colors.<имя>` нет — 180 мест без присмотра (N81).**
Завтра на обычном экране появится `Colors.white` вместо `kText`, и не
встанет ничто. Способ предложен — разделение по файлам, медиа против
обычных, — и **не проверен**. Ставить условились после 7б.

**2. Восемь соседок «нашлось хоть что-то»** — `docs/plan.md`, раздел
«Долг по сторожам». Вердикты вида «разобрал текст, плохого не нашлось»
зелены в том числе на пустом разборе.

**3. В работу 7б уехало пять решений о ВИДЕ:**

- десять осветлений `Colors.white.withAlpha(3…38)` — свести семь
  прозрачностей к двум именам;
- три красных: `Colors.red` (11) и системный `0xFFFF3B30` (1) → `kRed`;
- два зелёных «в сети» `0xFF4CAF50` → `kGreen`;
- плитка приложения — три экземпляра к одному цвету;
- бейдж `Müqavilələr` в красном состоянии.

**4. N75 — три сшивки «мои плюс участник»** в форме мероприятия и
проверке конфликтов. Вопрос там про ЗАНЯТОСТЬ, а не про показ, и по
аналогии с N74 не решается.

---

### Что стоит помнить про этот заход

**Обе находки вечера — N74 и N76 — нашёл человек, смотрящий на экран**,
а не код и не обзор. Кружок «4» при трёх строках под ним и «кто написал в
группу» — ни одна проверка их не искала.

**Проверка «на 9 avqust должно быть 3» прошла бы на Теймуре и упала на
Рафаэле** — у него там законные четыре. Проверять надо ОТНОШЕНИЕ, а не
константу, снятую с одной трубки.

**Порядок проверок решает.** В N76 случай «у системной записи имени нет»
пришёл первым и один не доказывал ничего: имя отсутствовало бы и при
неработающей правке. Смысл он получил только после того, как в двух
других случаях имя ПОЯВИЛОСЬ.

**Проход по форме сужает работу, но не заменяет чтения.** «Проход нашёл
67» не значит «мест 67» — это значит «мест не меньше 67».

---

## 1а. АРХИВ — с чего начинали 04.08, вечер (не актуально)

**Всё запушено.** Коммиты сеанса, по порядку: `28dbaba` (тест форм
запросов, N32 найден), `d7b1e83` (правило ухода из чата + сторож по
исходникам), `403abd0` (починка N32), `529cd3f` (реестр: N33–N36 и три
правила проекта), плюс этот конспект. В дереве только давний шум `ios/`
(N6) и неотслеживаемые assets — не коммитим.

**Обе трубки на сборке от 04.08, 17:18** — она включает правило ухода, но
**НЕ включает** починку N32 (её собрали позже коммитом `403abd0`, на
трубки не ставили). Собирать по разделу ниже; `flutter run` не годится.

### Что сделано за сеанс

| | |
|---|---|
| **N32** | уборка `activeUsers` на логауте не срабатывала ни разу — фильтр по `activeUsers` не доказывал правило чтения. Починен, проверен **фактом уборки в данных** после реального логаута: до — 1 отметка, после — 0 |
| **Правило ухода** | `leaveGroup` и `removeGroupMember` сведены в одну чистую функцию `lib/core/chat/chat_departure.dart`; `activeUsers` снимается вместе с членством. Проверено вживую на двух трубках: **обе дороги пишут оба поля одной записью** (14:29 и 13:27) |
| **N18** | причина не в малом пороге, а в сложении ТРЁХ ожиданий в одном `beforeEach`. `Promise.all` + порог 15 с от замеренного потолка 6.2 с |
| **Проверка N27 #5** | переписана (описывала несуществующее действие) и пройдена |

### Что найдено и записано, но НЕ починено

`N33` (экран чата не перепроверяет членство), `N34` (`watchChatMeta`
выдаёт удалённый чат пустым), `N35` (крестик на фишке участника не пишет
`_explicitlyRemoved`), `N36` (повторное подтверждение отмены переписывает
`cancelConfirmedBy` — **живёт в задеплоенных правилах**).

N33 и N35 клиентские, чинить **одной пересборкой** — так решено осознанно,
трижды за сеанс: окно с двумя живыми трубками дороже.

### Четыре правила проекта — читать в `AUDIT_TODO.md` до кода

1. **Клиентское состояние без сервера за спиной не размножать по путям.**
2. **Проверка называет ДОРОГУ, а не только исход.** Из 8 пунктов N27
   четыре описаны одним путём при наличии второго.
3. **Класс «два пути к одной операции, правило применено к одному».**
4. **Находку не чинят в одной сборке с проверками, которые она может
   подделать.** Мерить надо не размер правки, а пересечение: что она
   трогает и где идут проверки. Выведено 04.08 на N33 — принято
   владельцем как правило проекта, а не как разовое решение.

### Собирать РЕЛИЗ, ставить через devicectl

```
pgrep -f "Xcode.app/Contents/MacOS/Xcode"      # ДОЛЖНО быть пусто
flutter build ios --release
codesign --verify --deep --strict build/ios/iphoneos/Runner.app   # ОТДЕЛЬНОЙ командой
xcrun devicectl device install app --device <id> build/ios/iphoneos/Runner.app
xcrun devicectl device process launch --device <id> com.mugam.mugamFlutter
```

**Третья строка добавлена 09.08 и стоит там не для порядка (N89).**
`flutter build` сказал `✓ Built` и вернул 0 на бундле, чья подпись
разошлась с содержимым: инкрементальная сборка обновила `App.framework` с
кодом Dart и не переподписала целое. Признак успеха отличает «собралось»
от «не собралось» и не отличает больше ничего.

Чем это грозит — не подменой кода, а тем, что **установка откажется, а на
телефоне останется вчерашняя сборка**, и её будут проверять как
сегодняшнюю. Выглядит это как «поведение не изменилось», то есть как
отрицательный результат проверки.

Лечится пересборкой начисто:
`rm -rf build/ios/iphoneos build/ios/Release-iphoneos` и снова
`flutter build ios --release`.

**Помнить это не требуется:** установку через `devicectl` держит сторож
`.claude/hooks/verify-bundle-before-install.sh` — он сам проверяет подпись
и не пускает дальше при расхождении. Границы записаны в нём самом; главная
— он видит, что печать сошлась с содержимым, и **не видит, ТА ЛИ ЭТО
сборка**.

**Проверку запускать отдельной командой, без трубы.** 09.08
`codesign … | head` показал 0 при отказе: код возврата у конвейера берётся
у последней команды (I10).

`flutter run --release` **не доводит установку до конца** в этом проекте:
собирает в `build/ios/Release-iphoneos/`, а ищет в `build/ios/iphoneos/`,
которую создаёт только `flutter build`. Воспроизводилось на обеих трубках.
Разбор отладочных сборок и почему они не годятся — ниже, в блоке «Как
собирать».

Серийники: Рафаэль `00008101-001261402669003A`, Teymur
`00008101-001A146911A3003A`. **Кабель проверять серийником**, не по
памяти: за сессию он отваливался семь раз и дважды оказывался у другого
телефона.

### Что сделано за сессию 03–04.08

| Работа плана | Состояние |
|---|---|
| 1. Индикатор присутствия | ✅ |
| 2. Модель данных под будущее | ✅ |
| 3. Содержательное предложение | ✅ доведено: полноценное окно, конфликты, форма одежды |
| 4. Подтверждение перед «Razıyam» + ожидание | ✅ |
| 5. Уведомления получателю | ✅ три штуки |
| 6. **Отмена договора по согласию** | ❌ **следующая работа** |
| 7. Остаток раздражающего | ✅ закрыт целиком |

Сверх плана: одиннадцать уведомлений об изменениях мероприятия, догон
напоминаний, общая плашка и правила конфликта, замена мероприятия,
выход из чужого.

### ПОРЯДОК НОВОЙ СЕССИИ (переписан 04.08, вечер)

Прежний порядок ниже устарел: тест форм запросов **сделан**, N32
**починен**. Актуальный порядок такой.

#### 1. Пересборка обеих трубок — первым делом

На трубках нет починки N32. Заодно решить, не добавить ли к ней N33 и
N35 (обе клиентские, обе однострочные-двустрочные) — тогда одна сборка
закроет всё. Рафаэль на кабеле ПЕРВЫМ, Teymur по Wi-Fi вторым, по одной.
**Кабель проверять серийником**, не по памяти: 04.08 доступной оказалась
не та трубка, о которой шла речь.

#### 2. Долг: три проверки N27 обеими дорогами

`#7` переключатель в диалоге, `#8` дедуп на двух аккаунтах, `#3` три
ответа диалога конфликта. **У всех трёх дорог две** — диалог и баннер
живут и в форме мероприятия (`agreements_screen.dart`), и в листе
«İş təklif et» (`job_offer_date_sheet.dart`); `resolveConflictBanner`
зовут оба. Прогонять на обоих экранах и **записывать, на каком прогнали**.
Пересборки не требуют, обе дороги есть на текущей сборке.

#### 3. Работа №6 — отмена договора по согласию

**Порядок изменён по находке 04.08: серверная часть ПЕРВОЙ.** Отзыв
запроса отмены сейчас на сервере не выражен вовсе — правила на снятие
`cancelRequestedBy`/`cancelRequestedAt` нет, клиент получит отказ.

Строить **однопутно** — только карточка договора. Гарантия «по согласию»
и так на сервере (`requestsCancel`/`confirmsCancel` написаны), а каждая
лишняя дорога — ещё одно место, где забудут.

**Разбор сделан, писать по нему:**

- **Отзыв и отказ — два разных поступка с одинаковым следом в данных.**
  Отзыв — запросивший передумал, извещать вторую сторону. Отказ — вторая
  сторона не согласна, извещать запросившего. Триггер видит `before`/
  `after`, но не видит, кто писал, поэтому поступок обязан называться в
  самой записи: `lastActionType` ∈ `cancelWithdrawn` / `cancelDeclined`,
  плюс `stampsSelf()`, и название связано с тем, кто его совершает —
  тогда подделать нельзя.
- **После подтверждения отзыв невозможен:** `status != 'cancelled'`, и
  `status` этим ходом не трогать вовсе.
- **Одновременность решают правила, не UI.** Кто первый записал, тот и
  прав: после отзыва подтверждение отклонится (`cancelRequestedBy` стал
  `null`), после подтверждения отзыв отклонится (`status == 'cancelled'`).
  **Отказ проигравшему НЕЛЬЗЯ глотать** — он обязан стать словами «вторая
  сторона уже подтвердила отмену».
- **Признак нового запроса — по `cancelRequestedAt`, а не по появлению
  поля** (класс N29 дословно).
- **Заодно закрыть N36** тем же условием `status != 'cancelled'` в
  `confirmsCancel()`.

Предложенная форма правил — две названные дороги на общем условии:

```
function clearsCancelFields() {
  return changedKeys().hasOnly(['cancelRequestedBy', 'cancelRequestedAt',
                                'lastActionBy', 'lastActionType'])
    && request.resource.data.cancelRequestedBy == null
    && request.resource.data.cancelRequestedAt == null
    && resource.data.get('cancelRequestedBy', null) != null
    && resource.data.get('status', null) != 'cancelled'
    && stampsSelf();
}
function withdrawsCancelRequest() {   // запросивший передумал
  return clearsCancelFields()
    && resource.data.get('cancelRequestedBy', null) == request.auth.uid
    && request.resource.data.lastActionType == 'cancelWithdrawn';
}
function declinesCancelRequest() {    // вторая сторона не согласна
  return clearsCancelFields()
    && resource.data.get('cancelRequestedBy', null) != request.auth.uid
    && request.resource.data.lastActionType == 'cancelDeclined';
}
```

**Порядок работ: правила → парный тест → UI.** Тест обязан покрыть обе
дороги отдельно (разрешено кому положено, отказ всем прочим) и щель N36 —
повторное подтверждение после `cancelled` должно отклоняться.

### Инструменты, написанные за сеанс

- `functions/test/query-shapes.test.ts` — форма каждого `.where(`-запроса
  против настоящих правил, парами: с доказывающим фильтром и без.
- `test/source_invariants_test.dart` — проход по исходникам; сюда же
  класть будущий проход по цепочкам `.where(`.
- `functions/src/scripts/countStuckActiveUsers.js`, `censusPresence.js` —
  переписи прода, **только чтение**.

---

### ПРЕЖНИЙ ПОРЯДОК (выполнен, оставлен для истории)

Два дела, строго в этом порядке. Второе — содержательное, первое —
подпорка под него, и потому идёт раньше.

---

#### ПЕРВОЕ. Тест форм запросов в эмуляторе

**Зачем именно сейчас, а не «когда-нибудь».** За сессию 03–04.08
`permission-denied` укусил **дважды**, оба раза по одной причине: запрос
фильтровал не по тому, чем правило чтения доказывается. Правило Firestore
разрешает выборку, только если **сами фильтры запроса** доказывают право
на КАЖДЫЙ возможный результат — сервер не читает документы, чтобы это
выяснить. Оба раза дефект вылезал на трубке, в проде, молча (кнопка
залипала). Работа №6 добавляет и новые запросы, и новые правила — тест,
сделанный ДО неё, ловит её ошибки сам.

**Что именно строить.** Файл `functions/test/query-shapes.test.ts`. На
каждый `.where(`-запрос, который клиент реально выпускает, — один случай:
поднять его форму один в один против настоящего `firestore.rules` в
эмуляторе и потребовать `assertSucceeds`. Плюс к каждому — парный
`assertFails` на форму БЕЗ доказывающего фильтра, иначе тест не отличает
разрешение от «правила вообще не проверялись».

**Откуда брать список форм:**
```
grep -rn "\.where(" lib/ --include=*.dart
```
Основной источник — `lib/firebase/firestore_service.dart`; проверить и
остальные файлы из вывода, запросы живут не только там.

**Как это запускается** (эмулятор поднимается сам, отдельная команда не
нужна, проект `demo-mugam-test` подставляется скриптом):
```
cd functions && npm test
```
Один файл:
```
cd functions && npx jest test/query-shapes.test.ts --runInBand
```

**Образец для копирования** — `functions/test/rules.test.ts`: там уже
готовы `initializeTestEnvironment` с чтением настоящего `firestore.rules`
с диска, `assertSucceeds`/`assertFails` и посев данных через
`withSecurityRulesDisabled`. Общие константы — `functions/test/helpers.ts`.

**Обязательная проверка возвратом** (правило №2 ниже): выкинуть из одной
формы доказывающий фильтр — например `where('musicians', arrayContains:
uid)` — и убедиться, что тест **падает**. Не упал — тест бесполезен,
именно так уже был пропущен дефект в прод.

**Потом, отдельным шагом:** проход по `.where(`-цепочкам в CI, чтобы
новый запрос нельзя было добавить мимо теста. Это средство сильнее
комментария, а комментарий на этом самом месте уже один раз не сработал
(правило №1 ниже).

---

#### ВТОРОЕ. Работа №6 — отмена договора по согласию

Последнее, что осталось в Этапе I. Она же оживит два уже написанных
уведомления (`Ləğv təklifi`, `Müqavilə ləğv edildi`) и сделает достижимой
ветку `cancelled`, которая нарисована в карточке и **никогда не
наступала** (в проде 0 отменённых).

Смысл: один участник просит отмену — второй подтверждает — договор
переходит в `cancelled`. Сервер уведомляет вторую сторону, инициатора —
никогда. Правила Firestore на запрос/подтверждение уже написаны
(`requestsCancel()`, `confirmsCancel()` в `firestore.rules`), тесты на них
есть в `rules.test.ts` — начинать с их перечитывания, а не с нуля.

> **Требование, записанное заранее и не потерять:** отзыв запроса отмены
> обязан очищать `cancelRequestedBy` и `cancelRequestedAt`, либо признак
> должен строиться на отметке времени. Иначе второй запрос отмены по тому
> же договору не породит уведомления — тот же дефект, что уже поймали на
> `jobOfferBy` (класс «признак построен так, будто прошлого не было»,
> реестр). Случай редкий и молчаливый, найти его будет некому.

**Как проверять — сверкой данных, не глазами.** Экран покажет результат,
но не порядок, в котором он получен, а защита живёт в порядке. Четыре из
девяти находок 04.08 глазом не видны вовсе. Наблюдатель:
```
cd functions && node lib/scripts/watchChatNegotiation.js <chatId>
```

**Попутно, пока обе трубки живы:** две оставшиеся проверки `N27` —
переключатель в диалоге конфликта и дедуп на двух аккаунтах. Обе требуют
двух телефонов сразу, поэтому делать их в тот момент, когда трубки уже
собраны, а не откладывать.

### Что ещё ждёт

| Что | Состояние |
|---|---|
| `N27` — две проверки «вдвоём»: переключатель в диалоге, дедуп на двух аккаунтах | шесть из восьми пройдены 04.08 |
| CI-проход по `.where(`-цепочкам | после теста форм запросов, см. выше |
| `N25-push` — доставка уведомлений | ждёт платного аккаунта Apple |
| `N26` — старые даты в UTC | кандидатов ноль, не срочно |
| `B18/N19/jobOffer-cleanup` | одним заходом не раньше 10.08 |
| Сумма (гонорар) в договоре | по плану — после №6 |

### Три правила, выведенных этой сессией

Они в реестре с разбором, здесь — коротко, потому что применять их надо с
первой минуты:

1. **Комментарий защищает строку, а не класс.** Предупреждение, стоявшее
   строкой выше, не остановило повторение той же ошибки: оно адресовано
   правящему эту строку, а рядом ДОПИСЫВАЛИ. Порядок средств по силе:
   тип → тест → проход по исходникам в CI → комментарий.
2. **Новый тест проверяется возвратом к прежнему поведению.** Не упал —
   тест бесполезен. Оплачено зелёным тестом, пропустившим дефект в прод.
3. **Признак не строить так, будто прошлого не было.** Проверяется
   НАЛИЧИЕ там, где нужно СОБЫТИЕ: `jobOfferBy`, `cancelRequestedBy`,
   «договор создан». Работает ровно один раз за жизнь сущности.

### И четвёртое, про метод

**Сверять экран с данными даже когда на экране всё правильно.** Четыре из
девяти сегодняшних находок невидимы глазом: поздравление опережало договор
на 610 мс, уведомление уходило не того типа, закрытие листа не писало
ничего, «6 tədbiriniz var» вместо «3». Экран показывает результат; порядок,
в котором он получен, экран не показывает вовсе — а защита живёт в порядке.

Наблюдатель за документом чата поднимается так:
```
cd functions && node lib/scripts/watchChatNegotiation.js <chatId>
```

---

## 1. Первое дело — собрать обе трубки ✅ СДЕЛАНО 03.08 вечером

**Обе трубки собраны и доказаны машинно:** Teymur по сети (компиляция
57,2 с), Рафаэль по кабелю (90,4 с + 130,3 с установка). У обеих в логе
`Flutter run key commands.` плюс строка с боевым uid —
`PresenceService.start(1IBr5oq…)` у Teymur и `MY UID: 6s4Ffvkh…` у
Рафаэля, то есть приложение не просто установилось, а вошло в свой
аккаунт.

Правило порядка остаётся: **параллельно никогда**, ставить по одной —
одновременная установка конфликтует.

### Препятствие оказалось не тем, чем записано — поправка 03.08

Прежняя редакция утверждала: «кабель Рафаэля не даёт данные,
`system_profiler SPUSBDataType` не видит ни одного iPhone на шине вовсе».
**Первая половина неверна, вторая получена негодной командой.**

`system_profiler SPUSBDataType` на этой машине (macOS 26.4) возвращает
**ноль строк всегда**, есть на шине устройства или нет: тип данных
переименован в `SPUSBHostDataType`. Из пустого вывода был сделан вывод о
пустой шине — ложный отрицательный, уведший диагностику на кабель.

Рабочие проверки:

```
system_profiler SPUSBHostDataType | grep -A8 iPhone     # даёт Serial Number
ioreg -rc IOUSBHostDevice -w0 | grep '"USB Product Name"'
xcrun devicectl device info details --device <id> | grep -iE "tunnelState|lastConnectionDate"
```

Серийник в `SPUSBHostDataType` идёт **без дефиса**
(`00008101001A146911A3003A`), в `flutter devices` — с дефисом после
восьмого символа. Сверять надо именно серийник: 03.08 на кабеле висел не
Рафаэль, а Teymur, и конспект приписывал кабель не тому телефону.

**Настоящей причиной был кабель, но диагностируется она иначе.** Признак
исправного подключения — `tunnelState: connected` и **сдвигающийся**
`lastConnectionDate`. Пока Рафаэль был недоступен, `lastConnectionDate`
стоял намертво на времени первого появления: устройство мелькнуло на шине
и больше не соединялось. Перезапуск `remotepairingd` и `CoreDeviceService`
(оба под пользователем, `kill` без sudo, launchd поднимает обратно) не
помог и помочь не мог — туннель было не к чему поднимать, к моменту
проверки телефона на шине не было вовсе. Помогло переподключение кабеля:
серийник появился, `tunnelState` стал `connected`, `lastConnectionDate`
сдвинулся, `flutter devices` увидел устройство.

> Прежде чем винить кабель, порт или доверие к компьютеру — посмотреть,
> есть ли телефон на шине **годной командой**, и сверить серийник с тем,
> который ожидается.

### Ещё одно, чего в прежней редакции не было

`flutter run` **сама поднимает Xcode.app** на фазе установки («You may be
prompted to give access to control Xcode»). То есть требование «Xcode
закрыт» относится к состоянию ДО запуска; после первой же сборки он
окажется открыт, и перед следующей его надо закрывать заново
(`osascript -e 'quit app "Xcode"'`).

Перестановка кабеля **на фазе установки** её срывает: у Teymur
компиляция прошла, а присоединиться не удалось («The mDNS query for an
attached iOS device failed»), и сборку пришлось повторять целиком. Строки
`Flutter run key commands.` в логе при этом не было — то есть установка
осталась недоказанной, и повтор был обязателен, а не перестраховкой.

### Как собирать

```
pgrep -f "Xcode.app/Contents/MacOS/Xcode"      # ДОЛЖНО быть пусто
stdbuf -oL -eL flutter run --release -d <device-id> > build_X.log 2>&1
```

#### `--release` обязателен, и вот чего стоило его отсутствие

**Прежняя редакция этого блока давала команду без флага**, то есть
отладочную сборку, — при том что все состоявшиеся прогоны на устройствах
записаны в реестре как релизные («боевая сборка, релиз» — `AUDIT_TODO.md`
строки 339, 776, 1815). Инструкция расходилась с тем, чем на самом деле
пользовались, и расхождение было невидимым: обе сборки одинаково дают
`Flutter run key commands.`, то есть **доказательство установки у них не
различается**.

Цена расхождения, заплаченная 03.08 вечером: отладочные сборки поставлены
на обе трубки, после чего проверки оказались невозможны, и обе трубки
пришлось пересобирать заново.

**Три свойства отладочной сборки, измеренные в тот вечер:**

1. **Она не запускается с иконки.** Тап по иконке даёт чёрный экран —
   наблюдалось на обеих трубках независимо. Ей нужна живая сессия.
2. **Сессия рвётся от закрытия Xcode.** У Teymur при живой сессии
   закрытие Xcode дало `Lost connection to device.` в логе, приложение
   умерло. Механизм виден в списке процессов: отладочный запуск идёт
   **через Xcode** — `flutter` держит помощника
   `xcode_debug.js --xcode-path /Applications/Xcode.app … --device-id …`.
3. **Вторую сборку не запустить, не закрыв Xcode** (см. правило ниже), а
   закрытие убивает первую трубку.

Вместе это значит: **на отладочных сборках две трубки не могут быть живы
одновременно.** А проверки «вдвоём» — пункт 3 и подсветка — требуют ровно
двух живых телефонов. Релизная сборка ни одного из трёх свойств не имеет:
AOT-приложение живёт с иконки само, отцепление и закрытие Xcode ему
безразличны.

> Собирать **только релиз**. Отладочная сборка выглядит установленной так
> же, как релизная, и расходится с ней лишь тогда, когда телефон
> понадобится вторым.

- **Xcode.app обязан быть закрыт.** При открытом Xcode запуск на Teymur
  сорвался 03.08 с `Error starting debug session in Xcode: Timed out
  waiting for CONFIGURATION_BUILD_DIR to update` — приложение не встало
  вовсе. После закрытия та же сборка прошла. Воспроизведено вечером того
  же дня на Рафаэле: правило держится, обойти его «дешёвой пробой» не
  вышло.
- **Убитая сборка оставляет сироту, который поднимает Xcode обратно.**
  После `pkill` по `flutter run` процесс
  `osascript … xcode_debug.js` остаётся жить и переоткрывает Xcode, из-за
  чего `quit app "Xcode"` выглядит не сработавшим. Снимать помощника
  первым:
  `pkill -f "xcode_debug.js"`, только потом Xcode.
- `flutter run` **сам не завершается**. Ждать его выхода бессмысленно:
  признак готовности — строка `Flutter run key commands.` в логе.
  **Отцепляться только когда телефоны больше не нужны**, а не сразу по
  появлении строки: у релизной сборки приложение переживает отцепление, у
  отладочной — нет (проверено 03.08 на обеих трубках).
- Вывод собирать **построчно в файл**, не через `| tail`: иначе лога не
  будет вовсе до конца процесса.
- `Running Xcode build...` идёт **без перевода строки** — лог не растёт,
  и это нормально. Живость сборки проверять по процессам `xcodebuild` и
  `clang`, а не по логу.
- Времена: при полной пересборке нативной части Рафаэль по кабелю 1096 с
  + 353 с; при живом кеше — 90,4 с + 130,3 с (кабель) и 57,2 с + ~4 мин
  (сеть). Разброс задаёт кеш, а не транспорт.

### Что проверять после установки

**Пункт 3, вдвоём.** Рафаэль в чате с Teymur жмёт «İş təklif et»:

1. Открывается лист — заголовок **«İş təklifi»**, кнопка **«Təklifi
   göndər»**, у «Yer» и «Qeydlər» подпись **«vacib deyil»**.
2. Закрыть свайпом не заполняя — предложение **не должно появиться ни у
   кого**. Это проверка обратимости тапа.
3. Открыть снова, что-нибудь ввести, закрыть свайпом — должен спросить
   «Dəyişikliklər itəcək».
4. Попробовать выбрать вчерашнюю дату и отправить — должно отказать
   («Keçmiş tarix seçilə bilməz»).
5. Заполнить и отправить: у Teymur сразу дата, тип, место, **заметки
   (📝)** и **золотая** «Razıyam» — серой стадии быть не должно.

**Подсветка, вдвоём.** Оба открывают один договор в «Müqavilələr»,
Teymur держит карточку открытой, Рафаэль меняет место:

- у Teymur строка «Yer» заливается золотым и через 4 с плавно гаснет;
- **у самого Рафаэля после его же сохранения не загорается ничего** —
  это вторая половина проверки, не менее важная первой.

**Ни одна из этих двух проверок вживую пока не пройдена** — сборки
встали, но тестер до них не дошёл: увидел на первом же шаге, что лист
урезанный, и работа ушла туда (см. раздел «Вечер 03.08»). После установки
новой сборки проходить их заново, вместе с проверками листа ниже.

---

## 1б. Вечер 03.08 — лист предложения переделан в полноценное окно

**Что было.** Тап по «İş təklif et» открывал лист по высоте содержимого:
без формы одежды, без заголовков разделов, без закреплённых шапки и
кнопок. Рядом стоял комментарий «намеренно не построен на
`_EventFormModal`», и из-за него отличия выглядели решением — хотя
обоснование покрывало две вещи из десяти. Остальное разошлось недосмотром.

**Коммит `8f988c0`.** Что сделано:

| Что | Где |
|---|---|
| Разметка по образцу календарного окна: во весь экран, закреплённые шапка (заголовок, типы, дата золотом) и кнопки, скроллящаяся середина | `job_offer_date_sheet.dart` |
| Форма одежды + склейка/разбор заметок — **общий виджет**, не копия | `shared/widgets/event_notes_picker.dart` |
| Проверка конфликта со своим календарём + **тот же** диалог, что в календаре | `shared/widgets/event_conflict_dialog.dart` |
| Форматирование даты события | `core/time/az_date_format.dart` |
| Публичный маршрут к экрану конфликтующего мероприятия | `agreements_screen.dart` → `agreementConflictEventRoute` |

**Решения владельца, принятые в этой работе** (не переигрывать без
причины):

1. **Место остаётся необязательным** («vacib deyil»). Предложение часто
   уходит раньше, чем известна площадка; требование заполнить заставляет
   выдумывать. В календаре обязательность верна — туда пишут уже
   состоявшуюся договорённость.
2. **Проверка конфликта сделана сразу, не отложена.** Причина: музыкант
   предлагает дату, на которой у него уже стоит мероприятие, вторая
   сторона соглашается — и он в двух местах разом, а выясняется это в день
   события. Выпускать главную фичу с дырой в центре нельзя.
3. **Форма одежды — общий виджет, а не копия.** Заметки лежат в базе одной
   строкой через `', '`; договор создаётся ИЗ предложения, а правится в
   календаре. Разойдись список пунктов хоть на символ — выбранное молча
   уехало бы в свободный текст, без единой ошибки.
4. **Участники не переносятся** — в чате один на один стороны заданы
   самим чатом.

**Правило, записанное в шапке файла:** расхождение листа с календарным
окном либо перечислено там с причиной, либо считается ошибкой. Прежний
комментарий был неполон, и из-за слова «намеренно» потерянные отличия
выглядели решением.

**Проверено машинно:** `flutter analyze` — 36 замечаний, ровно давняя база
N8, ни одного нового; `flutter test` — 95 тестов, включая 9 новых на
сходимость склейки и разбора заметок (`test/event_notes_test.dart`).
**На устройстве не проверено ничего.**

### Что проверять в новом листе (после установки)

1. Лист открывается **во весь экран**; заголовок и кнопки не уезжают при
   прокрутке.
2. Есть раздел **GEYİM** с пятью пунктами и «Digər…», и **ƏLAVƏ QEYDLƏR**.
3. Выбрать пункт формы одежды, отправить, принять предложение — открыть
   получившийся договор в «Müqavilələr» и убедиться, что **пункт стоит
   галочкой**, а не съехал в свободный текст. Это главная проверка общего
   виджета.
4. Выбрать дату и время, на которые у инициатора **уже есть мероприятие**
   — под колесом должно появиться предупреждение **сразу при выборе**, а
   по «Təklifi göndər» — диалог «⚠️ Bu tarixdə tədbir var» с тремя
   ответами: «Bax» (открывает то мероприятие и возвращает к вопросу),
   «Əvəz et» (отправляет как есть), «Yeni tədbir» (запрещает отправку,
   пока время не сдвинут).
5. «Ləğv et» на тронутом листе спрашивает «Dəyişikliklər itəcək», как и
   свайп.

## 2. Что сделано за сессию 03.08 (день)

| Коммит | Суть |
|---|---|
| `5cc2814` | включено удаление мёртвых push-токенов (`PUSH_TOKEN_SWEEP_DELETE=true` + деплой), три оговорки в реестре |
| `4eda1f0` | посчитана цена дыры B11-source вместо оценки на глаз |
| `aab541e` | запрет на сужение окна сборщика сирот + задача N24 о его молчаливой смерти |
| `38c38c1` | N23 закрыт, прогон на двух устройствах записан |
| `bfca82e` | **починка N23** — карточки смотрят в поток по id, а не держат копию |
| `c78418d` | **пункт 3 плана** — содержательное предложение, одной операцией |
| `64600f2` | **подсветка чужой правки** в карточке |

**Логи ночных прогонов разобраны.** Сборщик сирот 04:20 с включённым
удалением снёс 2 объекта (421 КБ) — проверено отдельным обходом всей базы,
ссылок на них не осталось ни одной. Проверка живости токенов 04:35 в
режиме отчёта сошлась с ручным скриптом точно (2 документа, 2 токена,
0 мёртвых), удаление включено.

**Найдено по ходу:** источник сирот (B11) **не закрыт** — ни
`deleteMessagePermanently`, ни `onMessageDeleted` не трогают Storage.
Сборщик работает подметалкой за открытой дырой, и это осознанно:
наивная правка снесла бы медиа у всех, кому сообщение переслали
(`forwardMessage` переиспользует URL). Заведено как `B11-source`.

## 2б. Что сделано вечером 03.08

| Коммит | Суть |
|---|---|
| `b6dd142` | в план записан импорт мероприятий из фото — отдельная фича после договоров |
| `aa2deb3` | поправлена ложная диагностика кабеля в этом конспекте |
| `8f988c0` | **лист предложения работы переделан в полноценное окно** (разбор выше) |
| `85afed8`, `438d5a5`, `ce2188b` | конспект: вечерняя работа, порядок сборки, состояние трубок |
| `e4fd183` | **конфликт по дню, а не только по минуте; предупреждение переделано** |

### `e4fd183` — что именно и почему

Замечание владельца прямо с прогона: **в календаре человек видит дату
целиком и понимает, занята она или нет, а в листе видит только колесо** —
и о том, что на этот день у него уже что-то есть, не узнаёт, пока не
совпадёт минута в минуту. А совпадения минут в жизни почти не бывает:
мешает не одинаковое время, а второй тədbir в тот же день.

Предупреждение и диалог теперь поднимаются на **четырёх** случаях, и
порядок между ними не произволен — запреты старше находок, точное
совпадение старше занятого дня (оно говорит больше):

1. запрет на минуту («я сам попросил другое время»);
2. запрет на день («я сам попросил другой день»);
3. точное совпадение времени;
4. занятый день.

**Запрет от «Yeni tədbir» ставится ровно на то, о чём спрашивали:**
совпала минута — занята минута, занят день — занят день, и снимается
каждый своим действием (сдвигом времени / сменой дня). Иначе ответ
«выберу другое» снимался бы действием, которого человек не делал.

Диалог — тот же календарный, второго не заводили.

Само предупреждение переделано по просьбе владельца («читабельнее и
красивее»): две строки вместо одной (заголовок «что не так», подпись «чем
именно занято»), значок вынесен в свой кружок слева — эмодзи внутри строки
уезжал вместе с переносом и терял смысл отметки на полях. При нескольких
мероприятиях в день перечисляются все, по возрастанию времени.

**Идея владельца, записанная в план** (`docs/job-offer-plan.md`, раздел
«Импорт мероприятий из фото»): музыкант присылает фото своего списка
(скриншот календаря телефона, страница ежедневника), приложение
распознаёт даты и названия, показывает разобранное на подтверждение,
человек правит и создаёт мероприятия. **Фича переходного периода** —
нужна один-два раза при переезде в приложение, поэтому стоимость
распознавания мизерная, а приоритет ниже договоров. Корень проблемы
(детали теряются в голосовых) закрывают сами договоры; импорт решает
только перенос накопленного. Четыре вопроса записаны открытыми.

## 3. Состояние на момент остановки

```
main, последний коммит 8f988c0, всё запушено, не запушено 0
в дереве только давний шум ios/ (N6) и неотслеживаемые assets — не коммитим

Трубки: ОБЕ на 8f988c0, доказано строкой Flutter run key commands
        (Teymur по Wi-Fi, Рафаэль по кабелю, компиляция 94,4 с)
PUSH_TOKEN_SWEEP_DELETE=true задеплоен, проверен в живой ревизии
```

**Первое дело новой сессии — проверки на устройствах. Сборка стоит, но
вживую не проверено НИЧЕГО.** Порядок от нового к известному:

1. Лист открывается во весь экран, разделы MƏKAN / GEYİM / ƏLAVƏ QEYDLƏR
   на месте, шапка и кнопки не уезжают при прокрутке.
2. **Форма одежды насквозь** — выбрать пункт, отправить, принять второй
   стороной, открыть договор в «Müqavilələr» и убедиться, что пункт стоит
   **галочкой**, а не съехал в свободный текст. Главная проверка общего
   виджета: разойдись он, ошибки не возникнет нигде, галочка просто молча
   станет текстом.
3. **Конфликт** — выбрать время, на которое у инициатора уже есть
   мероприятие (у Рафаэля договор `G3sJ…` на 08.08): предупреждение сразу
   под колесом, диалог с тремя ответами по «Təklifi göndər».
4. Затем пять шагов пункта 3 и подсветка (раздел 1) — до них 03.08 так и
   не дошли.

### Про кабель — читать перед сборкой

**Порядок сборки задан владельцем и не обсуждается: сначала Рафаэль,
потом Teymur — всегда.** Причина: на кабеле именно Рафаэль, и кабель
может отвалиться в любой момент (за вечер отвалился дважды сам по себе,
без участия человека). Teymur по Wi-Fi собирается нормально, значит
хрупкое кабельное окно надо тратить на того, кому оно нужно. Соберёшь
Teymur первым — рискуешь обнаружить, что Рафаэля к его очереди уже нет.

Кабель за вечер трижды переставляли между телефонами, и дважды это
срывало установку. Остальные правила, выведенные на этом:

- **на шине живёт ровно один телефон**, второй в это время только по сети;
- **кто именно на кабеле — проверять серийником**, не по памяти:
  `system_profiler SPUSBHostDataType | grep "Serial Number"`;
- **во время установки кабель не трогать вовсе**: перестановка на этой
  фазе роняет её без строки `Flutter run key commands.` в логе, то есть
  установка остаётся недоказанной и повтор обязателен;
- **устройство отваливается само** — за вечер Рафаэль уходил с шины
  дважды без участия человека. Признак живого подключения:
  `tunnelState: connected` и **сдвигающийся** `lastConnectionDate` в
  `xcrun devicectl device info details --device <id>`.

**Завтра в 04:35 — первый прогон удаления токенов с включённым флагом.**
Посмотреть запись в `maintenance/pushTokenSweep/runs`: поля `deleted` и
`deadPaths`. Скорее всего удалит ноль — мёртвых токенов нет ни одного.

**Три переходных окна снимаются одним заходом, не раньше 10.08:**
`B18-cleanup`, `N19-cleanup`, `jobOffer-cleanup`. У каждого условие
проверяемое, у последнего — «ни одного чата, где `jobOfferBy` есть, а
`eventDate` нет» (на 03.08 таких 0).

## 4. Живые опоры

```
чат Teymur↔Rafael  1IBr5oqcHCUZRpkpE4lGyPAKBNn2_6s4Ffvkh8zQJDnxFakJJMfpmqLG3
Teymur             1IBr5oqcHCUZRpkpE4lGyPAKBNn2   (iPhone по сети,   00008101-001A146911A3003A)
Rafael             6s4Ffvkh8zQJDnxFakJJMfpmqLG3   (iPhone по кабелю, 00008101-001261402669003A)

договор Рафаэля    G3sJAyIRUe6GnuD3dJC6   08.08, Toy — на нём проверяли N23
договор Teymur     JvT4xlLxkxPy5NYYjfxx   16.08, Toy
```

**Сторож записей в документ чата** (постоянный, не снимать):

```
gcloud logging read 'resource.type="cloud_run_revision" AND
  resource.labels.service_name="onchatupdated" AND textPayload:"chat-write"' \
  --project=mugam-club --limit=40 --freshness=15m \
  --format="value(timestamp,textPayload)" | sort
```

**Наблюдатели** (в scratchpad сессии, переписать при новой — они
короткие): за полями договоров с печатью только изменений; за полями
раунда; за присутствием с подсчётом записей по минутам.

## 5. Решения, ожидающие владельца

1. **Периодический пересчёт присутствия на семи экранах** — список чатов,
   друзья, заявки, группы, добавление в группу, главный, профиль. Уход
   человека там записи не порождает, зелёная точка не гаснет.
2. **Уточнение «tətbiqdədir»** — человек может быть в другом чате,
   различимо по `activeChatId`. Лестница пройдена, вопрос созрел.
3. **Отметка «увидел предложение»** — разбор в плане, «Открытый вопрос».
4. **Сумма (гонорар) в договоре** — после пунктов 1–7 плана.

## 5б. Класс «утверждение об инструменте, не проверенное на инструменте»

Назван вечером 03.08, когда третья поправка подряд оказалась той же
формы. Записан отдельно, потому что это не три случайности, а один
повторяющийся способ ошибаться — и он дороже ошибок в коде.

**Форма.** Утверждение говорит не о поведении приложения, а о том, **чем
и как за приложением можно наблюдать**. Оно попадает в конспект без
проверки самого инструмента — и дальше молча определяет, каким способом
вообще ищут ответы.

| # | Утверждение | Чем оказалось | Что оно стоило |
|---|---|---|---|
| 1 | «`system_profiler SPUSBDataType` не видит ни одного iPhone на шине» | команда возвращает ноль строк **всегда** на этой macOS: тип переименован в `SPUSBHostDataType`. Ложный отрицательный | диагностику увело на кабель, чинили не то |
| 2 | «`debugPrint` не виден, поэтому единственный способ — вывод на экран» | посылка верна для **релиза**, вывод неверен вообще: при отладочной сборке с прицепленной сессией логи и стеки видны | несколько прогонов диагностировали вслепую при доступных логах |
| 3 | «строка `Flutter run key commands.` доказывает готовность трубки» | доказывает только «сборка встала». Релиз и debug дают её **одинаково**, а разница между ними решает, возможна ли проверка вообще | вечер 03.08: обе трубки числились готовыми и готовыми не были, пересборка целиком |

**Общее у всех трёх:** проверялся результат инструмента, а не сам
инструмент. Пустой вывод команды приняли за пустую шину; отсутствие
логов — за свойство устройств; появление строки — за готовность к
проверке. Во всех трёх случаях ошибка **не выглядит ошибкой**: инструмент
молчит ровно так же, как когда всё в порядке.

**Признак для поиска следующего:** утверждение вида «здесь нельзя
посмотреть X» или «X доказывается тем-то». Спрашивать не «верно ли это»,
а два других вопроса:

1. **чем проверено, что инструмент вообще работает** — что он показывает,
   когда искомое заведомо есть;
2. **чего этот признак не различает** — какие два разных состояния дают
   один и тот же вывод.

Второй вопрос и вскрыл случай 3: строка-доказательство честно отличает
«встало» от «не встало» и не отличает **ничего больше**.

**Кандидаты, оставшиеся непроверенными** (не находки, а места, где
утверждение об условиях наблюдения сделано рассуждением и на нём стоит
решение НЕ проверять):

- `AUDIT_TODO.md:1884` — «ступень „⏳ çoxdan görünmür“ на устройствах
  недостижима (нужны сутки без заходов)». Выведено из порога, не
  замерено. Достижима как минимум двумя путями — сутки не трогать трубку
  либо третий аккаунт, — то есть «недостижима» сильнее, чем есть.
- `AUDIT_TODO.md:130` — «состояние „Söhbət yüklənmədi“ из списка чатов
  недостижимо». Опирается на рассуждение о том, что офлайн-кэш всегда
  отдаёт документ чата; искусственная проверка ставилась на другую
  половину утверждения (достижимость через пуш).

Оба помечены здесь, а не переписаны: они могут быть верны. Отмечено
именно то, что **проверены они не были**, а решение не проверять принято
на них.

## 6. Правила, выведенные за эту сессию

- **Отчёт механизма и проверка механизма должны быть разными обходами.**
  Сборщик сирот сам себе сказал «удалил двоих законно»; поверить этому
  можно было только отдельным проходом по всей базе, ищущим ссылки на
  удалённое. Иначе проверяется самосогласованность, а не факт.
- **Комментарий у числа держит запрет крепче, чем реестр.** У
  `DEFAULT_MIN_AGE_MS` прежний текст сам приглашал сузить окно
  («реальный разрыв — секунды»), и правило снялось бы при первом чтении
  кода. Причина должна лежать рядом с числом.
- **Локальная запись возвращается в поток раньше, чем закроется форма.**
  Firestore отдаёт её из кеша немедленно. Всё, что различает «своё» и
  «чужое», должно объявляться ДО записи, а не после.
- **Ключ по подписи на экране ломается на втором экране.** Подписи строк
  у двух карточек разные; ключевать надо по имени поля.
- **Проверка «на глаз» не отличает старую сборку от новой.** Список
  процессов на телефоне одинаков. Доказательство — либо поведение,
  которого старая сборка не умеет, либо строка `Flutter run key commands`
  в логе.
  > **Поправка 03.08, вечер: эта строка доказывает меньше, чем ей
  > приписывали.** Она означает «сборка встала», и только. Она **не**
  > отличает релиз от debug — обе дают её одинаково, — а от этого
  > различия зависит, можно ли вообще провести проверку (см. блок «Как
  > собирать»). Весь вечер обе трубки числились готовыми по этой строке
  > и готовыми не были. Признак, который называют доказательством,
  > обязан быть проверен и на то, чего он **не** различает.
- **Утверждение об условиях наблюдения дороже утверждения о поле.**
  Первое определяет не ответ, а способ его искать, и ошибается молча:
  отсутствие логов выглядит свойством устройств, а не следствием флага
  сборки. Разбор — в реестре, врезка к «Условиям наблюдения» (A5).
  Отсюда правило: **запись вида «здесь нельзя посмотреть X» проверять
  так же, как замер** — называть, при каких условиях она получена, и
  чем именно проверялась.
