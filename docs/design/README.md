# Макеты — источник, а не переписка

Три макета от 05.08, положены в репозиторий 07.08. До этого они жили
только в переписке, и пункт 5 `docs/plan.md` (главный экран) чуть не был написан без
них — **пятый случай N63**, и на этот раз решение потерял владелец, а не
я. Поэтому они здесь: файл в репозитории спорит текстом, память — нет.

| Файл | Что в нём | К каким работам |
|---|---|---|
| `mugam-1-esas.html` | дневной экран; карточка мероприятия с участниками; входящее предложение работы | пункт 5; **пункт 14** (прежняя работа 8, см. ниже); лист предложения |
| `mugam-2-addim.png` | форма правки; «Məni azad et» с причинами; история мероприятия | **7а**; **8**; **13** |
| `mugam-3-cavab.html` | «Razılaşdınız»; разбор просьбы отпустить; отмена мероприятия с причиной | прежняя работа 5 — не переведена (см. ниже); **8**; **9** |

**Номера в последнем столбце — пункты `docs/plan.md`.** Прежний порядок
работ удалён 07.08, и перевод сделан не механически, а назван вслух:

| Было | Стало | Кто назначил |
|---|---|---|
| работа 6 — главный экран | **пункт 5** | перенос 07.08 |
| работа 7 — слить две формы | **7а** | перенос 07.08 |
| работа 8 — разобрать `agreements_screen` на маршруты | **пункт 14** | владелец 07.08: переезд договоров всё равно разбирает этот файл, отдельный разбор значил бы трогать 3977 строк дважды |
| работа 9 — четыре разные вещи одной строкой | **8** («Отпусти меня»), **11** (голос у мероприятия), **9** (отмена вместо удаления), **13** (история) | владелец 07.08: одним номером не переводится |
| работа 5 — уборка | **не назначена** | содержимое строки («Razılaşdınız», разбор просьбы отпустить) с уборкой не сходится вовсе; похоже на описку в исходной таблице. Назначать наугад нельзя |

**Кто переводит — называет соответствие вслух, а не угадывает по смыслу
строки.** Одна строка осталась непереведённой именно поэтому.

Второй — картинка, а не HTML (3,3 МБ). Это единственный его вид.

## Правила стиля, названные владельцем 07.08

- фон `#0D0D0D`, карточки `#1A1A1A`, скругление 14, **без теней и
  градиентов**;
- акцент `#E09A2B` (текст и обводки), заливка кнопки `#C1811F`;
- текст `#F5F3ED`, вторичный `#A8A8A2`, приглушённый `#5F5F5C`;
- заголовки разделов — мелко, с разрядкой: `SABAH`, `BU HƏFTƏ`;
- важное — вертикальная золотая полоска слева (4px), **не заливка**;
  неважное — серая полоска `#3A3A38`;
- **одна залитая кнопка на экран**, остальные обводкой.

### Что в этих правилах не сходится с самими файлами

1. **Третий серый.** В правилах названы два (`#A8A8A2`, `#5F5F5C`), а в
   макетах больше всего работает **`#8A8A85`** — им набраны заголовки
   разделов, третьи строки карточек и подписи. Без него разница между
   «вторичным» и «приглушённым» становится вдвое грубее.
2. **Фон против карточки.** По словам — фон `#0D0D0D`, карточки
   `#1A1A1A`. В файлах `#1A1A1A` — это **поверхность самого экрана**
   (прямоугольник телефона), а карточек с заливкой нет ни одной:
   важное отмечено только полоской слева, ровно как и записано отдельным
   правилом. `#0D0D0D` в файлах — фон СТРАНИЦЫ вокруг телефонов, то
   есть стол, а не экран.

**Оба расхождения ОТКРЫТЫ — решения по ним нет.** Названы владельцу
07.08 до кода, ответа пока не было. Записаны здесь именно поэтому: чтобы
следующий не выбрал молча одну из двух сторон и не выдал свой выбор за
утверждённый.
