# Что выложено в прод и когда

Одно место на весь проект. До 06.08 такого места не было: состояние прода
выводилось из коммитов и из памяти сессии — то есть из источников, которые
про прод не знают ничего. Коммит доказывает, что код написан, и не
доказывает, что он выложен; между этими двумя вещами у нас уже жила
разница в двое суток.

**Правило: выложил — дописал строку сюда, в тот же заход.** Строка
называет дату по UTC, что именно выложено и **чем это проверено на самом
проде**, а не «я помню, что деплоил».

Проверять состояние прода — не читать эту страницу, а снимать заново
(правило I6, часть 3: запись, названная по живым данным, обязана называть,
чем её снять). Команды ниже только читают.

```bash
# функции: имя и время последней выкладки каждой
gcloud functions list --project=mugam-club --format="value(name,updateTime)" | sort -k2

# переменные окружения конкретной функции
gcloud functions describe <имя> --project=mugam-club --region=europe-west3 \
  --format="value(serviceConfig.environmentVariables)"

# какой набор правил сейчас боевой (firestore и storage)
TOKEN=$(gcloud auth print-access-token)
curl -s -H "Authorization: Bearer $TOKEN" -H "X-Goog-User-Project: mugam-club" \
  "https://firebaserules.googleapis.com/v1/projects/mugam-club/releases"

# сам текст боевых правил — по id набора из ответа выше, дальше diff с файлом
curl -s -H "Authorization: Bearer $TOKEN" -H "X-Goog-User-Project: mugam-club" \
  "https://firebaserules.googleapis.com/v1/projects/mugam-club/rulesets/<id>"

# индексы
gcloud firestore indexes composite list --project=mugam-club
```

> **Ловушка REST-запроса к правилам:** без заголовка `X-Goog-User-Project`
> он отвечает `403 SERVICE_DISABLED` — «API требует quota project». Это
> сообщение читается как «доступа нет» и уводит к выводу, что боевые
> правила посмотреть нельзя. Их посмотреть можно; не хватало заголовка.
> Класс «утверждение об инструменте, не проверенное на инструменте»
> (конспект, раздел 5б).

---

## ФУНКЦИИ ВЫЛОЖЕНЫ 25.08, 22:06:09–22:08:33 UTC — накопленное за 16 суток

**Коммит: `dc7e94e`.** Прод до этого стоял на **`2a014fa`** (09.08) — то есть
шестнадцать суток исходники и прод расходились, и никто этого не знал, пока
не спросили перед новой работой.

**Выкладка сделана ОТДЕЛЬНО и раньше триггера отзыва — решение владельца,
довод I48:** у необратимого шага должна быть одна переменная, и сегодня она
одна — **работает ли дорога выкладки после аварии 09.08**. Понеси этот шаг
заодно новый триггер, при поломке было бы неизвестно, что сломало.

### Что уехало

Два файла, три коммита. Остальное в `functions/` — скрипты переписи и тесты,
в прод они не едут.

| файл | строк |
|---|---|
| `functions/src/index.ts` | **+39 / −1** |
| `functions/src/eventNotifications.ts` | **+48 / −0** |

- **`d6a0e74`** «Шаг 1: answers рядом с musicians» — `onChatUpdated`, рождая
  договор, дописывает в мероприятие `answers: {обе стороны → going}` и
  `answersWrittenByOwner: true`. **На сервере это не читает никто.** Сломайся
  — новые договоры получат неверную карту ответов, старые не тронуты;
- **`ed3c384`** «Шаг 3: напоминания» — **единственная правка, меняющая
  ПОВЕДЕНИЕ**: почасовое напоминание идёт через `remindableOf` вместо
  `recipientsOf` и молчит сказавшему «не могу». Сегодня в проде не меняет
  ничего (все ответы `going`, у кого поля нет — три запасных пути дают
  «идёт»). Сломайся — часть людей перестанет получать напоминания, **тихо**;
- **`4602909`** «N115» — `toEventSnapshot` перестал приводить отсутствующее
  `answers` к `null`: «поля нет» и «пустая карта» обязаны доехать разными
  (I47). Читатель один — `remindableOf`.

### «Было» — прогон до выкладки

**340 тестов из 340, 18 наборов из 18, ноль неуспешных**, код возврата 0,
176,6 с. Команда — `npm run test` из `functions/`, то есть
`emulators:exec --only firestore,functions,storage` вокруг `jest --runInBand`.

**Состав сверен со списком на диске, а не взят числом** (I13):
`ls functions/test/*.test.ts` → **18**; девятнадцатый файл — `helpers.ts`, не
тест. Число совпало с записанным 22.08 выше, то есть покрытие не сдвинулось.

### Что стало — снято с ПРОДА, а не из лога выкладки

| Что проверено | Чем | Ответ |
|---|---|---|
| код возврата | `firebase deploy --only functions` | **0**, `Deploy complete!` |
| сколько обновлено | лог выкладки | **31** `Successful update operation` |
| **сколько в проде** | `gcloud functions list` | **31, все `ACTIVE`** |
| **когда обновлены** | то же | **у всех 26.08** — ни одной с прежней отметкой, значит не осталось необновлённых |
| окна без триггера | лог выкладки | **`updating` × 31, `creating` × 0** |

**Различие двух последних строк — не педантизм.** Лог выкладки говорит, что
инструмент сделал; `gcloud functions list` — что в проде лежит. 09.08 они
разошлись: инструмент отказал, а отметки в проде выглядели свежими.

**Окна без триггера не было, и это предсказано до команды:**
`git diff 2a014fa HEAD -- functions/src/index.ts` не содержит ни одной
изменённой строки с `export const`, `onDocument`, `onSchedule`, `region` или
`document:` — меняются только тела. Лог подтвердил делом: везде `updating`.

**Расписание почасового не тронуто** (`every 1 hours`, `ENABLED`), но **точка
отсчёта сдвинулась** — было :28, стало :08. Это свойство относительного
расписания при выкладке, а не правка.

### Тихая правка проверена журналом

Напоминания — единственное место, где поломка была бы **не видна на трубке**:
почасовое срабатывает по часам, а неприход уведомления неотличим от «ещё не
время».

| | |
|---|---|
| последний запуск **до** выкладки | 25.08, **21:28 UTC** |
| запуски **после** выкладки | 25.08 **23:08** и 26.08 **00:08 UTC** — два |
| отказы за сутки | **ноль** (`severity>=WARNING` по этой функции — пусто) |
| что решил запуск 23:08 | «повтор доставки, пропущено» ×4 — то есть дошёл до рассылки и был остановлен замком `claimNotificationOnce` |

Снимается заново:

```bash
gcloud logging read 'resource.labels.service_name="remindupcomingeventshourly"' \
  --limit 30 --project=mugam-club --freshness=1d --format="value(timestamp,severity)"
```

### Чем откатывается

Прод-версия — **`2a014fa`**. Вернуть два файла на неё, пересобрать, выложить
снова:

```
functions/src/index.ts, functions/src/eventNotifications.ts  →  2a014fa
cd functions && npm run build && firebase deploy --only functions
```

**Возврат делается ПРАВКОЙ ФАЙЛОВ, а не `git checkout` по путям** — эту
команду запрещает сторож проекта (I10), и запрет стоит не зря: рядом всегда
лежит несохранённое. Если откат понадобится, файлы возвращаются руками либо
человеком.

### Что эта выкладка доказала помимо своего содержимого

**Дорога выкладки после аварии 09.08 работает.** До сегодня последняя
записанная попытка (09.08) отказала на Secret Manager из-за оплаты; счёт
оплачен с 14.08, но сама дорога с тех пор **не проверялась ни разу**. Первый
деплой и был этой проверкой — он прошёл.

### Чего эта выкладка НЕ проверила (I50)

- **канарейки на трубках** — сообщение и push, счётчик непрочитанного,
  звонок, уход из чужого вечера. На момент записи владелец их не прогонял;
- **что `answers` пишутся верно** — читателя у них на сервере нет, увидеть
  результат можно только в базе;
- **что `remindableOf` фильтрует правильно** — сегодня все ответы `going`,
  и фильтр инертен по построению. Проверится первым же `cant`.

## Выложено 14.08, 01:32:44 UTC — правила Firestore, детали у каждого дня свои

> **ЗАПИСЬ ВОССТАНОВЛЕНА ЗАДНИМ ЧИСЛОМ 19.08, а не сделана в день
> выкладки.** Пять суток эта выкладка в `docs/deployed.md` отсутствовала
> вовсе: её замер жил **только в сообщении коммита `71d2ae9`** — то есть
> был сохранён и потерян одновременно (I35). Сообщения коммитов не
> перечитывают, а человек открывает эту страницу; всё это время страница
> утверждала, что боевой набор — `6fceb8b2` от 13.08.
>
> **Числа ниже сняты заново 19.08**, а не переписаны из того сообщения.
> Совпали с ним, но проверяемы независимо: командами из шапки этого файла.

## ВЫКЛАДКА 22.08 — ДВА ВЕРХНИХ ПРЕДЕЛА, БОЕВОЙ НАБОР СМЕНИЛСЯ

**Набор `a7968515-56c1-4738-a2cc-eb47e84f8bd0`**, `updateTime`
**`2026-08-22T02:00:48.430018Z`**, снят с `/releases` сразу после выкладки,
а не взят из вывода `firebase deploy`. Команда — `firebase deploy --only
firestore:rules`, код возврата 0.

**ЧТО УШЛО В ПРОД — РОВНО ДВЕ ИСПОЛНЯЕМЫЕ СТРОКИ, дословно:**

```
&& request.resource.data.dates.size() <= 31
&& request.resource.data.eventType.size() <= 24
```

Обе — **добавления**; из прода не пропало ничего. В боевом тексте они
стоят на строках **753** и **770** (`grep -n "dates.size()\|eventType.size()"`
по снятому проду 22.08). Прочих прав правка не касается: оба предела — в
ветви `allow create` подколлекции `offers`, `allow update` не тронут.

**Зачем именно сейчас:** пределы защищают создание вечеров пачкой (шаг 3),
и первый прогон этой необратимой записи обязан идти при тех правилах, что
будут в проде. `dates.size() <= 31` даёт не больше 32 записей в `WriteBatch`
при пределе Firestore в 500 — предел взят извне и нами не мерен (I6).

### Сверка — снята с прода 22.08, сразу после выкладки

| проверка | число |
|---|---|
| код возврата | **0** |
| канарейка (всего расходится строк) | **0** |
| из них без `//` | **0** |
| строк в проде | **1359** |
| строк в файле `firestore.rules` | **1359** |
| **канарейка к сверке**: тот же `diff` против ПРЕЖНЕЙ боевой редакции (`2a8410bb`) | **37 строк, из них без `//` — 2**, и обе названы поимённо |
| пределов в прежней редакции | **0** |

**Канарейка к сверке обязательна (I31):** «расхождений ноль» и «сравнивать
нечем» — один и тот же вывод. Соседка дала 37/2 и назвала обе строки — те
самые, что выкладывались.

### Точка отката — набор `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe`

Прежняя боевая редакция, `updateTime` **`2026-08-14T01:32:44.053832Z`**,
1328 строк. Вернуть её можно **переводом выпуска на существующий набор**, не
пересобирая его:

```bash
TOKEN=$(gcloud auth print-access-token)
curl -X PATCH -H "Authorization: Bearer $TOKEN" \
  -H "X-Goog-User-Project: mugam-club" -H "Content-Type: application/json" \
  "https://firebaserules.googleapis.com/v1/projects/mugam-club/releases/cloud.firestore" \
  -d '{"release":{"name":"projects/mugam-club/releases/cloud.firestore",
       "rulesetName":"projects/mugam-club/rulesets/2a8410bb-d0b8-4609-ba6e-d9d7b05300fe"}}'
```

> **ОГОВОРКА, И ОНА СУЩЕСТВЕННАЯ (I50): ЭТА КОМАНДА В ПРОЕКТЕ НЕ
> ЗАПУСКАЛАСЬ НИ РАЗУ.** `grep` по этому файлу на «откат»/«rollback» до
> 22.08 давал **ноль**. То есть «одной командой» — это то, что обещает API,
> а не то, что мы наблюдали. **Запасной путь, проверенный тем, что это та
> же команда, что и выкладка:** снять текст прежнего набора командой из
> шапки этого файла, положить на место `firestore.rules` и выложить обычным
> `firebase deploy --only firestore:rules`. Содержимое вернётся байт в байт,
> id набора будет новый.

### Парный тест на эти два предела ЕСТЬ — заведён до выкладки

`functions/test/job-offer-rules.test.ts`, блок «верхние пределы на
создание»: **7 проверок, из них 2 возвратные.** Границы с обеих сторон (31
проходит / 32 отбивается, 24 знака проходят / 25 отбиваются), канарейка
«обычное предложение — 3 дня, «Toy» — проходит», и две проверки, что на
правилах БЕЗ этих строк те же записи проходят — то есть отбивает именно
предел.

Прогон 22.08: `job-offer-rules` — **34 из 34** (27 прежних + 7 новых);
полный прогон functions через `emulators:exec` — **340 из 340, 18 наборов
из 18**. Проверка возвратом отдельным прогоном: предсказаны поимённо «32 дня
— отбивается» и «тип работы в 25 знаков — отбивается», упали ровно эти два.

**Чего этот набор НЕ сторожит:** правило ответа музыканта
(`firestore.rules`, ветвь `!isInitiator()`) — по-прежнему **N152**, парного
теста у него нет.

---

**Набор `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe`** — прежний боевой, ниже
описана его выкладка (14.08). До 22.08 он был текущим; сейчас это точка
отката. Прежний перед ним — `6fceb8b2-eb38-43ea-ae8e-86b9fad6d7d8` от
13.08, 23:18 UTC. `updateTime` набора —
**`2026-08-14T01:32:44.053832Z`**, снят с `/releases` 19.08.

**ЧТО ИЗМЕНИЛОСЬ В ПРАВАХ — одной строкой:** в создании предложения работы
три общих поля `eventTime`/`eventLocation`/`eventNotes` заменены одним
`details`. Время, место и одежда стали **своими у каждого дня**, а не
одними на все вечера предложения. Ни одно другое право не тронуто.

### Сверка — снята с прода 19.08

| проверка | число |
|---|---|
| строк в проде | **1328** |
| строк в файле `firestore.rules` | **1328** |
| `diff` прод против файла | **0 различий**, код возврата 0 |
| **канарейка**: прод против редакции ДО `71d2ae9` | **1 расхождение** (2 строки → 1), код возврата 1 |
| удалено из прежней редакции | **2 строки**, взамен **1** |

**Канарейка здесь обязательна, а не для порядка (I31).** Пустой `diff` и
«сравнивать нечем» — один и тот же вывод, и без соседки первая строка
таблицы означала бы либо «сошлось», либо «инструмент слеп». Соседка
показала ровно ту правку, которую ждали:

```
<             'eventType', 'eventTime', 'eventLocation', 'eventNotes',
<             'answers',
---
>             'eventType', 'details', 'answers',
```

**Проверка по существу, а не по числу** (I13): в боевом тексте
`match /offers/{offerId}` — 1, `'details'` — 1 (строка 738),
`answerFitsOffer` — 2, `touchesOnlyOwnAnswer` — 2, `roundOpen` — 3;
`'eventTime'`/`'eventLocation'`/`'eventNotes'` в исполняемой части — **0**.

Сила этой проверки названа прямо, чтобы на неё не положились шире: `diff`
уже дал ноль, поэтому любой `grep` по проду и по файлу **обязан** давать
одинаковые числа. Она отвечает не на «правда ли они равны», а на **«тот ли
блок доехал»**.

> **ФАЙЛ РАЗОШЁЛСЯ С ПРОДОМ, И РАЗОШЁЛСЯ ПО-НАСТОЯЩЕМУ — ПЕРЕСНЯТО 20.08.**
>
> **ЗАМЕР 20.08: канарейка 37, из них без `//` — 2.** Файл **1359** строк
> против **1328** в проде; набор в проде подтверждён у него самого —
> `/releases` → `cloud.firestore` → `2a8410bb-d0b8-4609-ba6e-d9d7b05300fe`,
> `updateTime` **14.08 01:32:44Z**. Чем снято — команда ниже, в этом же
> разделе. Обе расходящиеся строки, дословно, и **обе добавлены в файл
> (`>`), из прода не пропало ничего (`<` = 0)**:
>
> ```
> >           && request.resource.data.dates.size() <= 31
> >           && request.resource.data.eventType.size() <= 24
> ```
>
> **ВЫКЛАДКА ТЕПЕРЬ ТРЕБУЕТСЯ.** Это уже не комментарии: два предела
> добавлены коммитом `26be038` (19.08), и его собственное сообщение
> говорит «**НЕ выложены**». До выкладки прод предел не проверяет.
>
> > **ЧТО СТОЯЛО ЗДЕСЬ ДО 20.08 И ПОЧЕМУ ЭТО СНЯТО.** Стояло: «повторный
> > `diff` даст **13 строк, и все они с `//`**… файл — **1335** строк»,
> > и ожидание было прописано как «канарейка 13, второе число 0». Замер
> > верный **на 19.08** и снятый при 1335 строках; `26be038` в тот же день
> > довёл файл до 1359.
> >
> > **И ВОТ ГЛАВНОЕ, РАДИ ЧЕГО ЭТА ВРЕЗКА ОСТАВЛЕНА: перемер БЫЛ СДЕЛАН
> > ТОГДА ЖЕ И БЫЛ ВЕРЕН — но попал только в сообщение коммита `26be038`.**
> > Там стоит дословно: «Расхождение firestore.rules с продом теперь **37
> > СТРОК, ИЗ НИХ ИСПОЛНЯЕМЫХ 2**… первое число будет 37, второе — 2, **а
> > НЕ 0**, как было после N137». Проверить: `git log -1 --format=%B
> > 26be038`.
> >
> > **Читаемый файл при этом сутки говорил 13 и 0.** Это I35 в чистом
> > виде — замер, сохранённый и потерянный одновременно, — и цена его
> > такая: 20.08 по этому файлу было написано, что число «не измерено».
> > Неправда; оно лежало в коммите. **Прежде чем объявить число
> > неизмеренным — спросить `git log` по файлу, который его портит**
> > (I36: «замер недоступен» проверяется попыткой). Разбор — **N150**.
>
> **ЭТО ЧИСЛО — ЧАСТЬ ЗАМЕРА, А НЕ СВОЙСТВО ПРОЕКТА. Оно устаревает при
> КАЖДОЙ правке `firestore.rules`, и пересчитывать его надо ТЕМ ЖЕ
> ЗАХОДОМ, ЧТО И ПРАВКУ** — не «когда-нибудь потом» и не «перед
> выкладкой». Правишь правила — в том же коммите переснимаешь оба числа и
> заменяешь их здесь. Иначе следующий сверяется с числом, которое ты сам
> же и сделал неверным, а выглядит оно как измеренное.
>
> **ПРОВЕРЯЕТСЯ ЭТО ОДНОЙ КОМАНДОЙ, ЧТОБЫ НЕ ЧИТАТЬ ГЛАЗАМИ** (из корня
> репозитория; `<id>` — набор из `/releases`, на 20.08 `2a8410bb-…`;
> **спрашивать его у `/releases`, а не брать отсюда** — запись про набор
> устаревает тем же способом, что и числа):
>
> ```bash
> set -o pipefail
> TOKEN=$(gcloud auth print-access-token)
> curl -s -H "Authorization: Bearer $TOKEN" -H "X-Goog-User-Project: mugam-club" \
>   "https://firebaserules.googleapis.com/v1/projects/mugam-club/rulesets/<id>" \
>   | python3 -c 'import sys,json; print(json.load(sys.stdin)["source"]["files"][0]["content"], end="")' \
>   > /tmp/prod.rules
> echo "код возврата: $?"
> echo -n "всего расходится строк (КАНАРЕЙКА): "
> diff /tmp/prod.rules firestore.rules | grep -c '^[<>]'
> echo -n "из них БЕЗ комментария: "
> diff /tmp/prod.rules firestore.rules | grep '^[<>]' | grep -vc '//'
> echo "--- и САМИ строки, а не только счёт: ---"
> diff /tmp/prod.rules firestore.rules | grep '^[<>]' | grep -v '//'
> ```
>
> **ОЖИДАЕТСЯ НА 22.08, ПОСЛЕ ВЫКЛАДКИ: код возврата 0, канарейка 0,
> второе число 0** — файл и прод сошлись. **Снято при размере файла в 1359
> строк** (столько же в проде); размер назван потому, что число-ожидание
> устаревает при каждой правке `firestore.rules`, а размер сразу показывает,
> от какой редакции оно снято.
>
> **Ноль канарейки сам по себе НИЧЕГО НЕ ЗНАЧИТ (I31):** «расхождений нет» и
> «инструмент слеп» — один вывод. Рядом обязателен тот же `diff` против
> ПРЕЖНЕЙ боевой редакции; 22.08 он дал **37 строк, из них без `//` — 2**, и
> обе строки — те самые пределы.
>
> **Разошлось со вторым числом больше нуля — стоп:** либо в файл уехало
> что-то, чего мы не помним, либо выкладка не состоялась. Назвать строки
> дословно прежде, чем что-либо делать.
>
> **ЧТО СТОЯЛО ЗДЕСЬ ДО 22.08:** «ожидается канарейка 37, второе число 2» —
> верно на 20.08 при файле в 1359 строк и проде в 1328. Пределы выложены
> 22.08, и оба числа стали нулями.
>
> **ПОСЛЕДНЯЯ СТРОКА КОМАНДЫ ДОБАВЛЕНА 20.08, И ВОТ ЗАЧЕМ.** Счёт говорит
> «две», а какие именно — не говорит; две правдоподобные строки и две
> неожиданные дают одно и то же число. **Состав, а не количество** (I13):
> список сам называет, кого назвал, и подменить в нём строку незаметно
> нельзя.
>
> **ВТОРОЕ ЧИСЛО БЕЗ ПЕРВОГО ЧИТАТЬ НЕЛЬЗЯ, и первое стоит здесь именно
> поэтому** (I14, I37). Второе число осмысленно ровно тогда, когда первое
> непусто. Промахнулись путём, `curl` вернул не то, `python3` упал внутри
> трубы — `diff` не увидит ничего, второе число будет **0**, и молчание
> прочитается как порядок. Канарейка отличает «сошлось» от «сравнивать
> нечем».
>
> **ОБА ЧИСЛА — НЕ ВЕЧНЫЕ, И ЭТО ИХ ГЛАВНОЕ СВОЙСТВО.** Они верны, пока
> `firestore.rules` не правился после 20.08, и **любая** правка их меняет —
> в том числе правка одного комментария. Изменилось — сверять по ним
> нельзя: пересчитать и заменить здесь **тем же заходом**. Сами они
> проверяемы: `git diff` по `firestore.rules` от коммита выкладки до
> текущего даёт те же строки.

**Чем это важно для работы:** создание предложения с полем `details`
**проходит** — прод ждёт именно его. Будь в проде прежний `6fceb8b2`,
клиент слал бы `details`, правило требовало бы три поля, и создание
падало бы на `hasOnly` — а человеку это видно как «кнопка не работает».

**`roundOpen()` в проде есть** — это проверено, а не унаследовано.
Разбор до кода 18.08 (создание вечеров клиентом одной пачкой) стоит на
утверждении «однократность уже выложена правилом»; здесь оно подтверждено
живым текстом.

### Чего эта выкладка НЕ делает

**`dates.size() <= 31` в проде НЕТ** — и в файле его тоже нет
(`grep "dates.size()"` по `firestore.rules` 19.08 → одна строка, `> 0`).
Предел 500 операций на `WriteBatch` взят извне и нами не мерен. Это
обязательная правка правил перед шагом 3 плана (создание вечеров пачкой):
**сверка 19.08 закрыла соответствие прода файлу, но тему правил не
закрыла** — выкладка ещё предстоит.

**Четырнадцать полей раунда на документе чата не тронуты** — как и 13.08.
`eventTime`/`eventLocation`/`eventNotes` ушли только из подколлекции
`offers`; на документе чата те же имена держат предложения прежней схемы.

---

## Выложено 13.08, 23:18 UTC — правила Firestore, предложение работы в подколлекции

**Набор `6fceb8b2-eb38-43ea-ae8e-86b9fad6d7d8`** (прежний —
`4c51929f-2148-4df8-9659-66bfd074b9bd` от 12.08, 19:03 UTC).

**ЧТО ИЗМЕНИЛОСЬ В ПРАВАХ — одной строкой:** участники чата получили право
создавать, читать и отвечать на предложения работы в новой подколлекции
`chats/{chatId}/offers/{offerId}`; **ни одно существующее право не изменено и
не отозвано**.

### Сверка — снята с прода, а не с вывода команды выкладки

| проверка | число |
|---|---|
| строк в проде | **1329** |
| строк в файле `firestore.rules` | **1329** |
| `diff` прод против файла | **0 различий**, код возврата 0 |
| канарейка: прод-после против прод-до | **150 строк**, все со знаком `+` |
| **удалено из прежней редакции** | **0** |

**Ноль удалённых — главное число этой выкладки.** Оно означает, что правка
**чисто добавляющая**: ни одно правило, работавшее вчера, не тронуто. Снято
`diff`'ом двух снимков прода (до и после), а не рассуждением.

**Проверка по существу, а не по числу** (I13: счёт бывает слеп): в боевом
тексте `match /offers/{offerId}` — 1, `answerFitsOffer` — 2 (объявление и
вызов), `touchesOnlyOwnAnswer` — 2. То есть доехал именно тот блок.

Текст прода снимался чтением ruleset через `firebaserules.googleapis.com`
(нужен заголовок `x-goog-user-project: mugam-club`, без него ADC отвечает
403). Команды — в шапке этого файла.

### Что теперь разрешено и что запрещено

**Разрешено, четыре хода:** создание (только назвав себя `createdBy`), отметки
дней получателем (только по своему uid и только из `dates` предложения),
принятие инициатором, отзыв инициатором. Раунд закрывается принятием или
отзывом.

**Запрещено:** музыканту трогать даты, тип, время, место, заметку и чужой
ответ; отмечать день не из `dates`; принимать и отзывать. Инициатору —
править отметки музыканта и переписывать `dates` задним числом. Обоим —
удалять предложение.

**Парный тест: 27 из 27**, `functions/test/job-offer-rules.test.ts`, прогон
через `emulators:exec`. Три порчи названы до прогона и сошлись поимённо.

### Чего эта выкладка НЕ делает

**Четырнадцать полей раунда на документе чата не тронуты** — они держат
предложения, созданные прежним путём. Новые правила стоят рядом, а не вместо.

**На трубках нового кода ещё нет** — сборка идёт отдельно. До неё правила
выложены, а звать их некому.

## Выложено 12.08, 07:07 UTC — правила Firestore, состояния вечера (шаг 6)

| | |
|---|---|
| боевой набор | `f0ca5a51-7865-4542-8c53-83324676f84d` |
| выложен | **12.08, 07:07 UTC** (прежний — `50a559f2…` от 11.08) |
| сверка | **1160 строк** в проде и в `firestore.rules`, `diff` — расхождений **ноль** (код возврата 0) |
| канарейка к сверке | тот же `diff` против **прежней боевой редакции** (снята из прода ДО выкладки) даёт **65** строк. Без неё «diff пуст» и «сравнивать нечем» — один вывод (I31) |
| имена ходов в проде | `ownerSetsStatus` **2**, `ownerCancelsOwnEvent` **2**, `ownerRestoresOwnEvent` **2**; в прежней редакции у всех трёх — **0** |
| `'left'` в значениях ответа | **3** вхождения в проде против **1** в прежней редакции |
| канарейка на имена | `answersForSelf` — **2** и до, и после: разбор видит имена, а не молчит |

**ЧТО ИЗМЕНИЛОСЬ В ПРАВАХ, одной строкой: владелец вечера теперь может сам
менять его состояние — переводить между «в силе» и «под вопросом», отменять и
возвращать СВОЙ ЛИЧНЫЙ вечер (у вечера с договорённостью отмена по-прежнему
идёт только через согласие второй стороны), а участник может ответить пятым
значением `left` — «вышел из состава».**

**ПРОВЕРЕНО НА ТРУБКЕ 12.08, СРАЗУ ПОСЛЕ ВЫКЛАДКИ — 4 хода из 4, на живых
данных прода.** Личный вечер 14 августа, 16:30 («Toy», состав — один Теймур):

| Ход | Правило | Что наблюдалось |
|---|---|---|
| в силе → под вопросом | `ownerSetsStatus` | плашка «Dəqiq» → «Şübhə altında» |
| под вопросом → в силе | `ownerSetsStatus` | вернулась в «Dəqiq» |
| отмена личного вечера | `ownerCancelsOwnEvent` | отменился **сразу**, без спроса второй стороны |
| возврат из отмены | `ownerRestoresOwnEvent` | вернулся в силу |

Отказов по правам — **ноль**: ни одного «Alınmadı». До этой выкладки все четыре
хода правило **запрещало**, то есть проверялись они в проде впервые.

**Заодно подтверждены три вещи, к правам не относящиеся, но проверенные тем же
заходом:** окошко подсвечивает **текущее** состояние (после перевода в «под
вопросом» золотом обведена уже «İş dəqiq deyil»); обе ветки `statusChoices`
дают **разную подпись под отменой** — у личного вечера «tədbir təqvimdə
qalacaq», у договорённости 24 августа «**Teymur Orucov təsdiqləməlidir**», и
разница видна **до** нажатия; при `ownerDoubt` строка повода под плашкой
**пуста**, как и решено 11.08.

**Отмену договорённости НЕ нажимали намеренно:** за кнопкой стоит
`requestAgreementCancel` — старый ход, проверенный 12.08 во всех трёх исходах,
и нажатие ушло бы живым запросом на настоящую работу. Проверена **читаемая
половина** — подпись, отличающая договорённость от личного вечера.

**Оговорка, без которой строка выше вводит в заблуждение: `left` в правилах
разрешён, а писать его НЕКОМУ.** Ни одно место в `lib` этого значения не
записывает (N121, замер с канарейкой: слово `left` встречается в `lib` 138 раз,
записей значения — ноль). Выход сегодня по-прежнему идёт через `leavesEvent()`,
то есть стиранием человека из `musicians`. **Право выложено, ход не написан.**

## Выложено 11.08, 15:25 UTC — правила Firestore, шаг 4 «участник отвечает за себя»

| | |
|---|---|
| боевой набор | `50a559f2-031b-48a4-8619-abf8eac850d3` |
| выложен | **11.08, 15:25 UTC** (прежний — `2432135e…` от 09.08) |
| сверка | **1102 строки** в проде и в `firestore.rules`, `diff` — расхождений **ноль**; `answersForSelf` — **2 вхождения в проде, 0 в прежней редакции** |
| канарейка к сверке | тот же `diff` против редакции ДО правила (`HEAD~1`) даёт **52** строки. Без неё «diff пуст» и «сравнивать нечем» — один вывод (I31) |

**Строк в проде читается 1103, если считать `count('\n')+1`, и 1102 через
`grep -c`** — разница в завершающем переводе строки, а не расхождение. Названо,
чтобы следующий не искал потерянную строку.

**ЧТО ИЗМЕНИЛОСЬ В ПРАВАХ, одной строкой: участник мероприятия теперь может
записать СВОЙ ответ (`going` / `waiting` / `cant`) в `answers` — и больше
ничего; до этой выкладки писать в чужой документ не мог никто, кроме владельца
и шести названных ходов.**

Чего выкладка НЕ изменила и что надо знать сразу: **владелец по-прежнему может
переписать `answers` целиком, включая чужие ответы** — его ветка разрешает
любые поля, кроме отмены. Это заведено отдельной работой (`docs/plan.md`,
«владелец может ответить за участника»); правилом шага 4 не закрывается по
устройству.

Проверено до выкладки: **15 из 15** в эмуляторе
(`functions/test/answers-rules.test.ts`, 4 `assertSucceeds` и 11
`assertFails`), весь набор functions — **281 из 281**. Проверка возвратом:
снятие `affectedKeys().hasOnly([uid])` роняет ровно два теста, и именно
названных.

---

## Выложено 09.08, 21:40 UTC — правила Firestore, Часть 6а

| | |
|---|---|
| боевой набор | `2432135e-55d3-4b30-a152-d54dbe488aa3` |
| выложен | **09.08, 21:40 UTC** (прежний — `a84fce83…` от 06.08) |
| сверка | 1055 строк в проде, 1055 в `firestore.rules`, `diff` пуст, `restoresEvent` — 2 вхождения в обоих |
| канарейка к сверке | тот же `diff` против редакции ДО 6а (`9bf634e`) даёт **56** строк. Без неё «diff пуст» и «сравнивать нечем» — один вывод (I31) |

Что уехало: **`restoresEvent()`** — возврат договора из «под вопросом» в
силу, и его имя в `allow update`. `hasOnly` на три ключа, только владелец,
только из `unsettled`, только из повода `memberLeft`, только в `agreed`,
значение прибито. До этой строки записать `agreed` в существующий документ
не мог никто.

**Работать этому пока негде** — см. ниже: сервер стоит.

## Функции выложены 09.08, 23:26 UTC — Часть 6а, сервер

Оплата восстановлена владельцем; счёт `Firebase Payment`
(`017F87-47CE1C-5721BD`) снова `open: True`, проект привязан к нему.

| | |
|---|---|
| выложено | **все 31**, `europe-west3` (плюс `onChatMediaUploaded` в `us-east1`) |
| новая | **`markEventUnsettled`** — тридцать первая |
| правка | `remindUpcomingEventsHourly` — третий вид напоминания `unsettled24h` и ветка «вечер под вопросом» |
| прогон до выкладки | functions **258 из 258**, отдельным вызовом, вывод в файл |
| сверка на проде | **состав имён, а не количество**: 31 в проде, 31 в коде, разность пуста в ОБЕ стороны, `markEventUnsettled` найден |

**Простой длился шесть часов: 09.08, 17:16 → 23:16 UTC.** Шесть проваленных
почасовых проходов подряд, последний отказ в 22:16, первый успешный запуск
в 23:16.

### Что простой стоил — ноль, и это доказано, а не понадеяно

Три вечных расхождения, названные заранее (`docs/handoff.md`), проверены
поимённо ПОСЛЕ оживления:

| Расхождение | Замер | Итог |
|---|---|---|
| договор из согласия не создан | `censusLostAgreements` | 12 чатов, 72 договора, `recipientAgreed == true` — **0**, пропаж **0**. Числа те же, что до восстановления |
| `messageCount` отстал | `censusMessageCount` | 377 сообщений, расхождение **0**, сообщений с начала простоя **0** |
| удалённый остался в поиске | сверка индекса Algolia с `users` | в Firestore **12**, в индексе **12**, призраков **0**, непроиндексированных **0** |

**Причина нуля названа замером, а не догадкой: событий за простой не было
вовсе.**

- **пачки задним числом не пришло** — после 23:10 в журнале три записи, все
  от почасового прохода. Канарейка к этой пустоте: тот же запрос на окне
  до простоя (10:00–17:16) даёт **153 записи по семи функциям**, то есть
  запрос триггеры видит;
- **очередь была пуста всё время** — история
  `pubsub/subscription/num_undelivered_messages` за окно простоя:
  **18 подписок отдали ряды, пик непоставленных — 0**. Восемнадцать рядов
  здесь и есть канарейка: будь метрика недоступна, рядов было бы ноль, а
  не ряды из нулей.

**Что из этого следует на будущее, и это не про везение.** Удержание в
подписках — 86400 с, и оживление уложилось в шесть часов. Но проверка
показала, что запас не понадобился: терять было нечего. **Тот же простой в
рабочий вечер стоил бы ровно столько, сколько за него написали бы люди.**

---

## ФУНКЦИИ НЕ БЫЛИ ВЫЛОЖЕНЫ, ПРОД СТОЯЛ — снято 09.08, 21:50 UTC (история, закрыто выше)

**Оба платёжных счёта проекта ЗАКРЫТЫ**, и Cloud Functions в проде не
выполняются вовсе.

```bash
gcloud billing accounts list --format="value(name,displayName,open)"
# 0156A8-F147BA-AA5C6F  My Billing Account  False
# 017F87-47CE1C-5721BD  Firebase Payment    False
```

**`gcloud billing projects describe mugam-club` отвечает
`billingEnabled: True` — и это неправда по смыслу.** Признак говорит, что
проект ПРИВЯЗАН к счёту, а не что счёт живой. Живость — у самого счёта,
поле `open`. Класс тот же, что у `✓ Built` при разошедшейся подписи (N89):
признак отвечает на соседний вопрос.

**Что снято делом:**

| Что проверено | Чем | Ответ |
|---|---|---|
| выкладка функций | `firebase deploy --only functions` | **код возврата 1**, отказ на Secret Manager (403, «requires billing»), ни одна функция не тронута |
| выполняются ли функции | `gcloud logging read 'textPayload:"billing is disabled"'` | **5 отказов подряд**, ежечасно, `remindUpcomingEventsHourly` |
| с какого часа | тот же журнал | первый отказ **09.08, 17:16 UTC** (21:16 местного) |
| состав в проде | `gcloud functions list` | **30**, все `ACTIVE`, у всех `updateTime` = **09.08 17:14 UTC** — это отметка ОСТАНОВКИ, а не выкладки: она за две минуты до первого отказа |
| Cloud Run | `gcloud run services list` | `Ready: True` у всех. **Готовность не означает исполнения** — вызовы всё равно отказывают |
| данные Firestore | REST-чтение `users?pageSize=1` | **HTTP 200**, читается. Данные живы, стоит только сервер |

**Чем доказано, что стоят ВСЕ функции, а не одна.** Прямо — только
почасовой: ночью больше ничего не звалось, и в журнале лежит лишь он.
Отказ приходит от проекта (`The request failed because billing is disabled
for this project`), не от кода функции, поэтому вывод «стоят все» —
**вывод, а не замер**. Замером он станет на первом же действии в
приложении.

**Что это значит для людей на трубках:** переписка пишется в Firestore
клиентом напрямую и работает; **не работает всё, что делает сервер** —
уведомления любые, напоминания, превью и счётчики чата (`onNewMessage`,
`refreshChatPreview`), переиндексация поиска, выдача токена для звонков
(`generateAgoraToken`), то есть сами звонки.

**Часть 6а до восстановления оплаты не проверяется ни на шаг:** состояние
«под вопросом» пишет ТОЛЬКО сервер (`markEventUnsettled`), а его в проде
нет и запустить нечем.

### Счёт потерь снят ДО восстановления — 09.08, 22:00 UTC

**Почему до, а не после.** Часть событий доедет из очереди задним числом и
создаст договоры сама; после этого «доехало» и «не терялось» станут
неотличимы, и счёта потерь не снять уже ничем. Снимок ниже — единственное,
с чем можно будет сравнить.

Снимается заново: из `functions/` — `npm run build`, затем
`node lib/scripts/censusLostAgreements.js`. Только чтение.

| | |
|---|---|
| чатов всего | **12** |
| договоров (`personalEvents`) всего | **72**, из них с `agreementChatId` — **26**, привязаны к **8** чатам |
| чатов с `recipientAgreed == true` | **0** |
| у них договор нашёлся (чат + отметка раунда) | **0** |
| **договора НЕТ — потеряно на эту минуту** | **0** |

**Канарейка к нулю** (он утверждает отсутствие, I31): раскладка
`recipientAgreed` по всем 12 чатам — **поля нет у 11, `false` у 1,
`true` ни у одного**. Значит ноль означает «таких чатов нет», а не «разбор
слеп».

**Слепое пятно, названное вместе со счётом:** сверка видит пропажу, только
пока признак согласия ещё стоит. Новое предложение в том же чате пишет
`recipientAgreed: false` и стирает след навсегда. Поэтому ноль здесь —
«на эту минуту не потеряно», а не «не потеряется».

---

## Снято с прода 06.08 (все времена — UTC)

### Правила Firestore — СОШЛИСЬ С РЕПОЗИТОРИЕМ

| | |
|---|---|
| боевой набор | `a84fce83-9979-47dd-a4db-ca5308c4bb54` |
| выложен | **06.08, 22:07** |
| сверка | 996 строк в проде, 996 в `firestore.rules`, `diff` — расхождений ноль |

Что уехало этой выкладкой:

- `clearsCancelFields`, `withdrawsCancelRequest`, `declinesCancelRequest` —
  серверная половина работы №6 (`7bd98dd`, 04.08): отзыв и отказ двумя
  названными дорогами;
- `requestsCancel` / `confirmsCancel` — четыре и пять ключей вместо двух и
  трёх, `stampsSelf()`, имя поступка, и условие `status != 'cancelled'`,
  закрывающее **щель N36**;
- `namesOnlySelf` — защита трёх полей «кто это сделал» на документе чата
  (N37, `1dc783b`);
- `namesCancelDeed` — владельческая ветка `personalEvents` больше не
  пропускает ходы отмены мимо правил согласия;
- `isParty()` — **владелец ∪ `musicians`**, и `allow read` зовёт её же
  (N49, `a7f3e53`). Без этого владелец не смог бы отменить свой договор на
  **11 из 26** договоров прода, а экран, по N45, назвал бы ложную причину.

Проверено на проде после выкладки: все шесть функций на месте, `isParty()`
содержит ветку владельца.

**Что стало возможно с этой минуты:** отмена договора по согласию — все
четыре хода, на сборке, которая уже стоит на обеих трубках. До выкладки
они получали отказ по правам (N45), и экран объяснял его выдуманной
причиной.

**Функции этой выкладкой НЕ трогались** и не требовали её: в
`functions/src` после сборки `6b6db2d` менялись только скрипты переписи,
которые функциями не разворачиваются. Триггер уведомлений об отмене в
проде с 05.08.

### Правила Storage — сходятся с репозиторием

| | |
|---|---|
| боевой набор | `e4a00a55-c679-4726-a2bd-8fd02ecc2ba4` |
| выложен | **14.07, 07:22** |
| сверка | `diff` с `storage.rules` — расхождений ноль |

### Функции — 26 штук, `europe-west3` (плюс `onChatMediaUploaded` в `us-east1`)

| Что | Когда |
|---|---|
| общий деплой всех функций | **03.08, 18:38–22:09** (несколько заходов подряд: `remindUpcomingEventsHourly` 19:19, `onPersonalEventCreated` 20:00, `onJobOfferRoundChanged` 21:03, `onChatUpdated` 22:09) |
| `onPersonalEventUpdated` — **переразвёрнута отдельно** | **05.08, 04:50** |

Что вошло в отдельную выкладку `onPersonalEventUpdated` 05.08:

- `clearReadMark` — снятие отметки прочтения у второй стороны при правке
  мероприятия (половина починки N40);
- вместе с ней — **весь `eventNotifications.ts` на тот момент, включая
  триггер уведомлений об отмене** (`d45b773`, 04.08 18:11 UTC): четыре
  хода, четыре адресата, признак нового запроса по сдвигу
  `cancelRequestedAt`. Он живёт внутри `onPersonalEventUpdated`
  (`planUpdatePushes`), отдельной функцией не является.

> **Как это установлено и чего эта запись НЕ доказывает.** Время выкладки
> (05.08 04:50) позже коммита `6b6db2d` (05.08 04:38) и много позже
> `d45b773` — значит выложенный бандл собран из дерева, где триггер уже
> был. Это **вывод из времён, а не сверка исходников**: содержимое бандла
> напрямую не читалось. Сверка станет бесплатной на первом же прогоне
> отмены — но только после выкладки правил, иначе ход не запишется вовсе
> и молчание уведомления не будет значить ничего (I9: проверка, которая не
> может провалиться, ничего не доказывает).

Переменные окружения (сняты с `sweepDeadPushTokensDaily`, общие для
деплоя): `PUSH_TOKEN_SWEEP_DELETE=true`, `ORPHAN_SWEEP_DELETE=true` —
удаление включено у обоих сборщиков.

### Индексы

4 составных индекса в проде, 4 в `firestore.indexes.json` — сходятся
(N15). Плюс 2 field override в файле, включая TTL на `statuses.expiresAt`
(I3: удалять индексы только точечно по id, `--force` не использовать).

---

## Что лежит в репозитории и ждёт выкладки

| Что | Коммит | Почему ждёт |
|---|---|---|
| Правила работы №6 — отзыв и отказ двумя названными дорогами | `7bd98dd` | решение владельца 04.08: выкладывать одним заходом после UI. UI готов с 04.08 (`16d838c`) — условие выполнено, деплой просто не сделан |
| Щель N36 — `status != 'cancelled'` в `confirmsCancel` | `7bd98dd` | тем же заходом |
| `namesOnlySelf` — N37 | `1dc783b` | тем же заходом |
| Клиент: N39/N40, отмена по согласию | `6b6db2d`, `16d838c` | не деплой, а сборка на трубки; по записи сессии 05.08 обе трубки собраны, но ни одна проверка на устройстве не пройдена |

### Что едет выкладкой 08.08 — три разные вещи и 24 попутные

**Считать их одним «деплоем» нельзя: у них разная цена ошибки и разный
способ проверки.**

**1. N76 — имя автора в строке группы.** Меняет четыре функции:
`onNewMessage`, `onMessageDeleted`, `onMessageTombstoned`,
`refreshChatPreview`. Два новых поля на документе чата. Проверяется на
трубках после пересборки — до неё не видно ничего.

**2. N43 — экономия на индексе.** Меняет `onUserWritten`. Проверяется
журналом за двадцать минут (ниже).

**3. ТРИГГЕР ОТМЕНЫ ДОГОВОРА — едет В ПРОД ВПЕРВЫЕ.** `onPersonalEventUpdated`,
написан **04.08** и с тех пор не проверялся ничем, кроме тестов. Он не
«заодно поехавший»: до сегодня его поведения в проде не существовало
вовсе. **После выкладки прогнать на трубках как новую работу** — со своим
списком шагов, а не галочкой в конце чужого.

**4. Остальные 24 функции переразвернутся ТЕМ ЖЕ кодом.** Мы их не
трогали. Знать об этом надо заранее по одной причине: **если сломается
что-то в них, причина будет не в коде, а в самой выкладке** — окружение,
версия рантайма, права, секреты. Искать тогда надо не правку, а деплой, и
это экономит первый час паники.

Всего в проекте **30 функций**: 9 вызываемых, 5 на создание документа, 6
на изменение, 4 на удаление, 2 на запись, 1 на файл, 3 по расписанию.
Выборочно мы не деплоим — `firebase deploy --only functions` разворачивает
все.

**Старым сборкам на трубках выкладка не повредит — проверено по коду, а не
рассуждением.** Клиент без правки N76 новых полей не читает: строгого
списка ключей у него нет (`Chat.fromFirestore` берёт named-ключи, лишние
игнорируются), а записать документ чата целиком он может ровно в одном
месте — и там `SetOptions(merge: true)`. Ни падения, ни затирания.

### Функции — правка N43 лежит в репозитории и НЕ выложена (07.08)

Коммит `e071884`: `shouldReindexUser` в `functions/src/algoliaShared.ts`,
выход по нему в `onUserWritten`. В проде до сих пор работает **прежняя**
функция — Algolia переписывается на каждое сердцебиение присутствия.

**Сегодня проверено окно, экономия не проверена ничем.** Это разные вещи,
и путать их нельзя:

- **окно поиска** (`onlineQueryWindow`, 11 минут) едет на трубки со
  сборкой, и пункт 12 списка проверок его проверяет — но только с одной
  стороны: он может опровергнуть сужение окна и НЕ может подтвердить
  экономию, потому что при невыложенной функции отметка в индексе свежая
  и выдача будет полной даже при сломанном пороге (I9);
- **экономия** (шаг в 10 минут) живёт в облачной функции и до выкладки не
  существует в проде вовсе. Проверяется отдельно и с прямого слова
  владельца.

**Чем проверять экономию в день выкладки** — числом записей в индекс за
сутки на одного человека с открытым приложением: до правки ≈1440
(сердцебиение раз в 60 с), после — ≈144. **Считается ТОЛЬКО по счётчику операций индексирования Algolia.**

**Поправка 08.08, до выкладки: «либо по логу вызовов `onUserWritten`» —
неверно, и дало бы ложный ответ «экономии нет».** Функция срабатывает на
КАЖДУЮ запись в `users/{uid}`, то есть на каждое сердцебиение, и после
правки будет срабатывать столько же. Экономится не вызов, а обращение к
Algolia: `shouldReindexUser` возвращает `false` и функция просто выходит
— **без единой строки в журнале** (в коде на этом пути нет ни `logger`,
ни чего-либо ещё). Считать по вызовам значит считать то, что не менялось.

Расхождение больше чем вдвое от ≈1440/≈144 — само по себе находка.

Порядок, записанный в `docs/test-steps.md`: сначала первый заход проверок
на нынешней сборке, потом серверная часть одним заходом, потом пересборка
обеих трубок и второй заход — уже обязательно вдвоём.
