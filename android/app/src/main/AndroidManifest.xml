<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for LocationPickerScreen (Geolocator.getCurrentPosition) —
         without these, Geolocator.requestPermission() can never actually
         grant anything on Android, so location-sharing silently falls
         back to LocationPickerScreen's hardcoded Baku fallback center
         every time instead of the device's real GPS fix. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:label="mugam_flutter"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- google_maps_flutter (location sharing, Part B) — Firebase-issued
             key, already restricted in Google Cloud Console to this app's
             package name + release/debug SHA-1 fingerprints and to the
             Maps SDK for Android API only, so embedding it here (the
             package's own documented integration point) isn't a secrecy
             concern the way a backend API key would be. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAcN7WN0ahmPpVJeMspSjpohTbukmFQ_gI" />
        <!-- Required by the pasteboard plugin (lib/core/media clipboard
             image paste) to expose files via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for canLaunchUrl(Uri.parse('waze://'))/
             canLaunchUrl(Uri.parse('comgooglemaps://')) to correctly
             detect these apps on Android 11+ package-visibility
             restrictions. The earlier <package android:name="..."/> form
             tried here first was NOT enough — that grants visibility for
             lookups BY PACKAGE NAME (explicit intents, getPackageInfo),
             but canLaunchUrl constructs an IMPLICIT intent
             (ACTION_VIEW + a data scheme, no package name attached),
             which needs a matching <intent> filter declaration instead —
             confirmed via url_launcher's own documented Android 11+
             requirement after Teymur found the app-picker never appeared
             even with both apps genuinely installed. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="waze"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="comgooglemaps"/>
        </intent>
    </queries>
</manifest>
