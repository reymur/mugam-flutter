import 'package:flutter/material.dart';

const Color kBg = Color(0xFF0C0A06);
const Color kBg2 = Color(0xFF131009);
const Color kBg3 = Color(0xFF1C1710);
const Color kCard = Color(0xFF18140D);
const Color kBorder = Color(0x26D4A03C);
const Color kGold = Color(0xFFD4A03C);
const Color kGold2 = Color(0xFFF0C060);
const Color kGoldDim = Color(0x14D4A03C);
const Color kRed = Color(0xFFC0392B);
const Color kGreen = Color(0xFF27AE60);
const Color kText = Color(0xFFF5EAD8);
const Color kMuted = Color(0xFF8A7A60);

/// Всё, что лежит НА золотом: текст кнопки, значок, крутилка, галочки
/// доставки на своём пузыре.
///
/// **Имя пришло не из головы — оно уже было в коде ЧЕТЫРЕЖДЫ.** До
/// 08.08 `_kOnGold` объявлялся приватно в `activity_type_screen.dart`,
/// `chats_screen.dart`, `filter_sheet.dart` и `search_screen.dart`:
/// четверо независимо ощутили нужду и назвали одинаково. Такое совпадение
/// — довод сильнее любого рассуждения о том, как назвать правильно.
///
/// **Имя по РОЛИ, а не по виду.** «Тёмно-коричневый» соврал бы при первой
/// же смене палитры: `kGold` уедет на другой оттенок, а имя останется
/// описывать прежний цвет. «На золотом» переживёт смену — потому что
/// описывает не краску, а место.
///
/// Проверено 08.08 поштучно: **60 употреблений, все на золотом.** 53 — по
/// `kGold`/`kGold2` рядом, 5 — по `isMe` (свой пузырь золотой,
/// `chat_screen.dart:3326`), 2 — чтением (галочки доставки; рядом с
/// каждой стоит парная `overlayCheckColor` для медиа). Несовпадений ноль.
const Color kOnGold = Color(0xFF1A0E00);

/// Всё, что лежит НА красном: число внутри бейджа непрочитанного,
/// счётчик активных фильтров.
///
/// **Пара к `kOnGold`, и заведена 08.08 по той же причине.** Значение —
/// белое, и в этом весь смысл имени: сегодня «белый», завтра `kRed`
/// станет светлее, и на нём понадобится тёмный. Роль переживёт смену,
/// слово «белый» соврёт.
///
/// Проверено поштучно: **7 мест, все на `kRed` внутри `BoxShape.circle`.**
/// Ещё два места с белым текстом на заливке НЕ переведены и разобраны
/// ниже — они не механические.
const Color kOnRed = Color(0xFFFFFFFF);

// ДВА МЕСТА, КОТОРЫЕ ВЫГЛЯДЯТ ТАКИМИ ЖЕ И НЕ ЯВЛЯЮТСЯ. Не переводить не
// глядя.
//
// 1. `agreements_screen.dart:491` — бейдж, у которого заливка МЕНЯЕТСЯ:
//    `badgeRed ? Color(0xFFFF3B30) : kGold`, а текст всегда белый. То
//    есть на золотой заливке стоит белое, хотя правило этого файла
//    говорит `kOnGold`. Замена здесь — не уборка, а ИЗМЕНЕНИЕ ВИДА:
//    надпись на золотом станет тёмной. Решение за владельцем.
//    (Заодно видно, что красных у нас два: системный `0xFFFF3B30` тут и
//    палитровый `kRed` = `0xFFC0392B` во всех остальных бейджах.)
//
// 2. `agreements_screen.dart:2356` — `backgroundColor: color`,
//    `foregroundColor: Colors.white`, где `color` приходит параметром.
//    Заливка произвольная, значит белое здесь — не «на красном», а
//    умолчание для неизвестного фона. Имени по роли ему дать нельзя,
//    пока роль не определена.

// ПРОИЗВОДНОЕ ОТ КОНСТАНТЫ НЕ ПОЛУЧАЕТ СВОЕГО ИМЕНИ.
//
// `Color(0x40D4A03C)` — это не новый цвет, это `kGold` с прозрачностью
// 25%. Заведи ему имя — и связь с исходным порвётся: `kGold` уедет на
// другой оттенок, а производное останется прежним, потому что в нём
// записана краска, а не отношение к ней.
//
// Пишется `kGold.withValues(alpha: 0.25)`, а не новая константа. Шесть
// таких мест найдено 08.08 (четыре от `kGold`, два от `kGreen`) — и ни
// одно имени не получило.
//
// Исключение уже стоит выше: `kBorder` и `kGoldDim` — производные от
// золота, названные ДО этого правила. Их не трогаем, но новых таких не
// заводим.

// БЕЛОЕ И ЧЁРНОЕ ПОВЕРХ МЕДИА — НЕ ПАЛИТРА. Не чинить.
//
// В `lib/` есть 88 мест с `Colors.white`, `Colors.black` и их
// полупрозрачными вариантами: просмотр статуса, видео в чате, камера,
// просмотрщик вложений, экран звонка. **Это не недосмотр, и заменять их
// на `kText`/`kBg` нельзя.**
//
// Причина, а не запрет: там фон — ЧУЖОЕ ИЗОБРАЖЕНИЕ, и он произвольный.
// `kText` (тёплый светлый) на светлом снимке не виден вовсе, а `kBg` на
// тёмном сливается. Белое с чёрной тенью — единственное, что читается
// поверх любого кадра.
//
// **Различение уже живёт в коде без имени** — пары вроде
// `checkColor` / `overlayCheckColor` в `chat_screen.dart`: первая для
// своего золотого пузыря, вторая для той же галочки поверх картинки.
// Когда дойдут руки до спорных мест, делить их надо именно по этому
// признаку — «на нашем фоне» против «поверх чужого», — а не по тому,
// какой класс цвета написан.

// ПРЕДУПРЕЖДЕНИЕ О КОНФЛИКТЕ — набор из шести, переехал сюда 08.08.
//
// Жил в `event_conflict_banner.dart`, а использовался ДВУМЯ файлами:
// им и `event_conflict_dialog.dart`. То есть общим набор был по
// факту, а лежал в одном из потребителей — второй тянул его импортом
// из соседнего виджета.
//
// ПОЧЕМУ СВОИ, А НЕ `kRed` С ПРОЗРАЧНОСТЬЮ.
//
// Фон обоих окон почти чёрный (`kBg2` = 0xFF131009), а сам `kRed`
// (0xFFC0392B) — тёмная кирпичная краска. Прежняя заливка `kRed`
// прозрачностью 22 давала панель, неотличимую от фона, а подпись
// `kRed.withAlpha(200)` на ней была нечитаема — замечено владельцем на
// устройстве 03.08 («немножко черноватая, не очень читабельно»).
//
// Осветлять прозрачностью здесь нечего в принципе: она смешивает с ЧЁРНЫМ
// фоном, то есть чем «легче» альфа, тем ближе к фону, а не к белому.
// Поэтому взяты явные светлые тона.
//
// Разделение ролей: заголовок красный — это предупреждение; строка
// мероприятия почти белая и крупная — это факт, который надо прочитать, а
// не сигнал тревоги. Красным по чёрному фактические данные читаются хуже
// всего, и именно эта строка на устройстве не читалась.
const Color kWarnBg = Color(0x33C0392B);
const Color kWarnBorder = Color(0x99C0392B);
const Color kWarnTitle = Color(0xFFF08A7A);
const Color kWarnHint = Color(0xFFDDA79E);
const Color kWarnFact = Color(0xFFE9D9D3);
const Color kWarnIconBg = Color(0x59C0392B);

// Отдельно от палитры: `Colors.transparent` — не цвет, а его отсутствие.
// 43 места, имени не требуют.

// ТРИ ВРЕМЕННЫХ ЗАМЕСТИТЕЛЯ, заведены 07.08 для дневного экрана.
//
// Макет (`docs/design/mugam-1-esas.html`) строит текст на ЧЕТЫРЁХ
// уровнях, а в палитре их два — `kText` и `kMuted`. Не хватало не
// одного уровня, а двух: вторичного и приглушённого.
//
// Взяты ТЁПЛЫМИ эквивалентами макетных `#A8A8A2`, `#8A8A85`, `#3A3A38`,
// а не самими макетными: нейтральный серый рядом с коричневым `kMuted`
// даёт не иерархию, а грязь — разная температура при одинаковой
// светлоте.
//
// **В работе 7б они ЗАМЕНЯЮТСЯ на макетные, а не встают рядом с ними.**
// Иначе здесь окажется шесть уровней вместо четырёх, и через месяц никто
// не вспомнит, почему их шесть.
const Color kTextSecondary = Color(0xFFBFB093);
const Color kTextDim = Color(0xFF5F564A);
const Color kBarOff = Color(0xFF3A322A);
// Darker and clearly distinct from kMuted — used for the voice message
// "not read yet" cursor/wave state, which needs to stay visible against
// the light gold outgoing-bubble background (kMuted is too close to gold
// in brightness there) while remaining unmistakably different from
// kMuted's own "read but not listened to" wave state.
const Color kUnreadGray = Color(0xFF4A4A4A);
const Color kReadBlue = Color(0xFF1A6B9E);
// Brighter/more saturated than kReadBlue — kReadBlue's luminance is too
// close to kMuted's (the "read but not listened" wave color) to read as
// a clearly different state at a glance on a thin waveform bar. This is
// WhatsApp's own read-tick blue, picked specifically for its strong
// luminance/saturation gap from kMuted. Used only for the voice message
// "listened" wave color, not the dot or checkmarks (which keep kReadBlue).
const Color kListenedBlue = Color(0xFF2D9DCF);

const double kRadius = 18.0;
const double kNavH = 68.0;
